# Code Index

> Generated: 2026-03-26T17:46:03Z | 72 files | Rust

## Project Structure

```
├── crates/
│   ├── crt-core/
│   │   ├── benches/
│   │   │   └── terminal.rs
│   │   └── src/
│   │       ├── lib.rs
│   │       └── pty.rs
│   ├── crt-renderer/
│   │   └── src/
│   │       ├── background_image.rs
│   │       ├── effects/
│   │       │   ├── grid.rs
│   │       │   ├── matrix.rs
│   │       │   ├── mod.rs
│   │       │   ├── motion.rs
│   │       │   ├── particles.rs
│   │       │   ├── rain.rs
│   │       │   ├── renderer.rs
│   │       │   ├── shape.rs
│   │       │   ├── sprite.rs
│   │       │   └── starfield.rs
│   │       ├── glyph_cache.rs
│   │       ├── golden.rs
│   │       ├── grid_renderer.rs
│   │       ├── headless.rs
│   │       ├── lib.rs
│   │       ├── mock.rs
│   │       ├── rect_renderer.rs
│   │       ├── shaders/
│   │       │   └── mod.rs
│   │       ├── shared_pipelines.rs
│   │       ├── sprite_renderer.rs
│   │       ├── tab_bar/
│   │       │   ├── layout.rs
│   │       │   ├── mod.rs
│   │       │   ├── state.rs
│   │       │   └── vello_renderer.rs
│   │       ├── terminal_vello.rs
│   │       └── traits.rs
│   └── crt-theme/
│       ├── benches/
│       │   └── theme_parsing.rs
│       └── src/
│           ├── lib.rs
│           └── parser.rs
├── src/
│   ├── app/
│   │   ├── effects.rs
│   │   ├── handler.rs
│   │   ├── initialization.rs
│   │   ├── menu_actions.rs
│   │   └── mod.rs
│   ├── bin/
│   │   ├── benchmark.rs
│   │   ├── benchmark_gpu.rs
│   │   ├── profile_gpu_memory.rs
│   │   └── profile_memory.rs
│   ├── config.rs
│   ├── font.rs
│   ├── gpu/
│   │   ├── buffer_pool.rs
│   │   ├── mod.rs
│   │   └── texture_pool.rs
│   ├── input/
│   │   ├── drag.rs
│   │   ├── key_encoder.rs
│   │   ├── keyboard.rs
│   │   ├── mod.rs
│   │   └── mouse.rs
│   ├── main.rs
│   ├── menu.rs
│   ├── profiling.rs
│   ├── render/
│   │   ├── context_menu.rs
│   │   ├── dialogs.rs
│   │   ├── mod.rs
│   │   ├── overlays.rs
│   │   └── selection.rs
│   ├── theme_registry.rs
│   ├── watcher.rs
│   └── window/
│       ├── interaction.rs
│       ├── mod.rs
│       ├── overrides.rs
│       ├── render.rs
│       ├── types.rs
│       └── ui.rs
└── tests/
    ├── common/
    │   └── mod.rs
    ├── shell_tests.rs
    ├── terminal_tests.rs
    └── visual_tests.rs
```

## Modules

### crates/crt-core/benches

> *Semantic summary to be generated by AI agent.*

#### crates/crt-core/benches/terminal.rs

-  `generate_terminal_output` function L9-36 — `(lines: usize, cols: usize) -> Vec<u8>` — Generate realistic terminal output: mixed ASCII with ANSI escape sequences.
-  `bench_process_input` function L39-60 — `(c: &mut Criterion)` — Benchmark terminal text processing throughput (process_input)
-  `bench_all_lines_text` function L63-84 — `(c: &mut Criterion)` — Benchmark all_lines_text() extraction
-  `bench_content_hash` function L87-118 — `(c: &mut Criterion)` — Benchmark content hash computation (renderable_content iteration)
-  `bench_damage_check` function L121-142 — `(c: &mut Criterion)` — Benchmark damage tracking query

### crates/crt-core/src

> *Semantic summary to be generated by AI agent.*

#### crates/crt-core/src/lib.rs

- pub `pty` module L8 — `-` — This crate provides:
- pub `SemanticZone` enum L40-50 — `Unknown | Prompt | Input | Output` — Semantic zone type from OSC 133 shell integration
- pub `ShellEvent` enum L57-64 — `Bell | CommandSuccess | CommandFail` — Shell events that can trigger theme effects
- pub `TerminalEventProxy` struct L75-77 — `{ queue: Arc<SegQueue<Event>> }` — Terminal event handler that collects events for the application.
- pub `new` function L80-84 — `() -> Self` — - PTY process management (via portable-pty)
- pub `take_events` function L87-93 — `(&self) -> Vec<Event>` — Take all pending events (lock-free drain)
- pub `Size` struct L110-113 — `{ columns: usize, lines: usize }` — Terminal size in characters
- pub `new` function L116-118 — `(columns: usize, lines: usize) -> Self` — - PTY process management (via portable-pty)
- pub `Terminal` struct L122-134 — `{ term: Term<TerminalEventProxy>, event_proxy: TerminalEventProxy, parser: ansi:...` — CRT Terminal wrapper around alacritty_terminal
- pub `new` function L138-154 — `(size: Size) -> Self` — Create a new terminal with the given size
- pub `size` function L157-159 — `(&self) -> Size` — Get terminal dimensions
- pub `columns` function L162-164 — `(&self) -> usize` — Get the number of columns
- pub `screen_lines` function L167-169 — `(&self) -> usize` — Get the number of visible lines
- pub `process_input` function L175-192 — `(&mut self, bytes: &[u8])` — Process input bytes through the terminal parser
- pub `get_line_zone` function L312-317 — `(&self, line: i32) -> SemanticZone` — Get semantic zone for a given line
- pub `has_semantic_zones` function L323-325 — `(&self) -> bool` — Check if any OSC 133 zones have been detected
- pub `current_zone` function L328-330 — `(&self) -> SemanticZone` — Get the current semantic zone state
- pub `renderable_content` function L333-335 — `(&self) -> term::RenderableContent<'_>` — Get access to renderable content (cells, cursor, etc.)
- pub `cursor` function L338-340 — `(&self) -> term::RenderableCursor` — Get the cursor information
- pub `cursor_mode_visible` function L343-345 — `(&self) -> bool` — Check if cursor should be visible (based on SHOW_CURSOR mode)
- pub `mode` function L348-350 — `(&self) -> TermMode` — Get terminal mode flags
- pub `take_events` function L353-355 — `(&self) -> Vec<Event>` — Take pending terminal events
- pub `take_shell_events` function L360-362 — `(&mut self) -> Vec<ShellEvent>` — Take pending shell events (bell, command success/fail)
- pub `resize` function L365-369 — `(&mut self, size: Size)` — Resize the terminal
- pub `inner` function L372-374 — `(&self) -> &Term<TerminalEventProxy>` — Access the underlying Term for advanced operations
- pub `inner_mut` function L377-379 — `(&mut self) -> &mut Term<TerminalEventProxy>` — Mutable access to the underlying Term
- pub `damage` function L385-387 — `(&mut self) -> TermDamage<'_>` — Get damage information since last reset
- pub `reset_damage` function L393-395 — `(&mut self)` — Reset damage state after rendering
- pub `has_damage` function L398-403 — `(&mut self) -> bool` — Check if any damage exists (needs redraw)
- pub `damaged_line_set` function L409-422 — `(&mut self) -> Option<Vec<usize>>` — Get the set of damaged line indices (0-based visible lines).
- pub `start_selection` function L425-429 — `(&mut self, point: Point, selection_type: SelectionType)` — Start a new selection at the given point
- pub `update_selection` function L432-437 — `(&mut self, point: Point)` — Update the selection end point
- pub `clear_selection` function L440-442 — `(&mut self)` — Clear the current selection
- pub `has_selection` function L445-447 — `(&self) -> bool` — Check if a selection exists
- pub `selection_to_string` function L450-452 — `(&self) -> Option<String>` — Get the selection as text, if any
- pub `scroll` function L458-460 — `(&mut self, scroll: alacritty_terminal::grid::Scroll)` — Scroll the terminal viewport
- pub `display_offset` function L464-466 — `(&self) -> usize` — Get the current display offset (how far scrolled into history)
- pub `is_scrolled_back` function L469-471 — `(&self) -> bool` — Check if the terminal is scrolled back (not showing live output)
- pub `scroll_to_bottom` function L474-477 — `(&mut self)` — Scroll to the bottom (show live output)
- pub `total_lines` function L480-482 — `(&self) -> usize` — Get total number of lines including history
- pub `history_size` function L485-487 — `(&self) -> usize` — Get history size (lines above visible area)
- pub `all_lines_text` function L491-524 — `(&self) -> Vec<(i32, String)>` — Get all lines as text (history + visible), returns Vec of (line_index, text)
- pub `bracketed_paste_enabled` function L527-529 — `(&self) -> bool` — Check if bracketed paste mode is enabled
- pub `ShellTerminalGeneric` struct L535-538 — `{ terminal: Terminal, pty: P }` — A terminal connected to a PTY backend running a shell.
- pub `ShellTerminal` type L541 — `= ShellTerminalGeneric<Pty>` — Backward-compatible alias for the concrete PTY implementation
- pub `new` function L545-550 — `(size: Size) -> anyhow::Result<Self>` — Create a new shell terminal with the given size
- pub `with_cwd` function L553-558 — `(size: Size, cwd: std::path::PathBuf) -> anyhow::Result<Self>` — Create a new shell terminal with a specific working directory
- pub `with_shell` function L561-566 — `(size: Size, shell: &str) -> anyhow::Result<Self>` — Create a new shell terminal with a specific shell
- pub `with_options` function L571-576 — `(size: Size, options: SpawnOptions) -> anyhow::Result<Self>` — Create a new shell terminal with full spawn options
- pub `pty` function L579-581 — `(&self) -> &Pty` — Get access to the PTY
- pub `with_backend` function L586-589 — `(size: Size, pty: P) -> Self` — Create a shell terminal with a custom PTY backend
- pub `working_directory` function L592-594 — `(&self) -> Option<std::path::PathBuf>` — Get the current working directory of the shell process
- pub `process_pty_output` function L598-630 — `(&mut self) -> bool` — Process any available PTY output through the terminal
- pub `send_input` function L633-635 — `(&self, data: &[u8])` — Send keyboard input to the PTY
- pub `resize` function L638-641 — `(&mut self, size: Size)` — Resize both the terminal and PTY
- pub `terminal` function L644-646 — `(&self) -> &Terminal` — Get access to the terminal for rendering
- pub `terminal_mut` function L649-651 — `(&mut self) -> &mut Terminal` — Get mutable access to the terminal
- pub `take_events` function L654-656 — `(&self) -> Vec<Event>` — Take any pending terminal events (title changes, bells, etc.)
- pub `check_title_change` function L660-663 — `(&self) -> Option<String>` — Check for title change and return it, preserving other events
- pub `check_events` function L670-691 — `(&self) -> (Option<String>, bool)` — Check for terminal events and return title changes and bell triggers
- pub `take_shell_events` function L700-719 — `(&mut self) -> (Vec<ShellEvent>, Option<String>)` — Take shell events for theme triggers (bell, command success/fail)
- pub `start_selection` function L722-724 — `(&mut self, point: Point, selection_type: SelectionType)` — Start a new selection at the given point
- pub `update_selection` function L727-729 — `(&mut self, point: Point)` — Update the selection end point
- pub `clear_selection` function L732-734 — `(&mut self)` — Clear the current selection
- pub `has_selection` function L737-739 — `(&self) -> bool` — Check if a selection exists
- pub `selection_to_string` function L742-744 — `(&self) -> Option<String>` — Get the selection as text, if any
- pub `scroll` function L747-749 — `(&mut self, scroll: crate::Scroll)` — Scroll the terminal viewport
- pub `display_offset` function L752-754 — `(&self) -> usize` — Get the current display offset (how far scrolled into history)
- pub `is_scrolled_back` function L757-759 — `(&self) -> bool` — Check if the terminal is scrolled back (not showing live output)
- pub `scroll_to_bottom` function L762-764 — `(&mut self)` — Scroll to the bottom (show live output)
- pub `bracketed_paste_enabled` function L767-769 — `(&self) -> bool` — Check if bracketed paste mode is enabled
- pub `MockPty` struct L994-999 — `{ output_queue: std::collections::VecDeque<Vec<u8>>, captured_input: std::cell::...` — Mock PTY backend for deterministic testing without real shell processes
- pub `with_output` function L1003-1010 — `(chunks: Vec<Vec<u8>>) -> Self` — Create a MockPty with pre-loaded output chunks
- pub `captured_input` function L1013-1015 — `(&self) -> Vec<u8>` — Get all input that was written to this mock PTY
-  `TerminalEventProxy` type L79-94 — `= TerminalEventProxy` — - PTY process management (via portable-pty)
-  `TerminalEventProxy` type L96-100 — `impl Default for TerminalEventProxy` — - PTY process management (via portable-pty)
-  `default` function L97-99 — `() -> Self` — - PTY process management (via portable-pty)
-  `TerminalEventProxy` type L102-106 — `impl EventListener for TerminalEventProxy` — - PTY process management (via portable-pty)
-  `send_event` function L103-105 — `(&self, event: Event)` — - PTY process management (via portable-pty)
-  `Size` type L115-119 — `= Size` — - PTY process management (via portable-pty)
-  `Terminal` type L136-530 — `= Terminal` — - PTY process management (via portable-pty)
-  `scan_osc133` function L199-263 — `(&mut self, bytes: &[u8])` — Scan input bytes for OSC 133 semantic prompt sequences
-  `handle_osc133` function L266-307 — `(&mut self, cmd: u8, exit_code: Option<i32>)` — Handle an OSC 133 command
-  `ShellTerminal` type L543-582 — `= ShellTerminal` — - PTY process management (via portable-pty)
-  `tests` module L773-1082 — `-` — - PTY process management (via portable-pty)
-  `create_terminal` function L779-783 — `()` — - PTY process management (via portable-pty)
-  `process_simple_text` function L786-790 — `()` — - PTY process management (via portable-pty)
-  `bell_event_triggered` function L793-808 — `()` — - PTY process management (via portable-pty)
-  `shell_terminal_integration` function L811-831 — `()` — - PTY process management (via portable-pty)
-  `osc133_no_zones_initially` function L834-839 — `()` — - PTY process management (via portable-pty)
-  `osc133_prompt_start_with_bel` function L842-851 — `()` — - PTY process management (via portable-pty)
-  `osc133_prompt_start_with_st` function L854-862 — `()` — - PTY process management (via portable-pty)
-  `osc133_full_sequence` function L865-893 — `()` — - PTY process management (via portable-pty)
-  `osc133_embedded_in_other_data` function L896-904 — `()` — - PTY process management (via portable-pty)
-  `osc133_unknown_command_ignored` function L907-916 — `()` — - PTY process management (via portable-pty)
-  `osc133_d_command_success` function L919-928 — `()` — - PTY process management (via portable-pty)
-  `osc133_d_command_fail` function L931-940 — `()` — - PTY process management (via portable-pty)
-  `osc133_d_command_fail_with_larger_code` function L943-952 — `()` — - PTY process management (via portable-pty)
-  `osc133_d_no_exit_code_defaults_success` function L955-964 — `()` — - PTY process management (via portable-pty)
-  `shell_events_clear_after_take` function L967-977 — `()` — - PTY process management (via portable-pty)
-  `multiple_shell_events_accumulated` function L980-991 — `()` — - PTY process management (via portable-pty)
-  `MockPty` type L1001-1016 — `= MockPty` — - PTY process management (via portable-pty)
-  `MockPty` type L1018-1049 — `impl PtyBackend for MockPty` — - PTY process management (via portable-pty)
-  `write` function L1019-1021 — `(&self, data: &[u8])` — - PTY process management (via portable-pty)
-  `try_read` function L1023-1027 — `(&self) -> Option<Vec<u8>>` — - PTY process management (via portable-pty)
-  `read_available` function L1029-1032 — `(&self) -> Vec<u8>` — - PTY process management (via portable-pty)
-  `resize` function L1034-1036 — `(&self, cols: u16, rows: u16)` — - PTY process management (via portable-pty)
-  `shutdown` function L1038-1040 — `(&self)` — - PTY process management (via portable-pty)
-  `process_id` function L1042-1044 — `(&self) -> Option<u32>` — - PTY process management (via portable-pty)
-  `working_directory` function L1046-1048 — `(&self) -> Option<std::path::PathBuf>` — - PTY process management (via portable-pty)
-  `test_mock_pty_terminal` function L1052-1081 — `()` — - PTY process management (via portable-pty)

#### crates/crt-core/src/pty.rs

- pub `SpawnOptions` struct L14-23 — `{ shell: Option<String>, cwd: Option<PathBuf>, semantic_prompts: bool, shell_ass...` — Options for spawning a shell with semantic prompt support
- pub `PtyInput` enum L26-33 — `Data | Resize | Shutdown` — Messages sent to the PTY writer thread
- pub `PtyBackend` interface L39-54 — `{ fn write(), fn try_read(), fn read_available(), fn resize(), fn shutdown(), fn...` — Trait abstracting PTY I/O operations.
- pub `Pty` struct L57-66 — `{ input_tx: Sender<PtyInput>, output_rx: Receiver<Vec<u8>>, recycle_tx: Sender<V...` — PTY handle for communicating with a shell process
- pub `spawn` function L156-158 — `(shell: Option<&str>, cols: u16, rows: u16) -> anyhow::Result<Self>` — Spawn a new shell in a PTY
- pub `spawn_with_options` function L161-247 — `(cols: u16, rows: u16, options: SpawnOptions) -> anyhow::Result<Self>` — Spawn a new shell with full options including semantic prompt support
- pub `spawn_with_cwd` function L250-288 — `( shell: Option<&str>, cols: u16, rows: u16, cwd: Option<PathBuf>, ) -> anyhow::...` — Spawn a new shell in a PTY with a specific working directory
- pub `process_id` function L291-293 — `(&self) -> Option<u32>` — Get the process ID of the shell
- pub `working_directory` function L297-300 — `(&self) -> Option<PathBuf>` — Get the current working directory of the shell process
- pub `write` function L303-305 — `(&self, data: &[u8])` — Write data to the PTY (keyboard input)
- pub `try_read` function L308-310 — `(&self) -> Option<Vec<u8>>` — Try to read available output from the PTY (non-blocking)
- pub `read_available` function L315-323 — `(&self) -> Vec<u8>` — Read all available output from the PTY (non-blocking)
- pub `resize` function L326-328 — `(&self, cols: u16, rows: u16)` — Resize the PTY
- pub `shutdown` function L331-333 — `(&self)` — Shutdown the PTY
- pub `ShellType` enum L418-423 — `Bash | Zsh | Fish | Unknown` — Shell types for semantic prompt integration
-  `spawn_pty_threads` function L73-152 — `( pair: portable_pty::PtyPair, child: Box<dyn portable_pty::Child + Send + Sync>...` — Spawn reader and writer threads for a PTY pair and return the Pty handle.
-  `READ_BUF_SIZE` variable L87 — `: usize` — Handles spawning shell processes and I/O between the shell and terminal.
-  `Pty` type L154-334 — `= Pty` — Handles spawning shell processes and I/O between the shell and terminal.
-  `Pty` type L336-364 — `impl PtyBackend for Pty` — Handles spawning shell processes and I/O between the shell and terminal.
-  `write` function L337-339 — `(&self, data: &[u8])` — Handles spawning shell processes and I/O between the shell and terminal.
-  `try_read` function L341-343 — `(&self) -> Option<Vec<u8>>` — Handles spawning shell processes and I/O between the shell and terminal.
-  `read_available` function L345-347 — `(&self) -> Vec<u8>` — Handles spawning shell processes and I/O between the shell and terminal.
-  `resize` function L349-351 — `(&self, cols: u16, rows: u16)` — Handles spawning shell processes and I/O between the shell and terminal.
-  `shutdown` function L353-355 — `(&self)` — Handles spawning shell processes and I/O between the shell and terminal.
-  `process_id` function L357-359 — `(&self) -> Option<u32>` — Handles spawning shell processes and I/O between the shell and terminal.
-  `working_directory` function L361-363 — `(&self) -> Option<PathBuf>` — Handles spawning shell processes and I/O between the shell and terminal.
-  `Pty` type L366-370 — `impl Drop for Pty` — Handles spawning shell processes and I/O between the shell and terminal.
-  `drop` function L367-369 — `(&mut self)` — Handles spawning shell processes and I/O between the shell and terminal.
-  `get_process_cwd` function L374-400 — `(pid: u32) -> Option<PathBuf>` — Get the current working directory of a process by PID
-  `get_process_cwd` function L404-408 — `(pid: u32) -> Option<PathBuf>` — Get the current working directory of a process by PID
-  `get_process_cwd` function L412-414 — `(_pid: u32) -> Option<PathBuf>` — Fallback for other platforms
-  `detect_shell_type` function L426-439 — `(shell: &str) -> ShellType` — Detect shell type from shell path or name
-  `tests` module L442-498 — `-` — Handles spawning shell processes and I/O between the shell and terminal.
-  `wait_for_pty_text` function L447-464 — `(pty: &Pty, expected: &str, timeout: Duration) -> String` — Poll PTY until expected text appears or timeout
-  `spawn_shell` function L467-497 — `()` — Handles spawning shell processes and I/O between the shell and terminal.

### crates/crt-renderer/src

> *Semantic summary to be generated by AI agent.*

#### crates/crt-renderer/src/background_image.rs

- pub `ImageFrame` struct L15-24 — `{ data: Vec<u8>, width: u32, height: u32, delay: Duration }` — A frame of an animated image
- pub `LoadedImage` enum L28-41 — `Static | Animated` — Loaded background image data (static or animated)
- pub `from_path` function L45-59 — `(path: impl AsRef<Path>) -> Result<Self, String>` — Load an image from file path
- pub `dimensions` function L129-141 — `(&self) -> (u32, u32)` — Get current frame dimensions
- pub `current_data` function L144-153 — `(&self) -> &[u8]` — Get current frame data
- pub `update_animation` function L156-174 — `(&mut self) -> bool` — Check if animation frame needs update, returns true if texture should be updated
- pub `is_animated` function L177-179 — `(&self) -> bool` — Check if this is an animated image
- pub `BackgroundTexture` struct L183-189 — `{ texture: wgpu::Texture, view: wgpu::TextureView, sampler: wgpu::Sampler, width...` — GPU texture for background image
- pub `new` function L193-237 — `(device: &wgpu::Device, queue: &wgpu::Queue, image: &LoadedImage) -> Self` — Create a new texture from loaded image data
- pub `update` function L240-261 — `(&self, queue: &wgpu::Queue, image: &LoadedImage)` — Update texture data (for animated images)
- pub `create_sampler_with_repeat` function L264-292 — `( device: &wgpu::Device, repeat: BackgroundRepeat, ) -> wgpu::Sampler` — Create sampler with specific repeat mode
- pub `BackgroundImageState` struct L296-300 — `{ image: LoadedImage, texture: BackgroundTexture, config: BackgroundImage }` — Complete background image state for rendering
- pub `new` function L304-323 — `( device: &wgpu::Device, queue: &wgpu::Queue, config: &BackgroundImage, ) -> Res...` — Load and create background image state
- pub `update` function L326-333 — `(&mut self, queue: &wgpu::Queue) -> bool` — Update animation state, returns true if texture was updated
- pub `calculate_uv_transform` function L337-431 — `(&self, screen_width: f32, screen_height: f32) -> [f32; 4]` — Calculate UV transform for background sizing and positioning
- pub `opacity` function L434-436 — `(&self) -> f32` — Get opacity
-  `LoadedImage` type L43-180 — `= LoadedImage` — Supports loading static images (PNG, JPEG) and animated GIFs with frame timing.
-  `load_static` function L62-74 — `(path: &Path) -> Result<Self, String>` — Load a static image (PNG, JPEG, etc.)
-  `load_gif` function L77-126 — `(path: &Path) -> Result<Self, String>` — Load a GIF (may be animated)
-  `BackgroundTexture` type L191-293 — `= BackgroundTexture` — Supports loading static images (PNG, JPEG) and animated GIFs with frame timing.
-  `BackgroundImageState` type L302-437 — `= BackgroundImageState` — Supports loading static images (PNG, JPEG) and animated GIFs with frame timing.
-  `tests` module L440-459 — `-` — Supports loading static images (PNG, JPEG) and animated GIFs with frame timing.
-  `test_uv_transform_cover_wide_screen` function L444-458 — `()` — Supports loading static images (PNG, JPEG) and animated GIFs with frame timing.

#### crates/crt-renderer/src/glyph_cache.rs

- pub `GlyphStyle` struct L15-18 — `{ bold: bool, italic: bool }` — Text style flags for glyph rendering
- pub `new` function L21-23 — `(bold: bool, italic: bool) -> Self` — Uses fixed-width grid positioning for terminal rendering.
- pub `GlyphKey` struct L28-32 — `{ character: char, size_tenths: u16, style: GlyphStyle }` — Key for glyph lookup - character + size + style
- pub `new` function L35-41 — `(character: char, size: f32) -> Self` — Uses fixed-width grid positioning for terminal rendering.
- pub `with_style` function L43-49 — `(character: char, size: f32, style: GlyphStyle) -> Self` — Uses fixed-width grid positioning for terminal rendering.
- pub `CachedGlyph` struct L54-64 — `{ uv_min: [f32; 2], uv_max: [f32; 2], width: f32, height: f32, offset_x: f32, of...` — Cached glyph data - position in atlas + metrics for positioning
- pub `PositionedGlyph` struct L151-161 — `{ x: f32, y: f32, width: f32, height: f32, uv_min: [f32; 2], uv_max: [f32; 2] }` — Positioned glyph ready for rendering
- pub `FontVariants` struct L165-170 — `{ regular: Vec<u8>, bold: Option<Vec<u8>>, italic: Option<Vec<u8>>, bold_italic:...` — Font variant data
- pub `new` function L173-180 — `(regular: Vec<u8>) -> Self` — Uses fixed-width grid positioning for terminal rendering.
- pub `with_bold` function L182-185 — `(mut self, data: Vec<u8>) -> Self` — Uses fixed-width grid positioning for terminal rendering.
- pub `with_italic` function L187-190 — `(mut self, data: Vec<u8>) -> Self` — Uses fixed-width grid positioning for terminal rendering.
- pub `with_bold_italic` function L192-195 — `(mut self, data: Vec<u8>) -> Self` — Uses fixed-width grid positioning for terminal rendering.
- pub `GlyphCache` struct L214-243 — `{ fonts: FontVariants, scale_context: ScaleContext, font_size: f32, line_height_...` — Glyph cache with GPU texture atlas
- pub `with_variants` function L247-254 — `( device: &wgpu::Device, fonts: FontVariants, font_size: f32, line_height_multip...` — Create a new glyph cache with font variants and line height multiplier
- pub `new` function L257-264 — `( device: &wgpu::Device, font_data: &[u8], font_size: f32, ) -> Result<Self, &'s...` — Create a new glyph cache with a single font (backwards compatible)
- pub `get_or_insert` function L370-372 — `(&mut self, character: char) -> Option<CachedGlyph>` — Get or create a cached glyph (regular style)
- pub `get_or_insert_styled` function L375-490 — `( &mut self, character: char, style: GlyphStyle, ) -> Option<CachedGlyph>` — Get or create a cached glyph with specific style
- pub `position_char` function L494-501 — `( &mut self, character: char, cell_x: f32, cell_y: f32, ) -> Option<PositionedGl...` — Position a character at a fixed grid cell (regular style)
- pub `position_char_styled` function L505-533 — `( &mut self, character: char, cell_x: f32, cell_y: f32, style: GlyphStyle, ) -> ...` — Position a styled character at a fixed grid cell
- pub `flush` function L536-562 — `(&mut self, queue: &wgpu::Queue)` — Upload pending glyphs to GPU
- pub `atlas_stats` function L565-572 — `(&self) -> (usize, f32, u32, u32)` — Get atlas capacity stats for monitoring
- pub `precache_ascii` function L575-581 — `(&mut self)` — Pre-cache ASCII characters
- pub `font_size` function L583-585 — `(&self) -> f32` — Uses fixed-width grid positioning for terminal rendering.
- pub `line_height` function L587-589 — `(&self) -> f32` — Uses fixed-width grid positioning for terminal rendering.
- pub `cell_width` function L591-593 — `(&self) -> f32` — Uses fixed-width grid positioning for terminal rendering.
- pub `baseline_offset` function L596-598 — `(&self) -> f32` — Get baseline offset from top of cell
- pub `strikethrough_offset` function L602-609 — `(&self) -> f32` — Get strikethrough position from top of cell
- pub `set_font_size` function L615-684 — `(&mut self, queue: &wgpu::Queue, new_font_size: f32)` — Update font size for zoom functionality
-  `GlyphStyle` type L20-24 — `= GlyphStyle` — Uses fixed-width grid positioning for terminal rendering.
-  `GlyphKey` type L34-50 — `= GlyphKey` — Uses fixed-width grid positioning for terminal rendering.
-  `AtlasPacker` struct L67-77 — `{ width: u32, height: u32, row_x: u32, row_y: u32, row_height: u32, pixels_alloc...` — Atlas packing state with capacity monitoring
-  `AtlasPacker` type L79-147 — `= AtlasPacker` — Uses fixed-width grid positioning for terminal rendering.
-  `new` function L80-90 — `(width: u32, height: u32) -> Self` — Uses fixed-width grid positioning for terminal rendering.
-  `total_pixels` function L93-95 — `(&self) -> u64` — Total atlas area in pixels
-  `utilization` function L98-100 — `(&self) -> f32` — Current utilization as a fraction (0.0 to 1.0)
-  `allocate` function L102-137 — `(&mut self, glyph_width: u32, glyph_height: u32) -> Option<(u32, u32)>` — Uses fixed-width grid positioning for terminal rendering.
-  `reset` function L140-146 — `(&mut self)` — Reset packing state (for atlas clear/rebuild)
-  `FontVariants` type L172-211 — `= FontVariants` — Uses fixed-width grid positioning for terminal rendering.
-  `get_for_style` function L198-210 — `(&self, style: GlyphStyle) -> &[u8]` — Get font data for the given style, falling back to regular
-  `GlyphCache` type L245-685 — `= GlyphCache` — Uses fixed-width grid positioning for terminal rendering.
-  `new_internal` function L266-367 — `( device: &wgpu::Device, fonts: FontVariants, font_size: f32, line_height_multip...` — Uses fixed-width grid positioning for terminal rendering.
-  `GlyphCache` type L687-692 — `impl Drop for GlyphCache` — Uses fixed-width grid positioning for terminal rendering.
-  `drop` function L688-691 — `(&mut self)` — Uses fixed-width grid positioning for terminal rendering.

#### crates/crt-renderer/src/golden.rs

- pub `DEFAULT_TOLERANCE` variable L28 — `: f64` — Default tolerance: allow up to 0.5% of pixels to differ.
- pub `ComparisonResult` struct L32-44 — `{ matched: bool, diff_percentage: f64, diff_pixels: usize, total_pixels: usize, ...` — Result of comparing a rendered frame against a golden file.
- pub `compare_with_golden` function L56-82 — `( actual_png: &[u8], golden_path: &Path, tolerance: f64, ) -> ComparisonResult` — Compare actual rendered pixels (raw RGBA) against a golden file.
- pub `compare_images` function L85-152 — `(actual: &RgbaImage, golden: &RgbaImage, tolerance: f64) -> ComparisonResult` — Compare two RGBA images with the given tolerance percentage.
- pub `golden_path` function L158-165 — `(golden_dir: &Path, test_name: &str) -> PathBuf` — Resolve the platform-specific golden file path for a test name.
- pub `update_golden` function L168-174 — `(actual_png: &[u8], path: &Path)` — Write the actual image as the new golden file (for `UPDATE_GOLDEN=1`).
- pub `save_failure_artifacts` function L177-191 — `( failures_dir: &Path, test_name: &str, actual_png: &[u8], result: &ComparisonRe...` — Save failure artifacts (actual + diff) to the failures directory.
- pub `should_update_golden` function L194-198 — `() -> bool` — Check if golden files should be updated (via `UPDATE_GOLDEN=1`).
- pub `env_tolerance` function L201-205 — `() -> Option<f64>` — Read the tolerance override from `VISUAL_TEST_TOLERANCE`, if set.
- pub `assert_visual_match` function L211-254 — `( actual_png: &[u8], test_name: &str, project_root: &Path, tolerance: Option<f64...` — Convenience function: run a full visual comparison for a test.
-  `PIXEL_THRESHOLD` variable L49 — `: u8` — Per-pixel color distance threshold.
-  `tests` module L257-356 — `-` — ```
-  `solid_png` function L260-266 — `(width: u32, height: u32, color: [u8; 4]) -> Vec<u8>` — ```
-  `test_identical_images_match` function L269-278 — `()` — ```
-  `test_slightly_different_within_tolerance` function L281-292 — `()` — ```
-  `test_different_images_fail` function L295-305 — `()` — ```
-  `test_diff_image_generated_on_mismatch` function L308-324 — `()` — ```
-  `test_dimension_mismatch_fails` function L327-333 — `()` — ```
-  `test_pixel_threshold_ignores_minor_differences` function L336-344 — `()` — ```
-  `test_golden_path_platform_suffix` function L347-355 — `()` — ```

#### crates/crt-renderer/src/grid_renderer.rs

- pub `GlyphInstance` struct L17-28 — `{ pos: [f32; 2], uv_min: [f32; 2], uv_max: [f32; 2], size: [f32; 2], color: [f32...` — Per-instance data for a glyph
- pub `from_positioned` function L31-39 — `(glyph: &PositionedGlyph, color: [f32; 4]) -> Self` — All text renders in a single draw call.
- pub `GridRenderer` struct L58-70 — `{ shared: Arc<SharedGridPipeline>, globals_buffer: wgpu::Buffer, instance_capaci...` — Grid renderer using instanced quads
- pub `MAX_INSTANCES` variable L74 — `: usize` — Maximum number of glyph instances per render call
- pub `INSTANCE_BUFFER_SIZE` variable L77-78 — `: u64` — Size of instance buffer in bytes (32K instances * 48 bytes = 1.5 MB)
- pub `create_instance_buffer` function L84-91 — `(device: &wgpu::Device) -> wgpu::Buffer` — Create an instance buffer for use with this renderer
- pub `new_with_shared` function L97-108 — `(device: &wgpu::Device, shared: &Arc<SharedGridPipeline>) -> Self` — Create a grid renderer using shared pipeline objects.
- pub `new` function L114-126 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — Create a grid renderer with its own pipeline objects.
- pub `set_glyph_cache` function L141-164 — `( &mut self, device: &wgpu::Device, glyph_cache: &crate::glyph_cache::GlyphCache...` — Update the bind group with a new glyph cache atlas
- pub `clear` function L167-169 — `(&mut self)` — Clear pending instances
- pub `push_glyphs` function L172-179 — `(&mut self, glyphs: &[PositionedGlyph], color: [f32; 4])` — Add positioned glyphs from layout
- pub `update_screen_size` function L182-194 — `(&mut self, queue: &wgpu::Queue, width: f32, height: f32)` — Update screen size uniform (only writes if size changed)
- pub `render` function L200-224 — `( &'a self, queue: &wgpu::Queue, render_pass: &mut wgpu::RenderPass<'a>, instanc...` — Upload instances and render
- pub `instance_count` function L226-228 — `(&self) -> usize` — All text renders in a single draw call.
-  `GlyphInstance` type L30-40 — `= GlyphInstance` — All text renders in a single draw call.
-  `Globals` struct L45-48 — `{ screen_size: [f32; 2], atlas_size: [f32; 2] }` — Global uniforms for the grid shader
-  `GridRenderer` type L72-229 — `= GridRenderer` — All text renders in a single draw call.
-  `create_globals_buffer` function L128-138 — `(device: &wgpu::Device) -> wgpu::Buffer` — All text renders in a single draw call.
-  `GridRenderer` type L231-237 — `impl Drop for GridRenderer` — All text renders in a single draw call.
-  `drop` function L232-236 — `(&mut self)` — All text renders in a single draw call.

#### crates/crt-renderer/src/headless.rs

- pub `HeadlessError` enum L10-19 — `NoAdapter | DeviceCreation | BufferMap | PngEncode` — Errors from headless rendering operations.
- pub `HeadlessRenderer` struct L25-36 — `{ device: wgpu::Device, queue: wgpu::Queue, render_texture: wgpu::Texture, rende...` — Offscreen wgpu renderer for visual regression tests.
- pub `new` function L46-48 — `(width: u32, height: u32) -> Result<Self, HeadlessError>` — Create a new headless renderer with the given dimensions.
- pub `with_options` function L54-132 — `( width: u32, height: u32, force_fallback: bool, ) -> Result<Self, HeadlessError...` — Create a headless renderer, optionally forcing software fallback.
- pub `device` function L135-137 — `(&self) -> &wgpu::Device` — The GPU device.
- pub `queue` function L140-142 — `(&self) -> &wgpu::Queue` — The GPU command queue.
- pub `texture_view` function L145-147 — `(&self) -> &wgpu::TextureView` — The render target texture view (use as a color attachment).
- pub `format` function L150-152 — `(&self) -> wgpu::TextureFormat` — The texture format used by this renderer.
- pub `width` function L155-157 — `(&self) -> u32` — Width in pixels.
- pub `height` function L160-162 — `(&self) -> u32` — Height in pixels.
- pub `capture_frame` function L168-225 — `(&self) -> Result<Vec<u8>, HeadlessError>` — Capture the current render texture contents as raw RGBA pixels.
- pub `capture_png` function L228-241 — `(&self) -> Result<Vec<u8>, HeadlessError>` — Capture the current render texture as PNG-encoded bytes.
- pub `submit_and_capture` function L246-249 — `(&self, encoder: wgpu::CommandEncoder) -> Result<Vec<u8>, HeadlessError>` — Submit a command encoder and immediately capture the frame as raw RGBA.
- pub `clear_and_capture` function L254-282 — `(&self, color: wgpu::Color) -> Result<Vec<u8>, HeadlessError>` — Clear the render texture to a solid color, then capture as raw RGBA.
-  `COPY_BYTES_PER_ROW_ALIGNMENT` variable L39 — `: u32` — Minimum alignment for buffer copy rows required by wgpu.
-  `HeadlessRenderer` type L41-283 — `= HeadlessRenderer` — texture, and captures the result as raw RGBA pixels or PNG bytes.
-  `tests` module L286-373 — `-` — texture, and captures the result as raw RGBA pixels or PNG bytes.
-  `test_create_headless_renderer` function L290-306 — `()` — texture, and captures the result as raw RGBA pixels or PNG bytes.
-  `test_clear_and_capture_solid_color` function L309-342 — `()` — texture, and captures the result as raw RGBA pixels or PNG bytes.
-  `test_capture_png_produces_valid_image` function L345-372 — `()` — texture, and captures the result as raw RGBA pixels or PNG bytes.

#### crates/crt-renderer/src/lib.rs

- pub `background_image` module L16 — `-` — text when it actually changes.
- pub `effects` module L17 — `-` — text when it actually changes.
- pub `glyph_cache` module L18 — `-` — text when it actually changes.
- pub `golden` module L19 — `-` — text when it actually changes.
- pub `grid_renderer` module L20 — `-` — text when it actually changes.
- pub `headless` module L21 — `-` — text when it actually changes.
- pub `mock` module L22 — `-` — text when it actually changes.
- pub `rect_renderer` module L23 — `-` — text when it actually changes.
- pub `shaders` module L24 — `-` — text when it actually changes.
- pub `shared_pipelines` module L25 — `-` — text when it actually changes.
- pub `sprite_renderer` module L26 — `-` — text when it actually changes.
- pub `tab_bar` module L27 — `-` — text when it actually changes.
- pub `terminal_vello` module L28 — `-` — text when it actually changes.
- pub `traits` module L29 — `-` — text when it actually changes.
- pub `BackgroundPipeline` struct L71-77 — `{ shared: Arc<SharedBackgroundPipeline>, uniform_buffer: wgpu::Buffer, bind_grou...` — Background pipeline - renders gradient + animated grid
- pub `new_with_shared` function L80-106 — `(device: &wgpu::Device, shared: &Arc<SharedBackgroundPipeline>) -> Self` — text when it actually changes.
- pub `new` function L108-111 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — text when it actually changes.
- pub `set_theme` function L113-115 — `(&mut self, theme: Theme)` — text when it actually changes.
- pub `theme` function L117-119 — `(&self) -> &Theme` — text when it actually changes.
- pub `update_uniforms` function L121-125 — `(&self, queue: &wgpu::Queue, width: f32, height: f32)` — text when it actually changes.
- pub `render` function L127-131 — `(&'a self, render_pass: &mut wgpu::RenderPass<'a>)` — text when it actually changes.
- pub `BackgroundImageUniforms` struct L137-144 — `{ uv_transform: [f32; 4], opacity: f32, _pad: [f32; 3] }` — Uniform buffer for background image shader
- pub `BackgroundImagePipeline` struct L147-150 — `{ shared: Arc<SharedBackgroundImagePipeline>, uniform_buffer: wgpu::Buffer }` — Background image pipeline - renders textured background with sizing/positioning
- pub `new_with_shared` function L153-170 — `(device: &wgpu::Device, shared: &Arc<SharedBackgroundImagePipeline>) -> Self` — text when it actually changes.
- pub `new` function L172-175 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — text when it actually changes.
- pub `create_bind_group` function L177-200 — `( &self, device: &wgpu::Device, texture_view: &wgpu::TextureView, ) -> wgpu::Bin...` — text when it actually changes.
- pub `update_uniforms` function L202-209 — `(&self, queue: &wgpu::Queue, uv_transform: [f32; 4], opacity: f32)` — text when it actually changes.
- pub `render` function L211-219 — `( &'a self, render_pass: &mut wgpu::RenderPass<'a>, bind_group: &'a wgpu::BindGr...` — text when it actually changes.
- pub `CompositePipeline` struct L223-228 — `{ shared: Arc<SharedCompositePipeline>, uniform_buffer: wgpu::Buffer, theme: The...` — Composite pipeline - blends text onto screen with glow
- pub `new_with_shared` function L231-247 — `(device: &wgpu::Device, shared: &Arc<SharedCompositePipeline>) -> Self` — text when it actually changes.
- pub `new` function L249-252 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — text when it actually changes.
- pub `set_theme` function L254-256 — `(&mut self, theme: Theme)` — text when it actually changes.
- pub `theme` function L258-260 — `(&self) -> &Theme` — text when it actually changes.
- pub `create_bind_group` function L262-285 — `( &self, device: &wgpu::Device, text_texture_view: &wgpu::TextureView, ) -> wgpu...` — text when it actually changes.
- pub `update_uniforms` function L287-291 — `(&self, queue: &wgpu::Queue, width: f32, height: f32)` — text when it actually changes.
- pub `render` function L293-301 — `( &'a self, render_pass: &mut wgpu::RenderPass<'a>, bind_group: &'a wgpu::BindGr...` — text when it actually changes.
- pub `EffectPipeline` struct L305-308 — `{ background: BackgroundPipeline, composite: CompositePipeline }` — text when it actually changes.
- pub `new_with_shared` function L311-320 — `( device: &wgpu::Device, background_shared: &Arc<SharedBackgroundPipeline>, comp...` — text when it actually changes.
- pub `new` function L322-327 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — text when it actually changes.
- pub `set_theme` function L329-332 — `(&mut self, theme: Theme)` — text when it actually changes.
- pub `theme` function L334-336 — `(&self) -> &Theme` — text when it actually changes.
- pub `theme_mut` function L338-342 — `(&mut self) -> &mut Theme` — text when it actually changes.
- pub `create_bind_group` function L344-350 — `( &self, device: &wgpu::Device, text_texture_view: &wgpu::TextureView, ) -> wgpu...` — text when it actually changes.
- pub `update_uniforms` function L352-355 — `(&self, queue: &wgpu::Queue, width: f32, height: f32)` — text when it actually changes.
- pub `render` function L358-364 — `( &'a self, _render_pass: &mut wgpu::RenderPass<'a>, _bind_group: &'a wgpu::Bind...` — text when it actually changes.
- pub `CrtUniforms` struct L374-386 — `{ screen_size: [f32; 2], time: f32, scanline_intensity: f32, scanline_frequency:...` — Uniform buffer for CRT post-processing shader
- pub `CrtPipeline` struct L389-395 — `{ shared: Arc<SharedCrtPipeline>, uniform_buffer: wgpu::Buffer, start_time: std:...` — CRT post-processing pipeline - applies scanlines, curvature, vignette
- pub `new_with_shared` function L398-426 — `(device: &wgpu::Device, shared: &Arc<SharedCrtPipeline>) -> Self` — text when it actually changes.
- pub `new` function L428-431 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — text when it actually changes.
- pub `set_effect` function L434-441 — `(&mut self, effect: Option<crt_theme::CrtEffect>)` — Set CRT effect from theme
- pub `is_enabled` function L444-446 — `(&self) -> bool` — Check if CRT effect is enabled
- pub `create_bind_group` function L448-471 — `( &self, device: &wgpu::Device, input_texture_view: &wgpu::TextureView, ) -> wgp...` — text when it actually changes.
- pub `update_uniforms` function L473-489 — `(&self, queue: &wgpu::Queue, width: f32, height: f32)` — text when it actually changes.
- pub `render` function L491-499 — `( &'a self, render_pass: &mut wgpu::RenderPass<'a>, bind_group: &'a wgpu::BindGr...` — text when it actually changes.
-  `BackgroundPipeline` type L79-132 — `= BackgroundPipeline` — text when it actually changes.
-  `BackgroundImagePipeline` type L152-220 — `= BackgroundImagePipeline` — text when it actually changes.
-  `CompositePipeline` type L230-302 — `= CompositePipeline` — text when it actually changes.
-  `EffectPipeline` type L310-365 — `= EffectPipeline` — text when it actually changes.
-  `CRT_REFERENCE_HEIGHT` variable L368 — `: f32` — Reference height for resolution-independent CRT effects (1080p baseline)
-  `CrtPipeline` type L397-500 — `= CrtPipeline` — text when it actually changes.
-  `BackgroundPipeline` type L504-508 — `impl Drop for BackgroundPipeline` — text when it actually changes.
-  `drop` function L505-507 — `(&mut self)` — text when it actually changes.
-  `CompositePipeline` type L510-514 — `impl Drop for CompositePipeline` — text when it actually changes.
-  `drop` function L511-513 — `(&mut self)` — text when it actually changes.
-  `BackgroundImagePipeline` type L516-520 — `impl Drop for BackgroundImagePipeline` — text when it actually changes.
-  `drop` function L517-519 — `(&mut self)` — text when it actually changes.
-  `CrtPipeline` type L522-526 — `impl Drop for CrtPipeline` — text when it actually changes.
-  `drop` function L523-525 — `(&mut self)` — text when it actually changes.
-  `tests` module L529-540 — `-` — text when it actually changes.
-  `test_shaders_compile` function L534-539 — `()` — text when it actually changes.

#### crates/crt-renderer/src/mock.rs

- pub `RenderCall` enum L10-45 — `Cells | Cursor | Selection | ClearText | Tabs | SearchMatches | ContextMenu | Be...` — Record of a render call for test inspection
- pub `MockRenderer` struct L53-58 — `{ calls: Vec<RenderCall>, needs_animation: bool }` — A mock renderer that records all render calls for testing
- pub `new` function L62-64 — `() -> Self` — Create a new mock renderer
- pub `clear_calls` function L67-69 — `(&mut self)` — Clear all recorded calls
- pub `call_count` function L72-74 — `(&self) -> usize` — Get the number of recorded calls
- pub `has_cell` function L79-89 — `(&self, character: char, row: i32, col: usize) -> bool` — Check if a specific character was rendered at a position
- pub `has_cursor_at` function L92-100 — `(&self, row: i32, col: usize) -> bool` — Check if cursor was rendered at a specific position
- pub `has_selection` function L103-107 — `(&self) -> bool` — Check if any selection was rendered
- pub `total_cells_rendered` function L110-121 — `(&self) -> usize` — Count total cells rendered across all calls
- pub `has_tabs` function L124-128 — `(&self) -> bool` — Check if tabs were rendered
- pub `last_tabs` function L131-139 — `(&self) -> Option<&[TabRenderInfo]>` — Get the last rendered tabs if any
- pub `has_context_menu` function L142-146 — `(&self) -> bool` — Check if context menu was rendered
- pub `has_bell_flash` function L149-153 — `(&self) -> bool` — Check if bell flash was rendered
- pub `last_bell_intensity` function L156-164 — `(&self) -> Option<f32>` — Get the last bell flash intensity if any
- pub `was_cleared` function L167-171 — `(&self) -> bool` — Check if text was cleared
- pub `has_effects_update` function L174-178 — `(&self) -> bool` — Check if effects update was called
- pub `last_effects_dt` function L181-189 — `(&self) -> Option<f32>` — Get the last effects update delta time if any
- pub `rendered_text` function L192-225 — `(&self) -> String` — Get all rendered characters as a string (for simple content assertions)
-  `MockRenderer` type L60-226 — `= MockRenderer` — all render calls for test assertions, without requiring GPU context.
-  `MockRenderer` type L228-244 — `impl TextRenderer for MockRenderer` — all render calls for test assertions, without requiring GPU context.
-  `render_cells` function L229-231 — `(&mut self, cells: &[CellContent])` — all render calls for test assertions, without requiring GPU context.
-  `render_cursor` function L233-235 — `(&mut self, cursor: &CursorInfo)` — all render calls for test assertions, without requiring GPU context.
-  `render_selection` function L237-239 — `(&mut self, ranges: &[SelectionRange])` — all render calls for test assertions, without requiring GPU context.
-  `clear` function L241-243 — `(&mut self)` — all render calls for test assertions, without requiring GPU context.
-  `MockRenderer` type L246-271 — `impl UiRenderer for MockRenderer` — all render calls for test assertions, without requiring GPU context.
-  `render_tabs` function L247-252 — `(&mut self, tabs: &[TabRenderInfo], bar_bounds: Rect)` — all render calls for test assertions, without requiring GPU context.
-  `render_search_matches` function L254-259 — `(&mut self, matches: &[SearchHighlight], highlight_color: Color)` — all render calls for test assertions, without requiring GPU context.
-  `render_context_menu` function L261-266 — `(&mut self, position: (f32, f32), items: &[ContextMenuItem])` — all render calls for test assertions, without requiring GPU context.
-  `render_bell_flash` function L268-270 — `(&mut self, intensity: f32)` — all render calls for test assertions, without requiring GPU context.
-  `MockRenderer` type L273-285 — `impl BackdropRenderer for MockRenderer` — all render calls for test assertions, without requiring GPU context.
-  `update` function L274-276 — `(&mut self, dt: f32)` — all render calls for test assertions, without requiring GPU context.
-  `render` function L278-280 — `(&mut self)` — all render calls for test assertions, without requiring GPU context.
-  `needs_animation` function L282-284 — `(&self) -> bool` — all render calls for test assertions, without requiring GPU context.
-  `tests` module L288-497 — `-` — all render calls for test assertions, without requiring GPU context.
-  `test_new_mock_is_empty` function L292-296 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_record_cells` function L299-313 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_total_cells` function L316-326 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_record_cursor` function L329-343 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_record_selection` function L346-358 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_no_selection` function L361-365 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_record_clear` function L368-372 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_record_tabs` function L375-399 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_record_context_menu` function L402-421 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_record_bell` function L424-431 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_record_effects` function L434-442 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_needs_animation` function L445-451 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_clear_calls` function L454-469 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_rendered_text` function L472-489 — `()` — all render calls for test assertions, without requiring GPU context.
-  `test_render_call_debug` function L492-496 — `()` — all render calls for test assertions, without requiring GPU context.

#### crates/crt-renderer/src/rect_renderer.rs

- pub `RectInstance` struct L15-22 — `{ pos: [f32; 2], size: [f32; 2], color: [f32; 4] }` — Per-instance data for a rectangle
- pub `RectRenderer` struct L40-50 — `{ shared: Arc<SharedRectPipeline>, globals_buffer: wgpu::Buffer, bind_group: wgp...` — Rect renderer using instanced quads
- pub `MAX_INSTANCES` variable L54 — `: usize` — Maximum number of rect instances per render call
- pub `INSTANCE_BUFFER_SIZE` variable L57-58 — `: u64` — Size of instance buffer in bytes (16K instances * 32 bytes = 512 KB)
- pub `create_instance_buffer` function L64-71 — `(device: &wgpu::Device) -> wgpu::Buffer` — Create an instance buffer for use with this renderer
- pub `new_with_shared` function L74-85 — `(device: &wgpu::Device, shared: &Arc<SharedRectPipeline>) -> Self` — Create a rect renderer using shared pipeline objects.
- pub `new` function L88-100 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — Create a rect renderer with its own pipeline objects.
- pub `clear` function L130-132 — `(&mut self)` — Clear pending instances
- pub `push_rect` function L135-143 — `(&mut self, x: f32, y: f32, width: f32, height: f32, color: [f32; 4])` — Add a rectangle
- pub `instance_count` function L146-148 — `(&self) -> usize` — Get the number of pending instances
- pub `update_screen_size` function L151-163 — `(&mut self, queue: &wgpu::Queue, width: f32, height: f32)` — Update screen size uniform (only writes if size changed)
- pub `render` function L169-188 — `( &'a self, queue: &wgpu::Queue, render_pass: &mut wgpu::RenderPass<'a>, instanc...` — Upload instances and render
-  `Globals` struct L27-30 — `{ screen_size: [f32; 2], _pad: [f32; 2] }` — Global uniforms for the rect shader
-  `RectRenderer` type L52-189 — `= RectRenderer` — All rectangles render in a single draw call.
-  `create_per_window_resources` function L102-127 — `( device: &wgpu::Device, bind_group_layout: &wgpu::BindGroupLayout, ) -> (wgpu::...` — All rectangles render in a single draw call.
-  `RectRenderer` type L191-197 — `impl Drop for RectRenderer` — All rectangles render in a single draw call.
-  `drop` function L192-196 — `(&mut self)` — All rectangles render in a single draw call.

#### crates/crt-renderer/src/shared_pipelines.rs

- pub `SharedGridPipeline` struct L16-20 — `{ pipeline: wgpu::RenderPipeline, bind_group_layout: wgpu::BindGroupLayout, samp...` — Shared pipeline objects for the grid (text) renderer.
- pub `SharedRectPipeline` struct L23-26 — `{ pipeline: wgpu::RenderPipeline, bind_group_layout: wgpu::BindGroupLayout }` — Shared pipeline objects for the rect (background) renderer.
- pub `SharedBackgroundPipeline` struct L29-32 — `{ pipeline: wgpu::RenderPipeline, bind_group_layout: wgpu::BindGroupLayout }` — Shared pipeline objects for the background gradient renderer.
- pub `SharedCompositePipeline` struct L35-39 — `{ pipeline: wgpu::RenderPipeline, bind_group_layout: wgpu::BindGroupLayout, samp...` — Shared pipeline objects for the text composite/glow renderer.
- pub `SharedCrtPipeline` struct L42-46 — `{ pipeline: wgpu::RenderPipeline, bind_group_layout: wgpu::BindGroupLayout, samp...` — Shared pipeline objects for the CRT post-processing renderer.
- pub `SharedBackgroundImagePipeline` struct L49-53 — `{ pipeline: wgpu::RenderPipeline, bind_group_layout: wgpu::BindGroupLayout, samp...` — Shared pipeline objects for the background image renderer.
- pub `SharedEffectsBlitPipeline` struct L56-60 — `{ pipeline: wgpu::RenderPipeline, bind_group_layout: wgpu::BindGroupLayout, samp...` — Shared pipeline objects for the effects blit renderer.
- pub `SharedPipelines` struct L67-75 — `{ grid: Arc<SharedGridPipeline>, rect: Arc<SharedRectPipeline>, background: Arc<...` — All shared render pipelines, created once and shared across windows.
- pub `new` function L79-89 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — Create all shared pipelines for the given surface format.
- pub `new` function L95-191 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — separate — only the compiled pipeline objects are shared.
- pub `new` function L197-266 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — separate — only the compiled pipeline objects are shared.
- pub `new` function L272-329 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — separate — only the compiled pipeline objects are shared.
- pub `new` function L335-417 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — separate — only the compiled pipeline objects are shared.
- pub `new` function L423-505 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — separate — only the compiled pipeline objects are shared.
- pub `new` function L511-595 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — separate — only the compiled pipeline objects are shared.
- pub `new` function L601-673 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — separate — only the compiled pipeline objects are shared.
-  `SharedPipelines` type L77-90 — `= SharedPipelines` — separate — only the compiled pipeline objects are shared.
-  `SharedGridPipeline` type L94-192 — `= SharedGridPipeline` — separate — only the compiled pipeline objects are shared.
-  `SharedRectPipeline` type L196-267 — `= SharedRectPipeline` — separate — only the compiled pipeline objects are shared.
-  `SharedBackgroundPipeline` type L271-330 — `= SharedBackgroundPipeline` — separate — only the compiled pipeline objects are shared.
-  `SharedCompositePipeline` type L334-418 — `= SharedCompositePipeline` — separate — only the compiled pipeline objects are shared.
-  `SharedCrtPipeline` type L422-506 — `= SharedCrtPipeline` — separate — only the compiled pipeline objects are shared.
-  `SharedBackgroundImagePipeline` type L510-596 — `= SharedBackgroundImagePipeline` — separate — only the compiled pipeline objects are shared.
-  `SharedEffectsBlitPipeline` type L600-674 — `= SharedEffectsBlitPipeline` — separate — only the compiled pipeline objects are shared.

#### crates/crt-renderer/src/sprite_renderer.rs

- pub `SpritePosition` enum L15-28 — `Fixed | Center | BottomRight | BottomLeft | TopRight | TopLeft` — Sprite position on screen
- pub `from_str` function L38-47 — `(s: &str) -> Self` — Parse position from string
- pub `resolve` function L50-73 — `( &self, screen_w: f32, screen_h: f32, sprite_w: f32, sprite_h: f32, margin: f32...` — Calculate actual position given screen and sprite dimensions
- pub `SpriteMotion` enum L78-87 — `Static | Bounce | Patrol | Wander` — Motion behavior for sprite
- pub `from_str` function L97-104 — `(s: &str) -> Self` — Parse motion from string
- pub `SpriteSheet` struct L108-122 — `{ data: Vec<u8>, width: u32, height: u32, frame_width: u32, frame_height: u32, c...` — Loaded sprite sheet data
- pub `load` function L126-174 — `( path: &Path, frame_width: u32, frame_height: u32, columns: u32, rows: u32, fra...` — Load sprite sheet from file
- pub `frame_uv` function L177-187 — `(&self, frame: u32) -> [f32; 4]` — Get UV coordinates for a frame (offset_x, offset_y, size_x, size_y)
- pub `SpriteTexture` struct L191-195 — `{ texture: wgpu::Texture, view: wgpu::TextureView, sampler: wgpu::Sampler }` — GPU texture for sprite sheet
- pub `new` function L199-236 — `(device: &wgpu::Device, queue: &wgpu::Queue, sheet: &SpriteSheet) -> Self` — Create GPU texture from sprite sheet
- pub `SpriteRenderer` struct L252-259 — `{ pipeline: wgpu::RenderPipeline, bind_group_layout: wgpu::BindGroupLayout, unif...` — Sprite renderer using raw wgpu
- pub `new` function L263-351 — `(device: &wgpu::Device, target_format: wgpu::TextureFormat) -> Self` — Create a new sprite renderer
- pub `load_sheet` function L354-382 — `(&mut self, device: &wgpu::Device, queue: &wgpu::Queue, sheet: SpriteSheet)` — Load a sprite sheet
- pub `is_loaded` function L385-387 — `(&self) -> bool` — Check if a sprite sheet is loaded
- pub `sheet` function L390-392 — `(&self) -> Option<&SpriteSheet>` — Get the sprite sheet (for frame info)
- pub `render` function L406-450 — `( &'a self, pass: &mut wgpu::RenderPass<'a>, queue: &wgpu::Queue, frame: u32, sc...` — Render the sprite
- pub `SpriteConfig` struct L455-480 — `{ path: PathBuf, frame_width: u32, frame_height: u32, columns: u32, rows: u32, f...` — Configuration for sprite animation
- pub `OriginalSpriteValues` struct L484-490 — `{ fps: f32, opacity: f32, scale: f32, motion_speed: f32, path: String }` — Stored original sprite values for restoration after patch expires
- pub `SpriteAnimationState` struct L493-525 — `{ renderer: SpriteRenderer, config: SpriteConfig, current_frame: u32, total_fram...` — Complete sprite animation state (similar to BackgroundImageState)
- pub `new` function L529-598 — `( device: &wgpu::Device, queue: &wgpu::Queue, config: SpriteConfig, target_forma...` — Create new sprite animation state from config
- pub `update` function L601-691 — `(&mut self, dt: f32, screen_width: f32, screen_height: f32)` — Update animation state
- pub `render` function L694-712 — `( &'a self, pass: &mut wgpu::RenderPass<'a>, queue: &wgpu::Queue, screen_width: ...` — Render the sprite
- pub `is_loaded` function L715-717 — `(&self) -> bool` — Check if loaded
- pub `has_patch` function L720-722 — `(&self) -> bool` — Check if a patch is currently applied
- pub `apply_patch` function L732-780 — `(&mut self, patch: &SpritePatch)` — Apply a sprite patch, overriding specific properties
- pub `apply_patch_with_device` function L783-822 — `( &mut self, patch: &crt_theme::SpritePatch, device: &wgpu::Device, queue: &wgpu...` — Apply a sprite patch with device/queue access for texture reloading
- pub `restore` function L825-866 — `(&mut self)` — Restore original sprite values after patch expires
- pub `restore_with_device` function L869-893 — `(&mut self, device: &wgpu::Device, queue: &wgpu::Queue)` — Restore original sprite values with device access for texture reloading
- pub `needs_device_for_patch` function L896-898 — `(patch: &crt_theme::SpritePatch) -> bool` — Check if a sprite path change is pending (needs device access)
- pub `original_values` function L901-903 — `(&self) -> &OriginalSpriteValues` — Get the original (un-patched) sprite values
- pub `current_position` function L906-908 — `(&self) -> (f32, f32)` — Get current position (for overlay tracking)
- pub `SpriteOverlayState` struct L915-939 — `{ renderer: SpriteRenderer, config: SpriteOverlay, current_frame: u32, total_fra...` — One-shot overlay sprite state for event effects
- pub `new` function L943-1037 — `( device: &wgpu::Device, queue: &wgpu::Queue, config: SpriteOverlay, base_path: ...` — Create a new sprite overlay from config
- pub `update` function L1047-1092 — `( &mut self, dt: f32, backdrop_pos: Option<(f32, f32)>, cursor_pos: (f32, f32), ...` — Update overlay animation state
- pub `render` function L1095-1117 — `( &'a self, pass: &mut wgpu::RenderPass<'a>, queue: &wgpu::Queue, screen_width: ...` — Render the overlay sprite
- pub `is_completed` function L1120-1122 — `(&self) -> bool` — Check if the overlay animation has completed
- pub `is_loaded` function L1125-1127 — `(&self) -> bool` — Check if the overlay is loaded and ready
- pub `progress` function L1130-1136 — `(&self) -> f32` — Get current animation progress (0.0 to 1.0)
- pub `position_type` function L1139-1141 — `(&self) -> SpriteOverlayPosition` — Get the position type
-  `SpritePosition` type L30-34 — `impl Default for SpritePosition` — to avoid memory growth issues.
-  `default` function L31-33 — `() -> Self` — to avoid memory growth issues.
-  `SpritePosition` type L36-74 — `= SpritePosition` — to avoid memory growth issues.
-  `SpriteMotion` type L89-93 — `impl Default for SpriteMotion` — to avoid memory growth issues.
-  `default` function L90-92 — `() -> Self` — to avoid memory growth issues.
-  `SpriteMotion` type L95-105 — `= SpriteMotion` — to avoid memory growth issues.
-  `SpriteSheet` type L124-188 — `= SpriteSheet` — to avoid memory growth issues.
-  `SpriteTexture` type L197-237 — `= SpriteTexture` — to avoid memory growth issues.
-  `SpriteUniforms` struct L242-249 — `{ transform: [f32; 4], frame_uv: [f32; 4], params: [f32; 4] }` — Uniforms for sprite rendering
-  `SpriteRenderer` type L261-451 — `= SpriteRenderer` — to avoid memory growth issues.
-  `SpriteAnimationState` type L527-909 — `= SpriteAnimationState` — to avoid memory growth issues.
-  `SpriteOverlayState` type L941-1142 — `= SpriteOverlayState` — to avoid memory growth issues.

#### crates/crt-renderer/src/terminal_vello.rs

- pub `CursorShape` enum L12-19 — `Block | Bar | Underline` — Cursor shape style
- pub `CursorState` struct L29-40 — `{ x: f32, y: f32, cell_width: f32, cell_height: f32, visible: bool }` — Cursor position and dimensions
- pub `TerminalVelloRenderer` struct L51-69 — `{ scene: Scene, target_texture: Option<wgpu::Texture>, target_view: Option<wgpu:...` — Terminal vello renderer with cached GPU resources
- pub `DEFAULT_BLINK_INTERVAL_MS` variable L73 — `: u64` — Default blink interval in milliseconds
- pub `new` function L77-93 — `(_device: &wgpu::Device) -> Self` — Create a new terminal vello renderer.
- pub `set_blink_enabled` function L96-101 — `(&mut self, enabled: bool)` — Enable or disable cursor blinking
- pub `set_blink_interval_ms` function L104-106 — `(&mut self, ms: u64)` — Set the blink interval in milliseconds
- pub `update_blink` function L110-120 — `(&mut self)` — Update blink state based on elapsed time
- pub `reset_blink` function L124-127 — `(&mut self)` — Reset blink state (cursor becomes visible, timer resets)
- pub `set_cursor_shape` function L130-132 — `(&mut self, shape: CursorShape)` — Set the cursor shape
- pub `set_cursor_color` function L135-137 — `(&mut self, color: [f32; 4])` — Set the cursor color
- pub `cursor_color` function L140-142 — `(&self) -> [f32; 4]` — Get the current cursor color
- pub `set_cursor_glow` function L145-147 — `(&mut self, glow: Option<([f32; 4], f32, f32)>)` — Set cursor glow effect (color, radius, intensity)
- pub `cursor_glow` function L150-152 — `(&self) -> Option<([f32; 4], f32, f32)>` — Get cursor glow effect (color, radius, intensity)
- pub `cursor_shape` function L155-157 — `(&self) -> CursorShape` — Get the current cursor shape
- pub `set_cursor` function L160-168 — `(&mut self, x: f32, y: f32, cell_width: f32, cell_height: f32, visible: bool)` — Update cursor position and visibility
- pub `set_selection_color` function L171-173 — `(&mut self, color: [f32; 4])` — Set the selection highlight color
- pub `prepare` function L202-210 — `(&mut self, device: &wgpu::Device, width: u32, height: u32)` — Prepare the scene with current cursor and selection state
- pub `prepare_backgrounds_only` function L214-218 — `(&mut self, device: &wgpu::Device, width: u32, height: u32)` — Prepare scene for backgrounds only (no cursor)
- pub `render_to_texture` function L296-319 — `( &mut self, renderer: &mut Renderer, device: &wgpu::Device, queue: &wgpu::Queue...` — Render the scene to the internal texture using the shared renderer
- pub `texture_view` function L322-324 — `(&self) -> Option<&wgpu::TextureView>` — Get the rendered texture view for compositing
- pub `cursor_visible` function L327-329 — `(&self) -> bool` — Check if cursor is currently visible (accounts for blink state)
- pub `has_cursor` function L332-334 — `(&self) -> bool` — Check if cursor has any state (for rendering decisions)
- pub `add_selection_cell` function L337-358 — `(&mut self, x: f32, y: f32, cell_width: f32, cell_height: f32)` — Add a selection rectangle for a single cell to the scene
- pub `add_selection_row` function L361-393 — `( &mut self, start_x: f32, y: f32, num_cells: usize, cell_width: f32, cell_heigh...` — Add a selection spanning multiple cells to the scene
- pub `add_background` function L396-419 — `( &mut self, x: f32, y: f32, cell_width: f32, cell_height: f32, color: [f32; 4],...` — Add a background rectangle for a cell
- pub `add_underline` function L422-447 — `( &mut self, x: f32, y: f32, cell_width: f32, cell_height: f32, color: [f32; 4],...` — Add an underline decoration for a cell
- pub `add_strikethrough` function L450-475 — `( &mut self, x: f32, y: f32, cell_width: f32, cell_height: f32, color: [f32; 4],...` — Add a strikethrough decoration for a cell
- pub `add_search_bar` function L479-539 — `( &mut self, screen_width: f32, content_offset_y: f32, scale_factor: f32, backgr...` — Add a search bar background to the scene
-  `CursorShape` type L21-25 — `impl Default for CursorShape` — The expensive vello::Renderer is shared across all renderers to save memory.
-  `default` function L22-24 — `() -> Self` — The expensive vello::Renderer is shared across all renderers to save memory.
-  `TerminalVelloRenderer` type L71-540 — `= TerminalVelloRenderer` — The expensive vello::Renderer is shared across all renderers to save memory.
-  `ensure_target` function L176-199 — `(&mut self, device: &wgpu::Device, width: u32, height: u32)` — Ensure render target is sized correctly
-  `build_cursor` function L221-293 — `(&mut self)` — Build cursor shape into scene
-  `color_from_f32` function L543-550 — `(r: f32, g: f32, b: f32, a: f32) -> peniko::Color` — Helper to create a peniko Color from f32 RGBA components (0.0-1.0)
-  `tests` module L553-573 — `-` — The expensive vello::Renderer is shared across all renderers to save memory.
-  `test_cursor_shapes` function L557-559 — `()` — The expensive vello::Renderer is shared across all renderers to save memory.
-  `test_cursor_state` function L562-572 — `()` — The expensive vello::Renderer is shared across all renderers to save memory.

#### crates/crt-renderer/src/traits.rs

- pub `Color` struct L9-14 — `{ r: f32, g: f32, b: f32, a: f32 }` — RGBA color as floats (0.0 - 1.0)
- pub `rgb` function L18-20 — `(r: f32, g: f32, b: f32) -> Self` — Create a new opaque color
- pub `rgba` function L23-25 — `(r: f32, g: f32, b: f32, a: f32) -> Self` — Create a new color with alpha
- pub `from_u8` function L28-35 — `(r: u8, g: u8, b: u8, a: u8) -> Self` — Create from 8-bit components
- pub `WHITE` variable L38 — `: Self` — White color
- pub `BLACK` variable L40 — `: Self` — Black color
- pub `TRANSPARENT` variable L42 — `: Self` — Transparent
- pub `Rect` struct L47-52 — `{ x: f32, y: f32, width: f32, height: f32 }` — Rectangle with position and size
- pub `new` function L56-63 — `(x: f32, y: f32, width: f32, height: f32) -> Self` — Create a new rectangle
- pub `contains` function L66-68 — `(&self, px: f32, py: f32) -> bool` — Check if a point is inside this rectangle
- pub `right` function L71-73 — `(&self) -> f32` — Get the right edge x coordinate
- pub `bottom` function L76-78 — `(&self) -> f32` — Get the bottom edge y coordinate
- pub `GridPosition` struct L83-86 — `{ row: i32, col: usize }` — Grid position (row, column)
- pub `new` function L89-91 — `(row: i32, col: usize) -> Self` — requiring actual GPU context.
- pub `CellContent` struct L96-113 — `{ character: char, position: GridPosition, fg_color: Color, bg_color: Option<Col...` — A single cell's content for rendering
- pub `simple` function L117-128 — `(character: char, row: i32, col: usize) -> Self` — Create a simple cell with just character and position
- pub `CursorShape` enum L133-138 — `Block | Underline | Beam` — Cursor shape styles
- pub `CursorInfo` struct L142-151 — `{ position: GridPosition, shape: CursorShape, color: Color, visible: bool }` — Cursor information for rendering
- pub `SelectionRange` struct L155-162 — `{ start: GridPosition, end: GridPosition, color: Color }` — Selection range for highlighting
- pub `TabRenderInfo` struct L166-175 — `{ title: String, active: bool, has_activity: bool, bounds: Rect }` — Tab rendering information
- pub `SearchHighlight` struct L179-184 — `{ bounds: Rect, is_current: bool }` — Search match highlight
- pub `ContextMenuItem` struct L188-195 — `{ label: String, enabled: bool, selected: bool }` — Context menu item
- pub `TextRenderer` interface L200-212 — `{ fn render_cells(), fn render_cursor(), fn render_selection(), fn clear() }` — Trait for rendering terminal text content
- pub `UiRenderer` interface L217-229 — `{ fn render_tabs(), fn render_search_matches(), fn render_context_menu(), fn ren...` — Trait for rendering UI elements
- pub `BackdropRenderer` interface L234-243 — `{ fn update(), fn render(), fn needs_animation() }` — Trait for rendering backdrop effects
-  `Color` type L16-43 — `= Color` — requiring actual GPU context.
-  `Rect` type L54-79 — `= Rect` — requiring actual GPU context.
-  `GridPosition` type L88-92 — `= GridPosition` — requiring actual GPU context.
-  `CellContent` type L115-129 — `= CellContent` — requiring actual GPU context.
-  `tests` module L246-401 — `-` — requiring actual GPU context.
-  `test_color_rgb` function L252-258 — `()` — requiring actual GPU context.
-  `test_color_rgba` function L261-264 — `()` — requiring actual GPU context.
-  `test_color_from_u8` function L267-272 — `()` — requiring actual GPU context.
-  `test_color_constants` function L275-279 — `()` — requiring actual GPU context.
-  `test_rect_new` function L284-290 — `()` — requiring actual GPU context.
-  `test_rect_contains` function L293-299 — `()` — requiring actual GPU context.
-  `test_rect_edges` function L302-306 — `()` — requiring actual GPU context.
-  `test_grid_position` function L311-315 — `()` — requiring actual GPU context.
-  `test_cell_content_simple` function L320-327 — `()` — requiring actual GPU context.
-  `test_cursor_shape_default` function L332-335 — `()` — requiring actual GPU context.
-  `test_cursor_info` function L340-349 — `()` — requiring actual GPU context.
-  `test_selection_range` function L354-362 — `()` — requiring actual GPU context.
-  `test_tab_render_info` function L367-376 — `()` — requiring actual GPU context.
-  `test_search_highlight` function L381-387 — `()` — requiring actual GPU context.
-  `test_context_menu_item` function L392-400 — `()` — requiring actual GPU context.

### crates/crt-renderer/src/effects

> *Semantic summary to be generated by AI agent.*

#### crates/crt-renderer/src/effects/grid.rs

- pub `GridEffect` struct L48-87 — `{ enabled: bool, color: Rgba, spacing: f64, line_width: f64, perspective: f64, h...` — Perspective grid effect
- pub `new` function L111-113 — `() -> Self` — Create a new grid effect with default settings
- pub `with_color` function L116-119 — `(mut self, r: u8, g: u8, b: u8, a: u8) -> Self` — Create a grid effect with custom color
- pub `with_enabled` function L122-125 — `(mut self, enabled: bool) -> Self` — Set enabled state
-  `Rgba` struct L30-35 — `{ r: u8, g: u8, b: u8, a: u8 }` — RGBA color stored as u8 components
-  `Rgba` type L37-45 — `= Rgba` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `new` function L38-40 — `(r: u8, g: u8, b: u8, a: u8) -> Self` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `with_alpha` function L42-44 — `(&self, alpha: u8) -> Color` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `GridEffect` type L89-107 — `impl Default for GridEffect` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `default` function L90-106 — `() -> Self` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `GridEffect` type L109-187 — `= GridEffect` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `perspective_y` function L129-133 — `(&self, t: f64, horizon_y: f64, bottom_y: f64) -> f64` — Calculate perspective-adjusted Y position
-  `distance_fade` function L136-141 — `(&self, t: f64) -> f64` — Calculate fade factor based on distance from horizon
-  `stroke_with_glow` function L145-186 — `( &self, scene: &mut Scene, shape: &S, base_width: f64, base_color: Color, )` — Draw a stroke with optional glow effect
-  `GridEffect` type L189-373 — `impl BackdropEffect for GridEffect` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `effect_type` function L190-192 — `(&self) -> &'static str` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `update` function L194-196 — `(&mut self, _dt: f32, time: f32)` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `render` function L198-314 — `(&self, scene: &mut Scene, bounds: Rect)` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `configure` function L316-368 — `(&mut self, config: &EffectConfig)` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `is_enabled` function L370-372 — `(&self) -> bool` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `parse_simple_color` function L377-427 — `(s: &str) -> Option<Rgba>` — Simple color parser for common formats
-  `tests` module L430-491 — `-` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `test_default_grid_is_disabled` function L434-437 — `()` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `test_configure_enables_grid` function L440-448 — `()` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `test_parse_hex_color` function L451-457 — `()` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `test_parse_rgba_color` function L460-466 — `()` — - `--grid-curved: true|false` (curved or straight vertical lines)
-  `test_perspective_y_calculation` function L469-490 — `()` — - `--grid-curved: true|false` (curved or straight vertical lines)

#### crates/crt-renderer/src/effects/matrix.rs

- pub `MatrixEffect` struct L31-44 — `{ enabled: bool, color: Color, columns: Vec<Column>, char_width: f64, char_heigh...` — Matrix falling code effect
- pub `new` function L47-61 — `() -> Self` — Uses bright head and fading trail for the classic matrix rain look.
-  `Column` struct L13-28 — `{ x: f64, head_y: f64, char_seeds: Vec<u32>, trail_length: f64, speed: f64, acti...` — A single column of falling characters
-  `MatrixEffect` type L46-344 — `= MatrixEffect` — Uses bright head and fading trail for the classic matrix rain look.
-  `hash` function L64-71 — `(mut h: u32) -> u32` — Murmur3-style hash for random number generation
-  `rand` function L74-76 — `(seed: u32) -> f64` — Get a random float [0, 1) from a seed
-  `draw_char` function L81-309 — `(center: Point, width: f64, height: f64, seed: u32, charset: &[char]) -> BezPath` — Draw a matrix-style character glyph
-  `generate_columns` function L312-343 — `(&mut self)` — Initialize columns
-  `MatrixEffect` type L346-350 — `impl Default for MatrixEffect` — Uses bright head and fading trail for the classic matrix rain look.
-  `default` function L347-349 — `() -> Self` — Uses bright head and fading trail for the classic matrix rain look.
-  `MatrixEffect` type L352-525 — `impl BackdropEffect for MatrixEffect` — Uses bright head and fading trail for the classic matrix rain look.
-  `effect_type` function L353-355 — `(&self) -> &'static str` — Uses bright head and fading trail for the classic matrix rain look.
-  `update` function L357-407 — `(&mut self, dt: f32, time: f32)` — Uses bright head and fading trail for the classic matrix rain look.
-  `render` function L409-479 — `(&self, scene: &mut Scene, bounds: Rect)` — Uses bright head and fading trail for the classic matrix rain look.
-  `configure` function L481-520 — `(&mut self, config: &EffectConfig)` — Uses bright head and fading trail for the classic matrix rain look.
-  `is_enabled` function L522-524 — `(&self) -> bool` — Uses bright head and fading trail for the classic matrix rain look.
-  `parse_color` function L528-575 — `(s: &str) -> Option<Color>` — Parse a CSS color string into a Color
-  `tests` module L578-938 — `-` — Uses bright head and fading trail for the classic matrix rain look.
-  `default_matrix_is_disabled` function L584-588 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `default_columns_empty_before_generation` function L591-595 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `hash_is_deterministic` function L600-604 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `rand_produces_zero_to_one` function L607-612 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `generate_columns_creates_correct_count` function L617-623 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `generated_columns_are_active` function L626-634 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `generated_columns_have_spread_x_positions` function L637-645 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `generated_columns_have_staggered_starts` function L648-656 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `generated_columns_have_char_seeds` function L659-666 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `generated_columns_have_varied_speeds` function L669-680 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `update_noop_when_disabled` function L685-693 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `update_moves_columns_downward` function L696-710 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `update_triggers_regeneration_when_needed` function L713-722 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `column_deactivates_when_off_screen` function L725-737 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `column_respawns_after_delay` function L740-751 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `configure_enables_effect` function L756-763 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `configure_density_changes_column_count` function L766-773 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `configure_density_clamps_to_range` function L776-789 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `configure_speed` function L792-798 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `configure_font_size` function L801-808 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `configure_charset` function L811-817 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `configure_color_hex` function L820-830 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `parse_color_hex6` function L835-842 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `parse_color_hex8` function L845-849 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `parse_color_rgb` function L852-858 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `parse_color_rgba` function L861-867 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `parse_color_invalid` function L870-874 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `draw_char_without_charset_produces_path` function L879-886 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `draw_char_with_charset_uses_char_shapes` function L889-894 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `draw_char_deterministic` function L897-902 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `render_noop_when_disabled` function L907-912 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `render_noop_when_no_columns` function L915-922 — `()` — Uses bright head and fading trail for the classic matrix rain look.
-  `render_with_active_columns` function L925-937 — `()` — Uses bright head and fading trail for the classic matrix rain look.

#### crates/crt-renderer/src/effects/mod.rs

- pub `grid` module L24 — `-` — Provides a trait-based effect system for rendering animated backgrounds.
- pub `matrix` module L25 — `-` — ```
- pub `motion` module L26 — `-` — ```
- pub `particles` module L27 — `-` — ```
- pub `rain` module L28 — `-` — ```
- pub `renderer` module L29 — `-` — ```
- pub `shape` module L30 — `-` — ```
- pub `sprite` module L31 — `-` — ```
- pub `starfield` module L32 — `-` — ```
- pub `EffectConfig` struct L50-53 — `{ properties: HashMap<String, String> }` — Configuration passed to effects from parsed CSS properties
- pub `new` function L57-59 — `() -> Self` — Create a new empty config
- pub `get` function L62-64 — `(&self, key: &str) -> Option<&str>` — Get a property value as a string
- pub `get_bool` function L67-73 — `(&self, key: &str) -> Option<bool>` — Get a property as a boolean (true/false)
- pub `get_f64` function L76-78 — `(&self, key: &str) -> Option<f64>` — Get a property as an f64
- pub `get_f32` function L81-83 — `(&self, key: &str) -> Option<f32>` — Get a property as an f32
- pub `get_u32` function L86-88 — `(&self, key: &str) -> Option<u32>` — Get a property as a u32
- pub `get_usize` function L91-93 — `(&self, key: &str) -> Option<usize>` — Get a property as a usize
- pub `insert` function L96-98 — `(&mut self, key: impl Into<String>, value: impl Into<String>)` — Insert a property
- pub `BackdropEffect` interface L105-164 — `{ fn effect_type(), fn update(), fn render(), fn configure(), fn is_enabled(), f...` — Trait for backdrop effects that render to a Vello scene
- pub `Position` struct L168-171 — `{ x: f64, y: f64 }` — Position with velocity for motion behaviors
- pub `new` function L174-176 — `(x: f64, y: f64) -> Self` — ```
- pub `to_vec2` function L178-180 — `(&self) -> Vec2` — ```
-  `EffectConfig` type L55-99 — `= EffectConfig` — ```
-  `prepare_gpu_resources` function L140-147 — `( &mut self, _device: &wgpu::Device, _queue: &wgpu::Queue, _renderer: &mut vello...` — Prepare GPU resources for effects that need persistent textures.
-  `needs_gpu_resources` function L153-155 — `(&self) -> bool` — Check if GPU resources need to be prepared or updated.
-  `cleanup_gpu_resources` function L161-163 — `(&mut self, _renderer: &mut vello::Renderer)` — Cleanup GPU resources when effect is disabled or removed.
-  `Position` type L173-181 — `= Position` — ```
-  `Position` type L183-187 — `= Position` — ```
-  `from` function L184-186 — `(v: Vec2) -> Self` — ```
-  `Vec2` type L189-193 — `= Vec2` — ```
-  `from` function L190-192 — `(p: Position) -> Self` — ```
-  `tests` module L196-300 — `-` — ```
-  `effect_config_new_is_empty` function L200-203 — `()` — ```
-  `effect_config_insert_and_get` function L206-211 — `()` — ```
-  `effect_config_get_bool` function L214-232 — `()` — ```
-  `effect_config_get_f64` function L235-242 — `()` — ```
-  `effect_config_get_f32` function L245-249 — `()` — ```
-  `effect_config_get_u32` function L252-258 — `()` — ```
-  `effect_config_get_usize` function L261-265 — `()` — ```
-  `effect_config_overwrite_key` function L268-273 — `()` — ```
-  `position_new_and_to_vec2` function L276-283 — `()` — ```
-  `position_from_vec2` function L286-291 — `()` — ```
-  `vec2_from_position` function L294-299 — `()` — ```

#### crates/crt-renderer/src/effects/motion.rs

- pub `MotionBehavior` enum L18-55 — `None | Bounce | Scroll | Float | Orbit` — Motion behavior for positional effects
- pub `bounce` function L65-69 — `(vx: f64, vy: f64) -> Self` — Create a bounce behavior with given velocity
- pub `bounce_angled` function L72-77 — `(speed: f64, angle_degrees: f64) -> Self` — Create a bounce behavior from speed and angle
- pub `scroll` function L80-84 — `(dx: f64, dy: f64) -> Self` — Create a scroll behavior with given direction
- pub `scroll_angled` function L87-92 — `(speed: f64, angle_degrees: f64) -> Self` — Create a scroll behavior from speed and angle
- pub `float` function L95-101 — `(seed: u32, amplitude: f64, speed: f64) -> Self` — Create a float behavior
- pub `orbit` function L104-111 — `(center_x: f64, center_y: f64, radius: f64, speed: f64) -> Self` — Create an orbit behavior
- pub `update` function L121-224 — `(&mut self, position: &mut Vec2, size: Vec2, bounds: Rect, dt: f32, time: f32)` — Update position based on motion behavior
- pub `is_animated` function L227-229 — `(&self) -> bool` — Check if this behavior requires animation updates
- pub `behavior_type` function L232-240 — `(&self) -> &'static str` — Get the behavior type name
- pub `from_str` function L243-251 — `(s: &str) -> Self` — Parse motion behavior from string
-  `MotionBehavior` type L57-61 — `impl Default for MotionBehavior` — - `Orbit` - Circular path around center
-  `default` function L58-60 — `() -> Self` — - `Orbit` - Circular path around center
-  `MotionBehavior` type L63-252 — `= MotionBehavior` — - `Orbit` - Circular path around center
-  `tests` module L255-415 — `-` — - `Orbit` - Circular path around center
-  `test_bounds` function L258-260 — `() -> Rect` — - `Orbit` - Circular path around center
-  `test_size` function L262-264 — `() -> Vec2` — - `Orbit` - Circular path around center
-  `test_bounce_reflects_at_right_edge` function L267-283 — `()` — - `Orbit` - Circular path around center
-  `test_bounce_reflects_at_left_edge` function L286-300 — `()` — - `Orbit` - Circular path around center
-  `test_bounce_reflects_at_bottom_edge` function L303-317 — `()` — - `Orbit` - Circular path around center
-  `test_scroll_wraps_horizontally` function L320-330 — `()` — - `Orbit` - Circular path around center
-  `test_float_stays_bounded` function L333-355 — `()` — - `Orbit` - Circular path around center
-  `test_orbit_maintains_radius` function L358-374 — `()` — - `Orbit` - Circular path around center
-  `test_none_does_not_move` function L377-386 — `()` — - `Orbit` - Circular path around center
-  `test_from_str` function L389-414 — `()` — - `Orbit` - Circular path around center

#### crates/crt-renderer/src/effects/particles.rs

- pub `ParticleShape` enum L28-40 — `Dot | Circle | Star | Heart | Sparkle` — Particle shape type
- pub `ParticleBehavior` enum L57-67 — `Float | Drift | Rise | Fall` — Particle behavior/movement pattern
- pub `ParticleEffect` struct L120-159 — `{ enabled: bool, color: Rgba, count: usize, shape: ParticleShape, behavior: Part...` — Particle effect with configurable shapes and behaviors
- pub `new` function L183-185 — `() -> Self` — Create a new particle effect with default settings
-  `ParticleShape` type L42-53 — `= ParticleShape` — - `--particles-glow-intensity: <number>` (0-1)
-  `from_str` function L43-52 — `(s: &str) -> Option<Self>` — - `--particles-glow-intensity: <number>` (0-1)
-  `ParticleBehavior` type L69-79 — `= ParticleBehavior` — - `--particles-glow-intensity: <number>` (0-1)
-  `from_str` function L70-78 — `(s: &str) -> Option<Self>` — - `--particles-glow-intensity: <number>` (0-1)
-  `Particle` struct L83-98 — `{ x: f64, y: f64, size: f64, brightness: f64, phase: f64, rotation: f64, speed: ...` — Individual particle data
-  `Rgba` struct L102-107 — `{ r: u8, g: u8, b: u8, a: u8 }` — RGBA color stored as u8 components
-  `Rgba` type L109-117 — `= Rgba` — - `--particles-glow-intensity: <number>` (0-1)
-  `new` function L110-112 — `(r: u8, g: u8, b: u8, a: u8) -> Self` — - `--particles-glow-intensity: <number>` (0-1)
-  `with_alpha` function L114-116 — `(&self, alpha: u8) -> Color` — - `--particles-glow-intensity: <number>` (0-1)
-  `ParticleEffect` type L161-179 — `impl Default for ParticleEffect` — - `--particles-glow-intensity: <number>` (0-1)
-  `default` function L162-178 — `() -> Self` — - `--particles-glow-intensity: <number>` (0-1)
-  `ParticleEffect` type L181-461 — `= ParticleEffect` — - `--particles-glow-intensity: <number>` (0-1)
-  `random` function L188-196 — `(&self, index: usize) -> f64` — Hash-based pseudo-random number generator
-  `generate_particles` function L199-224 — `(&mut self)` — Generate particles
-  `particle_position` function L227-282 — `(&self, particle: &Particle, width: f64, height: f64) -> (f64, f64, f64)` — Calculate particle position based on behavior
-  `draw_star` function L285-309 — `(center: Point, size: f64, rotation: f64) -> BezPath` — Draw a star shape
-  `draw_heart` function L312-346 — `(center: Point, size: f64, rotation: f64) -> BezPath` — Draw a heart shape
-  `draw_sparkle` function L349-368 — `(center: Point, size: f64, rotation: f64) -> BezPath` — Draw a sparkle (4-pointed star)
-  `draw_particle` function L371-460 — `( &self, scene: &mut Scene, x: f64, y: f64, size: f64, rotation: f64, color: Col...` — Draw a particle at the given position
-  `ParticleEffect` type L463-553 — `impl BackdropEffect for ParticleEffect` — - `--particles-glow-intensity: <number>` (0-1)
-  `effect_type` function L464-466 — `(&self) -> &'static str` — - `--particles-glow-intensity: <number>` (0-1)
-  `update` function L468-474 — `(&mut self, _dt: f32, time: f32)` — - `--particles-glow-intensity: <number>` (0-1)
-  `render` function L476-500 — `(&self, scene: &mut Scene, bounds: Rect)` — - `--particles-glow-intensity: <number>` (0-1)
-  `configure` function L502-548 — `(&mut self, config: &EffectConfig)` — - `--particles-glow-intensity: <number>` (0-1)
-  `is_enabled` function L550-552 — `(&self) -> bool` — - `--particles-glow-intensity: <number>` (0-1)
-  `parse_simple_color` function L556-603 — `(s: &str) -> Option<Rgba>` — Simple color parser for common formats
-  `tests` module L606-641 — `-` — - `--particles-glow-intensity: <number>` (0-1)
-  `test_default_particles_disabled` function L610-613 — `()` — - `--particles-glow-intensity: <number>` (0-1)
-  `test_shape_parsing` function L616-624 — `()` — - `--particles-glow-intensity: <number>` (0-1)
-  `test_behavior_parsing` function L627-640 — `()` — - `--particles-glow-intensity: <number>` (0-1)

#### crates/crt-renderer/src/effects/rain.rs

- pub `RainEffect` struct L59-98 — `{ enabled: bool, color: Rgba, density: usize, speed: f64, angle: f64, length: f6...` — Rain effect with configurable angle and density
- pub `new` function L122-124 — `() -> Self` — Create a new rain effect with default settings
-  `Raindrop` struct L26-37 — `{ x: f64, y: f64, speed: f64, length: f64, brightness: f64 }` — Individual raindrop data
-  `Rgba` struct L41-46 — `{ r: u8, g: u8, b: u8, a: u8 }` — RGBA color stored as u8 components
-  `Rgba` type L48-56 — `= Rgba` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `new` function L49-51 — `(r: u8, g: u8, b: u8, a: u8) -> Self` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `with_alpha` function L53-55 — `(&self, alpha: u8) -> Color` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `RainEffect` type L100-118 — `impl Default for RainEffect` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `default` function L101-117 — `() -> Self` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `RainEffect` type L120-232 — `= RainEffect` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `random` function L127-135 — `(&self, index: usize) -> f64` — Hash-based pseudo-random number generator
-  `generate_drops` function L138-159 — `(&mut self)` — Generate raindrops
-  `drop_position` function L162-193 — `(&self, drop: &Raindrop, width: f64, height: f64) -> (f64, f64)` — Calculate raindrop position with wrapping
-  `draw_drop` function L196-231 — `(&self, scene: &mut Scene, x: f64, y: f64, length: f64, color: Color)` — Draw a raindrop with optional glow
-  `RainEffect` type L234-321 — `impl BackdropEffect for RainEffect` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `effect_type` function L235-237 — `(&self) -> &'static str` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `update` function L239-245 — `(&mut self, _dt: f32, time: f32)` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `render` function L247-272 — `(&self, scene: &mut Scene, bounds: Rect)` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `configure` function L274-316 — `(&mut self, config: &EffectConfig)` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `is_enabled` function L318-320 — `(&self) -> bool` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `parse_simple_color` function L324-374 — `(s: &str) -> Option<Rgba>` — Simple color parser for common formats
-  `tests` module L377-407 — `-` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `test_default_rain_is_disabled` function L381-384 — `()` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `test_configure_enables_rain` function L387-395 — `()` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)
-  `test_angle_clamped` function L398-406 — `()` — - `--rain-glow-intensity: <number>` (0-1 glow brightness)

#### crates/crt-renderer/src/effects/renderer.rs

- pub `EffectsRenderer` struct L30-57 — `{ effects: Vec<Box<dyn BackdropEffect>>, vello_renderer: Arc<Mutex<Option<Render...` — Manages backdrop effects and renders them to a texture
- pub `new_with_shared` function L61-76 — `( vello_renderer: Arc<Mutex<Option<Renderer>>>, shared_blit: &Arc<SharedEffectsB...` — Create a new effects renderer with shared pipeline objects
- pub `new` function L79-86 — `( device: &wgpu::Device, vello_renderer: Arc<Mutex<Option<Renderer>>>, format: w...` — Create a new effects renderer with its own pipeline objects
- pub `add_effect` function L89-91 — `(&mut self, effect: Box<dyn BackdropEffect>)` — Add an effect to the renderer
- pub `clear_effects` function L94-96 — `(&mut self)` — Remove all effects
- pub `effects_mut` function L99-101 — `(&mut self) -> &mut Vec<Box<dyn BackdropEffect>>` — Get a mutable reference to effects for configuration
- pub `configure` function L107-126 — `(&mut self, config: &EffectConfig)` — Configure all effects from theme config
- pub `update` function L132-140 — `(&mut self, dt: f32)` — Update all effects' animation state
- pub `has_enabled_effects` function L143-145 — `(&self) -> bool` — Check if any effects are enabled
- pub `apply_effect_patch` function L151-163 — `(&mut self, effect_type: &str, config: &EffectConfig)` — Apply a temporary patch configuration to a specific effect type
- pub `composite` function L214-220 — `(&'a self, pass: &mut wgpu::RenderPass<'a>)` — Composite the effects texture onto the frame
- pub `render` function L226-286 — `( &mut self, device: &wgpu::Device, queue: &wgpu::Queue, size: (u32, u32), ) -> ...` — Render all enabled effects to texture
- pub `render_to_view` function L295-369 — `( &mut self, device: &wgpu::Device, queue: &wgpu::Queue, target_view: &wgpu::Tex...` — Render all enabled effects directly to a target texture view
- pub `texture_view` function L372-374 — `(&self) -> Option<&wgpu::TextureView>` — Get the current render target texture view
- pub `target_size` function L377-379 — `(&self) -> (u32, u32)` — Get current target size
- pub `elapsed_time` function L382-384 — `(&self) -> f32` — Get total elapsed time
- pub `reset_time` function L387-389 — `(&mut self)` — Reset elapsed time
-  `EffectsRenderer` type L59-390 — `= EffectsRenderer` — texture via the shared Vello renderer.
-  `ensure_target` function L166-208 — `(&mut self, device: &wgpu::Device, width: u32, height: u32)` — Ensure render target is sized correctly
-  `LOGGED` variable L240 — `: std::sync::atomic::AtomicBool` — texture via the shared Vello renderer.
-  `LOGGED2` variable L280 — `: std::sync::atomic::AtomicBool` — texture via the shared Vello renderer.
-  `EffectsRenderer` type L392-399 — `impl Drop for EffectsRenderer` — texture via the shared Vello renderer.
-  `drop` function L393-398 — `(&mut self)` — texture via the shared Vello renderer.

#### crates/crt-renderer/src/effects/shape.rs

- pub `ShapeType` enum L32-48 — `Rect | Circle | Ellipse | Triangle | Star | Heart | Polygon` — Shape type for the effect
- pub `RotationBehavior` enum L67-75 — `None | Spin | Wobble` — Rotation behavior
- pub `MotionType` enum L113-125 — `None | Bounce | Scroll | Float | Orbit` — Motion type for deterministic position calculation
- pub `ShapeEffect` struct L141-183 — `{ enabled: bool, shape_type: ShapeType, size: f64, fill: Option<Rgba>, stroke: O...` — Single geometric shape effect with motion and rotation
- pub `new` function L208-210 — `() -> Self` — Create a new shape effect with default settings
-  `ShapeType` type L50-63 — `= ShapeType` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `from_str` function L51-62 — `(s: &str) -> Option<Self>` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `RotationBehavior` type L77-86 — `= RotationBehavior` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `from_str` function L78-85 — `(s: &str) -> Option<Self>` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `Rgba` struct L90-95 — `{ r: u8, g: u8, b: u8, a: u8 }` — RGBA color stored as u8 components
-  `Rgba` type L97-109 — `= Rgba` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `new` function L98-100 — `(r: u8, g: u8, b: u8, a: u8) -> Self` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `to_color` function L102-104 — `(&self) -> Color` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `with_alpha` function L106-108 — `(&self, alpha: u8) -> Color` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `MotionType` type L127-138 — `= MotionType` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `from_str` function L128-137 — `(s: &str) -> Option<Self>` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `ShapeEffect` type L185-204 — `impl Default for ShapeEffect` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `default` function L186-203 — `() -> Self` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `ShapeEffect` type L206-523 — `= ShapeEffect` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `calculate_rotation` function L213-222 — `(&self) -> f64` — Calculate current rotation based on behavior and time
-  `calculate_position` function L225-281 — `(&self, bounds: Rect) -> Point` — Calculate position based on motion type and time (deterministic)
-  `draw_rect` function L284-299 — `(&self, scene: &mut Scene, center: Point, angle: f64)` — Draw a rectangle
-  `draw_circle` function L302-315 — `(&self, scene: &mut Scene, center: Point, _angle: f64)` — Draw a circle
-  `draw_ellipse` function L318-331 — `(&self, scene: &mut Scene, center: Point, angle: f64)` — Draw an ellipse
-  `draw_triangle` function L334-348 — `(&self, scene: &mut Scene, center: Point, angle: f64)` — Draw a triangle
-  `draw_star` function L351-365 — `(&self, scene: &mut Scene, center: Point, angle: f64)` — Draw a star
-  `draw_heart` function L368-382 — `(&self, scene: &mut Scene, center: Point, angle: f64)` — Draw a heart
-  `draw_polygon` function L385-399 — `(&self, scene: &mut Scene, center: Point, angle: f64)` — Draw a polygon
-  `render_shape_with_glow` function L402-444 — `( &self, scene: &mut Scene, transform: Affine, fill_fn: F, stroke_fn: S, )` — Render shape with glow, fill, and stroke
-  `build_polygon_path` function L447-464 — `(&self, sides: usize, rotation: f64) -> BezPath` — Build a regular polygon path
-  `build_star_path` function L467-490 — `(&self, points: usize, rotation: f64) -> BezPath` — Build a star path
-  `build_heart_path` function L493-522 — `(&self, rotation: f64) -> BezPath` — Build a heart path
-  `ShapeEffect` type L525-630 — `impl BackdropEffect for ShapeEffect` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `effect_type` function L526-528 — `(&self) -> &'static str` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `update` function L530-532 — `(&mut self, _dt: f32, time: f32)` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `render` function L534-555 — `(&self, scene: &mut Scene, bounds: Rect)` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `configure` function L557-625 — `(&mut self, config: &EffectConfig)` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `is_enabled` function L627-629 — `(&self) -> bool` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `parse_simple_color` function L633-680 — `(s: &str) -> Option<Rgba>` — Simple color parser for common formats
-  `tests` module L683-734 — `-` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `test_default_shape_disabled` function L687-690 — `()` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `test_shape_type_parsing` function L693-699 — `()` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `test_rotation_parsing` function L702-715 — `()` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)
-  `test_color_parsing` function L718-733 — `()` — - `--shape-polygon-sides: <number>` (sides for polygon, 3-12)

#### crates/crt-renderer/src/effects/sprite.rs

- pub `SpriteMotion` enum L32-44 — `None | Bounce | Scroll | Float | Orbit` — Motion type for sprite movement
- pub `SpritePosition` enum L61-72 — `Center | TopLeft | Top | TopRight | Left | Right | BottomLeft | Bottom | BottomR...` — Static position for sprite when motion is None
- pub `SpriteEffect` struct L194-244 — `{ enabled: bool, path: Option<String>, base_dir: Option<String>, frame_width: u3...` — Animated sprite effect with configurable sprite sheet
- pub `new` function L273-275 — `() -> Self` — Create a new sprite effect with default settings
-  `SpriteMotion` type L46-57 — `= SpriteMotion` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `from_str` function L47-56 — `(s: &str) -> Option<Self>` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `SpritePosition` type L74-89 — `= SpritePosition` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `from_str` function L75-88 — `(s: &str) -> Option<Self>` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `SpriteSheet` struct L94-109 — `{ image_data: ImageData, sheet_width: u32, sheet_height: u32, frame_width: u32, ...` — Sprite sheet configuration
-  `SpriteSheet` type L111-191 — `= SpriteSheet` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `load` function L113-181 — `( path: &Path, frame_width: u32, frame_height: u32, columns: u32, rows: u32, fra...` — Load sprite sheet from path with frame configuration
-  `frame_offset` function L184-190 — `(&self, frame: u32) -> (f64, f64)` — Get the UV offset for a specific frame index
-  `SpriteEffect` type L246-269 — `impl Default for SpriteEffect` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `default` function L247-268 — `() -> Self` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `SpriteEffect` type L271-392 — `= SpriteEffect` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `calculate_position` function L278-349 — `(&self, bounds: Rect, sprite_width: f64, sprite_height: f64) -> Point` — Calculate position based on motion type and time
-  `try_load_sprite_sheet` function L352-391 — `(&mut self)` — Try to load the sprite sheet
-  `SpriteEffect` type L394-548 — `impl BackdropEffect for SpriteEffect` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `effect_type` function L395-397 — `(&self) -> &'static str` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `update` function L399-410 — `(&mut self, _dt: f32, time: f32)` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `render` function L412-469 — `(&self, scene: &mut Scene, bounds: Rect)` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `configure` function L471-543 — `(&mut self, config: &EffectConfig)` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `is_enabled` function L545-547 — `(&self) -> bool` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `tests` module L551-596 — `-` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `test_default_sprite_disabled` function L555-558 — `()` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `test_motion_parsing` function L561-566 — `()` — - `--sprite-motion-speed: <number>` (motion speed multiplier)
-  `test_frame_offset` function L569-595 — `()` — - `--sprite-motion-speed: <number>` (motion speed multiplier)

#### crates/crt-renderer/src/effects/starfield.rs

- pub `StarDirection` enum L30-42 — `Static | Up | Down | Left | Right` — Direction stars drift
- pub `StarfieldEffect` struct L93-141 — `{ enabled: bool, color: Rgba, density: usize, layer_count: usize, speed: f64, di...` — Starfield effect with parallax layers
- pub `new` function L168-170 — `() -> Self` — Create a new starfield effect with default settings
-  `StarDirection` type L44-55 — `= StarDirection` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `from_str` function L45-54 — `(s: &str) -> Option<Self>` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `Star` struct L59-71 — `{ x: f64, y: f64, size: f64, brightness: f64, layer: usize, twinkle_phase: f64 }` — Individual star data
-  `Rgba` struct L75-80 — `{ r: u8, g: u8, b: u8, a: u8 }` — RGBA color stored as u8 components
-  `Rgba` type L82-90 — `= Rgba` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `new` function L83-85 — `(r: u8, g: u8, b: u8, a: u8) -> Self` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `with_alpha` function L87-89 — `(&self, alpha: u8) -> Color` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `StarfieldEffect` type L143-164 — `impl Default for StarfieldEffect` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `default` function L144-163 — `() -> Self` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `StarfieldEffect` type L166-295 — `= StarfieldEffect` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `random` function L173-183 — `(&self, index: usize) -> f64` — Hash-based pseudo-random number generator for better distribution
-  `generate_stars` function L186-213 — `(&mut self)` — Generate stars for all layers
-  `star_position` function L216-255 — `(&self, star: &Star, width: f64, height: f64) -> (f64, f64)` — Calculate animated star position
-  `draw_star` function L258-294 — `(&self, scene: &mut Scene, x: f64, y: f64, radius: f64, color: Color)` — Draw a star with optional glow
-  `StarfieldEffect` type L297-416 — `impl BackdropEffect for StarfieldEffect` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `effect_type` function L298-300 — `(&self) -> &'static str` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `update` function L302-309 — `(&mut self, _dt: f32, time: f32)` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `render` function L311-350 — `(&self, scene: &mut Scene, bounds: Rect)` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `configure` function L352-411 — `(&mut self, config: &EffectConfig)` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `is_enabled` function L413-415 — `(&self) -> bool` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `parse_simple_color` function L419-469 — `(s: &str) -> Option<Rgba>` — Simple color parser for common formats
-  `tests` module L472-514 — `-` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `test_default_starfield_is_disabled` function L476-479 — `()` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `test_configure_enables_starfield` function L482-490 — `()` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `test_star_generation` function L493-500 — `()` — - `--starfield-max-size: <number>` (maximum star size in pixels)
-  `test_direction_parsing` function L503-513 — `()` — - `--starfield-max-size: <number>` (maximum star size in pixels)

### crates/crt-renderer/src/shaders

> *Semantic summary to be generated by AI agent.*

#### crates/crt-renderer/src/shaders/mod.rs

- pub `builtin` module L8-29 — `-` — Built-in shaders included at compile time
- pub `BACKGROUND` variable L10 — `: &str` — Background shader - renders gradient + animated perspective grid
- pub `BACKGROUND_IMAGE` variable L13 — `: &str` — Background image shader - renders textured background with sizing/positioning
- pub `COMPOSITE` variable L16 — `: &str` — Composite shader - applies glow blur to text texture (25-sample Gaussian)
- pub `GRID` variable L19 — `: &str` — Grid shader - GPU-accelerated text glyph rendering using instanced quads
- pub `RECT` variable L22 — `: &str` — Rect shader - solid color rectangle rendering using instanced quads
- pub `CRT` variable L25 — `: &str` — CRT post-processing shader - scanlines, curvature, vignette
- pub `EFFECTS_BLIT` variable L28 — `: &str` — Effects blit shader - composites effects texture onto frame

### crates/crt-renderer/src/tab_bar

> *Semantic summary to be generated by AI agent.*

#### crates/crt-renderer/src/tab_bar/layout.rs

- pub `TabRect` struct L11-18 — `{ x: f32, y: f32, width: f32, height: f32, close_x: f32, close_width: f32 }` — Rectangle for a tab (for hit testing and rendering)
- pub `contains` function L21-23 — `(&self, x: f32, y: f32) -> bool` — Tab bar is always positioned at the top of the window.
- pub `close_contains` function L25-30 — `(&self, x: f32, y: f32) -> bool` — Tab bar is always positioned at the top of the window.
- pub `TabLayout` struct L35-43 — `{ tab_rects: Vec<TabRect>, bar_height: f32, content_padding: f32, screen_width: ...` — Tab bar layout - manages dimensions and positioning
- pub `new` function L52-62 — `() -> Self` — Tab bar is always positioned at the top of the window.
- pub `set_scale_factor` function L65-68 — `(&mut self, scale_factor: f32)` — Set scale factor for HiDPI displays
- pub `scale_factor` function L71-73 — `(&self) -> f32` — Get scale factor
- pub `height` function L76-78 — `(&self) -> f32` — Get current tab bar height (in logical pixels)
- pub `content_offset` function L82-84 — `(&self) -> (f32, f32)` — Get the content offset (x, y) in logical pixels
- pub `set_content_padding` function L87-90 — `(&mut self, padding: f32)` — Set content padding from theme
- pub `resize` function L93-97 — `(&mut self, width: f32, height: f32)` — Update screen size (in physical pixels)
- pub `screen_size` function L100-102 — `(&self) -> (f32, f32)` — Get screen dimensions
- pub `set_bar_height` function L105-108 — `(&mut self, height: f32)` — Set bar height from theme
- pub `mark_dirty` function L111-113 — `(&mut self)` — Mark layout as dirty (needs rebuild)
- pub `is_dirty` function L116-118 — `(&self) -> bool` — Check if layout needs rebuild
- pub `clear_dirty` function L121-123 — `(&mut self)` — Clear dirty flag
- pub `tab_rects` function L126-128 — `(&self) -> &[TabRect]` — Get tab rectangles
- pub `hit_test` function L131-139 — `(&self, x: f32, y: f32) -> Option<(usize, bool)>` — Hit test - returns (tab_index, is_close_button) if hit
- pub `calculate_rects` function L143-181 — `(&mut self, state: &TabBarState, theme: &TabTheme)` — Calculate tab rectangles based on current state and theme
-  `TabRect` type L20-31 — `= TabRect` — Tab bar is always positioned at the top of the window.
-  `TabLayout` type L45-49 — `impl Default for TabLayout` — Tab bar is always positioned at the top of the window.
-  `default` function L46-48 — `() -> Self` — Tab bar is always positioned at the top of the window.
-  `TabLayout` type L51-182 — `= TabLayout` — Tab bar is always positioned at the top of the window.
-  `tests` module L185-387 — `-` — Tab bar is always positioned at the top of the window.
-  `layout_with_rects` function L189-199 — `(tab_count: usize) -> TabLayout` — Tab bar is always positioned at the top of the window.
-  `new_layout_defaults` function L202-208 — `()` — Tab bar is always positioned at the top of the window.
-  `content_offset_respects_padding` function L211-215 — `()` — Tab bar is always positioned at the top of the window.
-  `resize_updates_screen_size` function L218-223 — `()` — Tab bar is always positioned at the top of the window.
-  `calculate_rects_produces_correct_count` function L226-230 — `()` — Tab bar is always positioned at the top of the window.
-  `calculate_rects_tabs_are_contiguous` function L233-245 — `()` — Tab bar is always positioned at the top of the window.
-  `calculate_rects_zero_tabs` function L248-263 — `()` — Tab bar is always positioned at the top of the window.
-  `tab_width_clamped_to_max` function L266-272 — `()` — Tab bar is always positioned at the top of the window.
-  `many_tabs_shrink_to_fit` function L275-284 — `()` — Tab bar is always positioned at the top of the window.
-  `hit_test_first_tab` function L287-293 — `()` — Tab bar is always positioned at the top of the window.
-  `hit_test_close_button` function L296-305 — `()` — Tab bar is always positioned at the top of the window.
-  `hit_test_outside_returns_none` function L308-314 — `()` — Tab bar is always positioned at the top of the window.
-  `hit_test_second_tab` function L317-323 — `()` — Tab bar is always positioned at the top of the window.
-  `scale_factor_affects_rects` function L326-343 — `()` — Tab bar is always positioned at the top of the window.
-  `tab_rect_contains` function L346-361 — `()` — Tab bar is always positioned at the top of the window.
-  `tab_rect_close_contains` function L364-376 — `()` — Tab bar is always positioned at the top of the window.
-  `dirty_flag_lifecycle` function L379-386 — `()` — Tab bar is always positioned at the top of the window.

#### crates/crt-renderer/src/tab_bar/mod.rs

- pub `DragMode` enum L23-30 — `Reorder | Merge | Detach` — Visual mode for the drag feedback indicator
- pub `DragFeedback` struct L36-45 — `{ dragged_tab_id: u64, insertion_index: Option<usize>, ghost_position: Option<(f...` — Visual feedback state for tab dragging.
- pub `TabBar` struct L53-60 — `{ state: TabBarState, layout: TabLayout, vello_renderer: VelloTabBarRenderer, th...` — Tab bar facade - combines state, layout, and rendering
- pub `new` function L63-71 — `(device: &wgpu::Device, format: wgpu::TextureFormat) -> Self` — triggered by CSS properties like `text-shadow`.
- pub `with_initial_id` function L74-82 — `(device: &wgpu::Device, format: wgpu::TextureFormat, id: u64) -> Self` — Create a tab bar with a specific initial tab ID (for global ID support)
- pub `set_drag_feedback` function L85-87 — `(&mut self, feedback: Option<DragFeedback>)` — Set drag visual feedback for this frame's rendering
- pub `set_theme` function L92-97 — `(&mut self, theme: TabTheme)` — Set the tab theme
- pub `set_scale_factor` function L102-104 — `(&mut self, scale_factor: f32)` — Set scale factor for HiDPI displays
- pub `height` function L107-109 — `(&self) -> f32` — Get current tab bar height (in logical pixels)
- pub `content_offset` function L113-115 — `(&self) -> (f32, f32)` — Get the content offset (x, y) in logical pixels
- pub `resize` function L118-120 — `(&mut self, width: f32, height: f32)` — Update screen size (in physical pixels)
- pub `add_tab` function L125-128 — `(&mut self, id: u64, title: impl Into<String>)` — Add a new tab with a caller-provided globally unique ID
- pub `move_tab` function L131-134 — `(&mut self, from: usize, to: usize)` — Move a tab from one index to another
- pub `tab_index` function L137-139 — `(&self, id: u64) -> Option<usize>` — Get the index of a tab by its ID
- pub `remove_tab` function L142-148 — `(&mut self, id: u64) -> Option<super::Tab>` — Remove a tab by ID and return it (for cross-window transfer)
- pub `add_existing_tab` function L151-154 — `(&mut self, tab: super::Tab)` — Insert a pre-existing tab at the end
- pub `insert_existing_tab` function L157-160 — `(&mut self, tab: super::Tab, index: usize)` — Insert a pre-existing tab at a specific index
- pub `close_tab` function L163-169 — `(&mut self, id: u64) -> bool` — Close a tab by ID
- pub `select_tab` function L172-178 — `(&mut self, id: u64) -> bool` — Select a tab by ID
- pub `select_tab_index` function L181-187 — `(&mut self, index: usize) -> bool` — Select tab by index (0-based)
- pub `next_tab` function L190-193 — `(&mut self)` — Select next tab
- pub `prev_tab` function L196-199 — `(&mut self)` — Select previous tab
- pub `active_tab_id` function L202-204 — `(&self) -> Option<u64>` — Get active tab ID
- pub `tab_rects` function L207-209 — `(&self) -> &[TabRect]` — Get tab rectangles for hit testing and drag computations
- pub `active_tab_rect` function L213-219 — `(&self) -> Option<(f32, f32, f32, f32)>` — Get active tab rectangle (for focus indicator rendering)
- pub `tab_count` function L222-224 — `(&self) -> usize` — Get number of tabs
- pub `hit_test` function L227-232 — `(&self, x: f32, y: f32) -> Option<(u64, bool)>` — Hit test - returns (tab_id, is_close_button) if hit
- pub `set_tab_title` function L235-241 — `(&mut self, id: u64, title: impl Into<String>) -> bool` — Update a tab's title by ID (from OSC escape sequences)
- pub `set_custom_tab_title` function L244-250 — `(&mut self, id: u64, title: impl Into<String>) -> bool` — Set a custom title for a tab (user-initiated)
- pub `clear_custom_title` function L253-255 — `(&mut self, id: u64)` — Clear custom title flag
- pub `has_custom_title` function L258-260 — `(&self, id: u64) -> bool` — Check if a tab has a custom title
- pub `get_tab_title` function L263-265 — `(&self, id: u64) -> Option<&str>` — Get a tab's title by ID
- pub `is_editing` function L270-272 — `(&self) -> bool` — Check if currently editing a tab
- pub `editing_tab_id` function L275-277 — `(&self) -> Option<u64>` — Get the tab ID being edited (if any)
- pub `start_editing` function L280-286 — `(&mut self, id: u64) -> bool` — Start editing a tab's title
- pub `cancel_editing` function L289-292 — `(&mut self)` — Cancel editing without saving
- pub `confirm_editing` function L295-299 — `(&mut self) -> bool` — Confirm editing and save the new title
- pub `edit_insert_char` function L302-305 — `(&mut self, c: char)` — Handle a character input during editing
- pub `edit_backspace` function L308-311 — `(&mut self)` — Handle backspace during editing
- pub `edit_delete` function L314-317 — `(&mut self)` — Handle delete during editing
- pub `edit_cursor_left` function L320-323 — `(&mut self)` — Move cursor left during editing
- pub `edit_cursor_right` function L326-329 — `(&mut self)` — Move cursor right during editing
- pub `edit_cursor_home` function L332-335 — `(&mut self)` — Move cursor to start during editing
- pub `edit_cursor_end` function L338-341 — `(&mut self)` — Move cursor to end during editing
- pub `inactive_tab_color` function L346-348 — `(&self) -> [f32; 4]` — Get the foreground color for inactive tabs
- pub `active_tab_color` function L351-353 — `(&self) -> [f32; 4]` — Get the foreground color for active tabs
- pub `inactive_tab_text_shadow` function L356-361 — `(&self) -> Option<(f32, [f32; 4])>` — Get text shadow for inactive tabs (if any)
- pub `active_tab_text_shadow` function L364-369 — `(&self) -> Option<(f32, [f32; 4])>` — Get text shadow for active tabs (if any)
- pub `get_tab_labels` function L374-407 — `(&self) -> Vec<(f32, f32, String, bool, bool)>` — Get tab titles for text rendering (returns position and title in physical pixels)
- pub `get_close_button_labels` function L410-424 — `(&self) -> Vec<(f32, f32)>` — Get close button positions for text rendering (returns x, y position for 'x' glyph)
- pub `prepare` function L427-436 — `(&mut self, device: &wgpu::Device, _queue: &wgpu::Queue)` — Prepare the tab bar for rendering (builds vello scene)
- pub `render_vello` function L439-447 — `( &mut self, renderer: &mut vello::Renderer, device: &wgpu::Device, queue: &wgpu...` — Render vello scene to internal texture using shared renderer
- pub `vello_texture_view` function L450-452 — `(&self) -> Option<&wgpu::TextureView>` — Get vello texture view for compositing
- pub `render_shapes_to_rects` function L464-595 — `(&self, rect_renderer: &mut crate::RectRenderer)` — Render tab bar shapes using RectRenderer (sharp corners, no Vello needed)
-  `layout` module L11 — `-` — GPU-accelerated tab bar with theme support, separated into:
-  `state` module L12 — `-` — triggered by CSS properties like `text-shadow`.
-  `vello_renderer` module L13 — `-` — triggered by CSS properties like `text-shadow`.
-  `TabBar` type L62-453 — `= TabBar` — triggered by CSS properties like `text-shadow`.
-  `color_to_array` function L455-457 — `(color: &crt_theme::Color) -> [f32; 4]` — triggered by CSS properties like `text-shadow`.
-  `TabBar` type L459-596 — `= TabBar` — triggered by CSS properties like `text-shadow`.

#### crates/crt-renderer/src/tab_bar/state.rs

- pub `Tab` struct L7-13 — `{ id: u64, title: String, is_active: bool, has_custom_title: bool }` — A single tab in the tab bar
- pub `new` function L16-23 — `(id: u64, title: impl Into<String>) -> Self` — Pure data structures for tab state - no GPU dependencies.
- pub `EditState` struct L28-35 — `{ tab_id: Option<u64>, text: String, cursor: usize }` — State for inline tab title editing
- pub `TabBarState` struct L41-45 — `{ tabs: Vec<Tab>, active_tab: usize, edit_state: EditState }` — Tab bar state - manages tabs without any GPU concerns
- pub `new` function L54-61 — `() -> Self` — Pure data structures for tab state - no GPU dependencies.
- pub `with_initial_id` function L64-71 — `(id: u64) -> Self` — Create a tab bar with a specific initial tab ID (for global ID support)
- pub `empty` function L74-80 — `() -> Self` — Create an empty tab bar (no initial tab)
- pub `add_tab` function L86-88 — `(&mut self, id: u64, title: impl Into<String>)` — Add a new tab with a caller-provided ID.
- pub `remove_tab` function L93-106 — `(&mut self, id: u64) -> Option<Tab>` — Remove a tab by ID and return it (for cross-window transfer).
- pub `add_existing_tab` function L109-111 — `(&mut self, tab: Tab)` — Insert a pre-existing tab (preserving its ID) at the end.
- pub `insert_existing_tab` function L114-121 — `(&mut self, tab: Tab, index: usize)` — Insert a pre-existing tab at a specific index.
- pub `move_tab` function L127-149 — `(&mut self, from: usize, to: usize)` — Move a tab from one index to another, updating active_tab to follow.
- pub `close_tab` function L152-165 — `(&mut self, id: u64) -> bool` — Close a tab by ID.
- pub `select_tab` function L168-174 — `(&mut self, id: u64) -> bool` — Select a tab by ID
- pub `select_tab_index` function L177-183 — `(&mut self, index: usize) -> bool` — Select tab by index (0-based)
- pub `next_tab` function L186-190 — `(&mut self)` — Select next tab (wraps around)
- pub `prev_tab` function L193-201 — `(&mut self)` — Select previous tab (wraps around)
- pub `active_tab_id` function L204-206 — `(&self) -> Option<u64>` — Get active tab ID
- pub `active_tab_index` function L209-211 — `(&self) -> usize` — Get active tab index
- pub `tab_count` function L214-216 — `(&self) -> usize` — Get number of tabs
- pub `tabs` function L219-221 — `(&self) -> &[Tab]` — Get tabs slice
- pub `set_tab_title` function L225-249 — `(&mut self, id: u64, title: impl Into<String>) -> bool` — Update a tab's title by ID (from OSC escape sequences)
- pub `set_custom_tab_title` function L252-269 — `(&mut self, id: u64, title: impl Into<String>) -> bool` — Set a custom title for a tab (user-initiated)
- pub `clear_custom_title` function L272-276 — `(&mut self, id: u64)` — Clear custom title flag (allows OSC to update title again)
- pub `has_custom_title` function L279-285 — `(&self, id: u64) -> bool` — Check if a tab has a custom title
- pub `get_tab_title` function L288-293 — `(&self, id: u64) -> Option<&str>` — Get a tab's title by ID
- pub `is_editing` function L298-300 — `(&self) -> bool` — Check if currently editing a tab
- pub `editing_tab_id` function L303-305 — `(&self) -> Option<u64>` — Get the tab ID being edited (if any)
- pub `start_editing` function L308-318 — `(&mut self, id: u64) -> bool` — Start editing a tab's title
- pub `cancel_editing` function L321-323 — `(&mut self)` — Cancel editing without saving
- pub `confirm_editing` function L326-333 — `(&mut self) -> bool` — Confirm editing and save the new title
- pub `edit_insert_char` function L349-355 — `(&mut self, c: char)` — Handle a character input during editing
- pub `edit_backspace` function L358-364 — `(&mut self)` — Handle backspace during editing
- pub `edit_delete` function L367-372 — `(&mut self)` — Handle delete during editing
- pub `edit_cursor_left` function L375-379 — `(&mut self)` — Move cursor left during editing
- pub `edit_cursor_right` function L382-386 — `(&mut self)` — Move cursor right during editing
- pub `edit_cursor_home` function L389-393 — `(&mut self)` — Move cursor to start during editing
- pub `edit_cursor_end` function L396-400 — `(&mut self)` — Move cursor to end during editing
- pub `edit_state` function L403-405 — `(&self) -> &EditState` — Get edit state for display purposes
-  `Tab` type L15-24 — `= Tab` — Pure data structures for tab state - no GPU dependencies.
-  `TabBarState` type L47-51 — `impl Default for TabBarState` — Pure data structures for tab state - no GPU dependencies.
-  `default` function L48-50 — `() -> Self` — Pure data structures for tab state - no GPU dependencies.
-  `TabBarState` type L53-406 — `= TabBarState` — Pure data structures for tab state - no GPU dependencies.
-  `char_to_byte_index` function L336-341 — `(text: &str, char_idx: usize) -> usize` — Convert character index to byte index
-  `char_count` function L344-346 — `(&self) -> usize` — Get the number of characters in the edit text
-  `tests` module L409-952 — `-` — Pure data structures for tab state - no GPU dependencies.
-  `test_new_state` function L413-417 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `test_add_and_close_tabs` function L420-435 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `test_navigation` function L438-456 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `add_tab_uses_caller_provided_ids` function L459-467 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `with_initial_id_sets_first_tab_id` function L470-474 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `empty_creates_no_tabs` function L477-481 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `select_tab_by_id` function L484-495 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `select_tab_by_index` function L498-509 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `close_active_tab_selects_previous` function L512-528 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `close_nonexistent_tab_returns_false` function L531-536 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `set_tab_title_from_osc` function L539-543 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `set_tab_title_truncates_long_titles` function L546-553 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `set_tab_title_strips_control_chars` function L556-560 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `set_tab_title_rejects_empty` function L563-567 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `custom_title_blocks_osc_updates` function L570-584 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `get_tab_title_nonexistent_returns_none` function L587-590 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `edit_start_and_cancel` function L595-607 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `edit_start_nonexistent_tab_fails` function L610-614 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `edit_insert_and_confirm` function L617-638 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `edit_cursor_movement` function L641-667 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `edit_backspace_and_delete` function L670-694 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `edit_insert_respects_max_length` function L697-711 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `edit_operations_noop_when_not_editing` function L714-726 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `confirm_empty_edit_does_not_save` function L729-740 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `prev_tab_single_tab_stays` function L743-750 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `tabs_slice_reflects_state` function L753-762 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `make_state_with_tabs` function L766-776 — `(names: &[&str]) -> TabBarState` — Pure data structures for tab state - no GPU dependencies.
-  `tab_titles` function L778-780 — `(state: &TabBarState) -> Vec<String>` — Pure data structures for tab state - no GPU dependencies.
-  `move_tab_forward` function L783-788 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `move_tab_backward` function L791-796 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `move_tab_same_position_is_noop` function L799-803 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `move_tab_to_start` function L806-810 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `move_tab_to_end` function L813-817 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `move_tab_active_follows_moved_tab` function L820-828 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `move_tab_active_shifts_when_between_from_and_to` function L831-839 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `move_tab_active_shifts_backward_move` function L842-850 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `move_tab_out_of_bounds_is_noop` function L853-859 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `move_tab_adjacent_swap` function L862-866 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `remove_tab_returns_tab` function L871-876 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `remove_tab_nonexistent_returns_none` function L879-883 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `remove_tab_adjusts_active_index` function L886-892 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `remove_tab_active_clamps_when_last` function L895-900 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `add_existing_tab_appends` function L903-909 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `insert_existing_tab_at_beginning` function L912-917 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `insert_existing_tab_at_middle` function L920-925 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `insert_existing_tab_adjusts_active` function L928-936 — `()` — Pure data structures for tab state - no GPU dependencies.
-  `round_trip_remove_and_insert` function L939-951 — `()` — Pure data structures for tab state - no GPU dependencies.

#### crates/crt-renderer/src/tab_bar/vello_renderer.rs

- pub `VelloTabBarRenderer` struct L22-28 — `{ scene: Scene, target_texture: Option<wgpu::Texture>, target_view: Option<wgpu:...` — Vello-based tab bar renderer
- pub `new` function L33-40 — `(_device: &wgpu::Device, _format: wgpu::TextureFormat) -> Self` — Create a new tab bar vello renderer.
- pub `prepare` function L71-88 — `( &mut self, device: &wgpu::Device, state: &TabBarState, layout: &TabLayout, the...` — Build the tab bar scene from current state
- pub `render_to_texture` function L183-206 — `( &mut self, renderer: &mut Renderer, device: &wgpu::Device, queue: &wgpu::Queue...` — Render the scene to the internal texture using the shared renderer
- pub `texture_view` function L209-211 — `(&self) -> Option<&wgpu::TextureView>` — Get the rendered texture view for compositing
- pub `target_size` function L214-216 — `(&self) -> (u32, u32)` — Get target size
-  `VelloTabBarRenderer` type L30-217 — `= VelloTabBarRenderer` — The expensive vello::Renderer is shared across all renderers to save memory.
-  `ensure_target` function L43-66 — `(&mut self, device: &wgpu::Device, width: u32, height: u32)` — Ensure render target is sized correctly
-  `build_scene` function L91-180 — `(&mut self, state: &TabBarState, layout: &TabLayout, theme: &TabTheme)` — Build vello shapes for the tab bar
-  `VelloTabBarRenderer` type L219-226 — `impl Drop for VelloTabBarRenderer` — The expensive vello::Renderer is shared across all renderers to save memory.
-  `drop` function L220-225 — `(&mut self)` — The expensive vello::Renderer is shared across all renderers to save memory.
-  `color_to_brush` function L229-236 — `(color: &Color) -> peniko::Brush` — Convert theme color to vello brush

### crates/crt-theme/benches

> *Semantic summary to be generated by AI agent.*

#### crates/crt-theme/benches/theme_parsing.rs

-  `MINIMAL_THEME` variable L8-14 — `: &str` — Run with: cargo bench -p crt-theme
-  `MEDIUM_THEME` variable L16-49 — `: &str` — Run with: cargo bench -p crt-theme
-  `FULL_THEME` variable L51-108 — `: &str` — Run with: cargo bench -p crt-theme
-  `bench_parse_theme` function L110-138 — `(c: &mut Criterion)` — Run with: cargo bench -p crt-theme

### crates/crt-theme/src

> *Semantic summary to be generated by AI agent.*

#### crates/crt-theme/src/lib.rs

- pub `parser` module L8 — `-` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `Mergeable` interface L14-17 — `{ fn merge() }` — Trait for patch structs that can be merged (CSS cascade: later values win)
- pub `merge_optional_patch` function L20-24 — `(target: &mut Option<T>, source: Option<T>)` — Helper function to merge optional patch structs
- pub `ToEffectConfig` interface L42-45 — `{ fn to_config_pairs() }` — Trait for converting patch/theme structs to effect config key-value pairs
- pub `Color` struct L49-54 — `{ r: f32, g: f32, b: f32, a: f32 }` — RGBA color with f32 components (0.0 - 1.0)
- pub `rgb` function L57-59 — `(r: f32, g: f32, b: f32) -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `rgba` function L61-63 — `(r: f32, g: f32, b: f32, a: f32) -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `from_hex` function L66-73 — `(hex: u32) -> Self` — Create from hex color (e.g., 0xff5555)
- pub `from_hex_alpha` function L76-83 — `(hex: u32) -> Self` — Create from hex with alpha (e.g., 0xff555580)
- pub `to_array` function L85-87 — `(self) -> [f32; 4]` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `to_rgba_string` function L90-98 — `(&self) -> String` — Convert to CSS rgba() string format for effect configs
- pub `Typography` struct L109-117 — `{ font_family: Vec<String>, font_size: f32, line_height: f32, font_bold: Option<...` — Typography settings
- pub `AnsiPalette` struct L136-156 — `{ black: Color, red: Color, green: Color, yellow: Color, blue: Color, magenta: C...` — 256-color ANSI palette
- pub `get` function L160-180 — `(&self, index: u8) -> Color` — Get base color by ANSI index (0-15)
- pub `get_extended` function L183-185 — `(&self, index: u8) -> Option<Color>` — Get extended color by index (16-255), returns None if not overridden
- pub `set_extended` function L188-192 — `(&mut self, index: u8, color: Color)` — Set an extended color (16-255)
- pub `has_extended` function L195-197 — `(&self, index: u8) -> bool` — Check if an extended color is overridden
- pub `calculate_extended` function L201-226 — `(index: u8) -> Color` — Calculate the default color for extended palette indices (16-255)
- pub `TextShadow` struct L256-260 — `{ color: Color, radius: f32, intensity: f32 }` — Text shadow / glow effect
- pub `LinearGradient` struct L274-277 — `{ top: Color, bottom: Color }` — Linear gradient (two-stop for now)
- pub `BackgroundSize` enum L290-305 — `Cover | Contain | Auto | Fixed | CanvasPercent | ImageScale` — Background image sizing mode (CSS background-size)
- pub `BackgroundPosition` enum L309-331 — `Center | TopLeft | Top | TopRight | Left | Right | BottomLeft | Bottom | BottomR...` — Background image positioning (CSS background-position)
- pub `BackgroundRepeat` enum L335-345 — `NoRepeat | Repeat | RepeatX | RepeatY` — Background image repeat mode (CSS background-repeat)
- pub `CursorShape` enum L349-357 — `Block | Bar | Underline` — Cursor shape for theme overrides
- pub `BackgroundImage` struct L361-374 — `{ path: Option<String>, base_dir: Option<std::path::PathBuf>, size: BackgroundSi...` — Background image configuration
- pub `new` function L377-386 — `(path: impl Into<String>) -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `has_image` function L388-390 — `(&self) -> bool` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `resolved_path` function L393-410 — `(&self) -> Option<std::path::PathBuf>` — Get the resolved path, handling relative paths against base_dir
- pub `GridEffect` struct L415-427 — `{ enabled: bool, color: Color, spacing: f32, line_width: f32, perspective: f32, ...` — Backdrop grid effect (synthwave floor)
- pub `StarDirection` enum L449-461 — `Static | Up | Down | Left | Right` — Direction stars drift
- pub `from_str` function L464-473 — `(s: &str) -> Option<Self>` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `as_str` function L475-483 — `(&self) -> &'static str` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `StarfieldEffect` struct L488-501 — `{ enabled: bool, color: Color, density: u32, layers: u32, speed: f32, direction:...` — Backdrop starfield effect (parallax star layers)
- pub `RainEffect` struct L524-534 — `{ enabled: bool, color: Color, density: u32, speed: f32, angle: f32, length: f32...` — Backdrop rain effect (falling raindrops)
- pub `ParticleShape` enum L554-561 — `Dot | Circle | Star | Heart | Sparkle` — Particle shape type
- pub `from_str` function L564-573 — `(s: &str) -> Option<Self>` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `as_str` function L575-583 — `(&self) -> &'static str` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `ParticleBehavior` enum L588-594 — `Float | Drift | Rise | Fall` — Particle behavior/movement pattern
- pub `from_str` function L597-605 — `(s: &str) -> Option<Self>` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `as_str` function L607-614 — `(&self) -> &'static str` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `ParticleEffect` struct L619-629 — `{ enabled: bool, color: Color, count: u32, shape: ParticleShape, behavior: Parti...` — Backdrop particle effect (floating shapes)
- pub `MatrixEffect` struct L649-656 — `{ enabled: bool, color: Color, density: f32, speed: f32, font_size: f32, charset...` — Backdrop matrix effect (falling code columns)
- pub `ShapeType` enum L673-682 — `Circle | Rect | Ellipse | Triangle | Star | Heart | Polygon` — Shape type for geometric effect
- pub `from_str` function L685-696 — `(s: &str) -> Option<Self>` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `as_str` function L698-708 — `(&self) -> &'static str` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `ShapeRotation` enum L713-718 — `None | Spin | Wobble` — Rotation behavior for shape effect
- pub `from_str` function L721-728 — `(s: &str) -> Option<Self>` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `as_str` function L730-736 — `(&self) -> &'static str` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `ShapeMotion` enum L741-748 — `None | Bounce | Scroll | Float | Orbit` — Motion behavior for shape effect
- pub `from_str` function L751-760 — `(s: &str) -> Option<Self>` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `as_str` function L762-770 — `(&self) -> &'static str` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `ShapeEffect` struct L775-789 — `{ enabled: bool, shape_type: ShapeType, size: f32, fill: Option<Color>, stroke: ...` — Backdrop shape effect (single geometric shape with motion)
- pub `SpriteMotion` enum L813-820 — `None | Bounce | Scroll | Float | Orbit` — Motion behavior for sprite effect
- pub `from_str` function L823-832 — `(s: &str) -> Option<Self>` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `as_str` function L834-842 — `(&self) -> &'static str` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `SpritePosition` enum L847-858 — `Center | TopLeft | Top | TopRight | Left | Right | BottomLeft | Bottom | BottomR...` — Static position for sprite effect
- pub `from_str` function L861-874 — `(s: &str) -> Option<Self>` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `as_str` function L876-888 — `(&self) -> &'static str` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `CrtEffect` struct L893-909 — `{ enabled: bool, scanline_intensity: f32, scanline_frequency: f32, curvature: f3...` — CRT post-processing effect (scanlines, curvature, vignette)
- pub `SpriteEffect` struct L928-956 — `{ enabled: bool, path: Option<String>, base_dir: Option<std::path::PathBuf>, fra...` — Backdrop sprite effect (animated sprite sheet)
- pub `SelectionStyle` struct L981-984 — `{ background: Color, foreground: Color }` — Selection appearance
- pub `HighlightStyle` struct L997-1004 — `{ background: Color, foreground: Color, current_background: Color }` — Highlight appearance (search matches)
- pub `TabBarStyle` struct L1018-1029 — `{ background: Color, border_color: Color, height: f32, padding: f32, content_pad...` — Tab bar styling
- pub `TabStyle` struct L1045-1062 — `{ background: Color, foreground: Color, border_radius: f32, padding_x: f32, padd...` — Individual tab styling
- pub `TabActiveStyle` struct L1081-1090 — `{ background: Color, foreground: Color, accent: Color, text_shadow: Option<TextS...` — Active tab styling (inherits from TabStyle where not specified)
- pub `TabCloseStyle` struct L1105-1116 — `{ background: Color, foreground: Color, hover_background: Color, hover_foregroun...` — Tab close button styling
- pub `TabTheme` struct L1132-1137 — `{ bar: TabBarStyle, tab: TabStyle, active: TabActiveStyle, close: TabCloseStyle ...` — Complete tab theme
- pub `FocusStyle` struct L1141-1150 — `{ ring_color: Color, glow_color: Color, ring_thickness: f32, glow_size: f32 }` — Focus indicator styling (accessibility)
- pub `HoverStyle` struct L1165-1168 — `{ background: Color }` — Hover state styling
- pub `ContextMenuStyle` struct L1180-1189 — `{ background: Color, border_color: Color, text_color: Color, shortcut_color: Col...` — Context menu styling
- pub `SearchBarStyle` struct L1204-1213 — `{ background: Color, placeholder_color: Color, text_color: Color, no_match_color...` — Search bar styling
- pub `RenameBarStyle` struct L1228-1235 — `{ background: Color, label_color: Color, text_color: Color }` — Window rename bar styling
- pub `UiStyle` struct L1249-1255 — `{ focus: FocusStyle, hover: HoverStyle, context_menu: ContextMenuStyle, search_b...` — Complete UI styling (overlays, menus, focus indicators)
- pub `TerminalEvent` enum L1263-1274 — `Bell | CommandSuccess | CommandFail | FocusGained | FocusLost` — Terminal events that can trigger theme overrides
- pub `SpriteOverlayPosition` enum L1278-1288 — `Center | Cursor | Sprite | Random` — Position for overlay sprites during events
- pub `from_str` function L1291-1299 — `(s: &str) -> Option<Self>` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `as_str` function L1301-1308 — `(&self) -> &'static str` — Provides theme structures that map to shader uniforms for terminal rendering.
- pub `SpritePatch` struct L1314-1329 — `{ path: Option<String>, columns: Option<u32>, rows: Option<u32>, fps: Option<f32...` — Patches to existing backdrop sprite - keeps position and motion
- pub `StarfieldPatch` struct L1333-1356 — `{ color: Option<Color>, density: Option<u32>, layers: Option<u32>, speed: Option...` — Patches to starfield effect - all fields Optional; None means "keep current value"
- pub `ParticlePatch` struct L1360-1377 — `{ color: Option<Color>, count: Option<u32>, shape: Option<ParticleShape>, behavi...` — Patches to particle effect - all fields Optional; None means "keep current value"
- pub `GridPatch` struct L1381-1402 — `{ color: Option<Color>, spacing: Option<f32>, line_width: Option<f32>, perspecti...` — Patches to grid effect - all fields Optional; None means "keep current value"
- pub `RainPatch` struct L1406-1423 — `{ color: Option<Color>, density: Option<u32>, speed: Option<f32>, angle: Option<...` — Patches to rain effect - all fields Optional; None means "keep current value"
- pub `MatrixPatch` struct L1427-1438 — `{ color: Option<Color>, density: Option<f32>, speed: Option<f32>, font_size: Opt...` — Patches to matrix effect - all fields Optional; None means "keep current value"
- pub `ShapePatch` struct L1442-1467 — `{ shape_type: Option<ShapeType>, size: Option<f32>, fill: Option<Color>, stroke:...` — Patches to shape effect - all fields Optional; None means "keep current value"
- pub `SpriteOverlay` struct L1870-1885 — `{ path: String, position: SpriteOverlayPosition, columns: u32, rows: u32, fps: f...` — One-shot overlay sprite at specified position
- pub `EventOverride` struct L1904-1941 — `{ duration_ms: u32, sprite_patch: Option<SpritePatch>, sprite_overlay: Option<Sp...` — Event override - temporary theme modifications triggered by terminal events
- pub `merge` function L1945-1987 — `(&mut self, other: EventOverride)` — Merge another EventOverride into this one (CSS cascade: later values win)
- pub `Theme` struct L1992-2031 — `{ typography: Typography, foreground: Color, background: LinearGradient, backgro...` — Complete terminal theme
- pub `synthwave` function L2041-2081 — `() -> Self` — Synthwave theme - the default extra AF experience
- pub `minimal` function L2084-2115 — `() -> Self` — Minimal theme - no effects, just colors
- pub `from_css` function L2118-2120 — `(css: &str) -> Result<Self, parser::ThemeParseError>` — Load theme from CSS string
- pub `from_css_with_base` function L2123-2138 — `( css: &str, base_dir: impl AsRef<Path>, ) -> Result<Self, parser::ThemeParseErr...` — Load theme from CSS string with base directory for resolving relative paths
- pub `from_css_file` function L2141-2146 — `(path: impl AsRef<Path>) -> Result<Self, Box<dyn std::error::Error>>` — Load theme from CSS file
- pub `to_uniforms` function L2149-2178 — `(&self, screen_width: f32, screen_height: f32, time: f32) -> ThemeUniforms` — Convert theme to GPU-ready uniforms
- pub `ThemeUniforms` struct L2185-2203 — `{ screen_size: [f32; 2], time: f32, grid_intensity: f32, gradient_top: [f32; 4],...` — GPU-ready uniform buffer matching WGSL Params struct
-  `impl_mergeable` macro L27-39 — `-` — Macro to implement Mergeable for structs with all-Option fields
-  `Color` type L56-99 — `= Color` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `Color` type L101-105 — `impl Default for Color` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L102-104 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `Typography` type L119-131 — `impl Default for Typography` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L120-130 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `AnsiPalette` type L158-227 — `= AnsiPalette` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `AnsiPalette` type L229-252 — `impl Default for AnsiPalette` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L230-251 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `TextShadow` type L262-270 — `impl Default for TextShadow` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L263-269 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `LinearGradient` type L279-286 — `impl Default for LinearGradient` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L280-285 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `BackgroundImage` type L376-411 — `= BackgroundImage` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `GridEffect` type L429-445 — `impl Default for GridEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L430-444 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `StarDirection` type L463-484 — `= StarDirection` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `StarfieldEffect` type L503-520 — `impl Default for StarfieldEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L504-519 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `RainEffect` type L536-550 — `impl Default for RainEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L537-549 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ParticleShape` type L563-584 — `= ParticleShape` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ParticleBehavior` type L596-615 — `= ParticleBehavior` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ParticleEffect` type L631-645 — `impl Default for ParticleEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L632-644 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `MatrixEffect` type L658-669 — `impl Default for MatrixEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L659-668 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ShapeType` type L684-709 — `= ShapeType` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ShapeRotation` type L720-737 — `= ShapeRotation` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ShapeMotion` type L750-771 — `= ShapeMotion` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ShapeEffect` type L791-809 — `impl Default for ShapeEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L792-808 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `SpriteMotion` type L822-843 — `= SpriteMotion` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `SpritePosition` type L860-889 — `= SpritePosition` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `CrtEffect` type L911-924 — `impl Default for CrtEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L912-923 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `SpriteEffect` type L958-977 — `impl Default for SpriteEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L959-976 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `SelectionStyle` type L986-993 — `impl Default for SelectionStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L987-992 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `HighlightStyle` type L1006-1014 — `impl Default for HighlightStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1007-1013 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `TabBarStyle` type L1031-1041 — `impl Default for TabBarStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1032-1040 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `TabStyle` type L1064-1077 — `impl Default for TabStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1065-1076 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `TabActiveStyle` type L1092-1101 — `impl Default for TabActiveStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1093-1100 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `TabCloseStyle` type L1118-1128 — `impl Default for TabCloseStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1119-1127 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `FocusStyle` type L1152-1161 — `impl Default for FocusStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1153-1160 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `HoverStyle` type L1170-1176 — `impl Default for HoverStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1171-1175 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ContextMenuStyle` type L1191-1200 — `impl Default for ContextMenuStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1192-1199 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `SearchBarStyle` type L1215-1224 — `impl Default for SearchBarStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1216-1223 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `RenameBarStyle` type L1237-1245 — `impl Default for RenameBarStyle` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1238-1244 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `SpriteOverlayPosition` type L1290-1309 — `= SpriteOverlayPosition` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `StarfieldPatch` type L1547-1588 — `impl ToEffectConfig for StarfieldPatch` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1548-1587 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ParticlePatch` type L1590-1619 — `impl ToEffectConfig for ParticlePatch` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1591-1618 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `GridPatch` type L1621-1656 — `impl ToEffectConfig for GridPatch` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1622-1655 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `RainPatch` type L1658-1687 — `impl ToEffectConfig for RainPatch` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1659-1686 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `MatrixPatch` type L1689-1709 — `impl ToEffectConfig for MatrixPatch` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1690-1708 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ShapePatch` type L1711-1752 — `impl ToEffectConfig for ShapePatch` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1712-1751 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `StarfieldEffect` type L1755-1774 — `impl ToEffectConfig for StarfieldEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1756-1773 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ParticleEffect` type L1776-1789 — `impl ToEffectConfig for ParticleEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1777-1788 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `GridEffect` type L1791-1809 — `impl ToEffectConfig for GridEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1792-1808 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `RainEffect` type L1811-1824 — `impl ToEffectConfig for RainEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1812-1823 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `MatrixEffect` type L1826-1836 — `impl ToEffectConfig for MatrixEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1827-1835 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `ShapeEffect` type L1838-1866 — `impl ToEffectConfig for ShapeEffect` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `to_config_pairs` function L1839-1865 — `(&self) -> Vec<(&'static str, String)>` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `SpriteOverlay` type L1887-1899 — `impl Default for SpriteOverlay` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L1888-1898 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `EventOverride` type L1943-1988 — `= EventOverride` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `Theme` type L2033-2037 — `impl Default for Theme` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `default` function L2034-2036 — `() -> Self` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `Theme` type L2039-2179 — `= Theme` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `tests` module L2206-2486 — `-` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_color_from_hex` function L2210-2215 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_theme_uniforms_size` function L2218-2221 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_ansi_palette_get` function L2224-2228 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_ansi_palette_extended` function L2231-2254 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_calculate_extended` function L2257-2281 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_event_override_merge_duration` function L2288-2299 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_event_override_merge_zero_duration_preserves_base` function L2302-2313 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_event_override_merge_starfield_patch` function L2316-2342 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_event_override_merge_particle_patch` function L2345-2368 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_event_override_merge_grid_patch` function L2371-2387 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_event_override_merge_multiple_patches` function L2390-2419 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_sprite_patch_default` function L2422-2429 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_starfield_patch_default` function L2432-2438 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_particle_patch_default` function L2441-2447 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_grid_patch_default` function L2450-2456 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_rain_patch_default` function L2459-2465 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_matrix_patch_default` function L2468-2474 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.
-  `test_shape_patch_default` function L2477-2485 — `()` — Provides theme structures that map to shader uniforms for terminal rendering.

#### crates/crt-theme/src/parser.rs

- pub `ThemeParseError` enum L26-38 — `CssError | InvalidColor | InvalidGradient | MissingProperty` — CSS parsing including support for calc(), color functions, and @keyframes.
- pub `parse_hex_color` function L166-231 — `(hex: &str) -> Result<Color, ThemeParseError>` — Parse a hex color (#rgb, #rgba, #rrggbb, #rrggbbaa)
- pub `parse_rgb_color` function L234-277 — `(input: &str) -> Result<Color, ThemeParseError>` — Parse rgb(r, g, b) or rgba(r, g, b, a)
- pub `parse_color` function L280-282 — `(value: &str) -> Result<Color, ThemeParseError>` — Parse any CSS color value (string fallback)
- pub `parse_linear_gradient` function L285-316 — `(value: &str) -> Result<LinearGradient, ThemeParseError>` — Parse linear-gradient from string
- pub `parse_text_shadow` function L319-346 — `(value: &str) -> Result<TextShadow, ThemeParseError>` — Parse text-shadow: offset-x offset-y blur-radius color
- pub `parse_background_size` function L349-386 — `(value: &str) -> BackgroundSize` — Parse background-size from CSS string
- pub `parse_background_position` function L389-414 — `(value: &str) -> BackgroundPosition` — Parse background-position from CSS string
- pub `parse_background_repeat` function L417-426 — `(value: &str) -> BackgroundRepeat` — Parse background-repeat from CSS string
- pub `parse_cursor_shape` function L429-437 — `(value: &str) -> Option<CursorShape>` — Parse cursor shape from string
- pub `parse_theme` function L694-710 — `(css: &str) -> Result<Theme, ThemeParseError>` — Parse CSS theme using lightningcss
-  `opts` function L41-43 — `() -> PrinterOptions<'static>` — Helper to get PrinterOptions (since it doesn't implement Copy)
-  `css_color_to_color` function L47-64 — `(css_color: &CssColor) -> Result<Color, ThemeParseError>` — Convert lightningcss CssColor to our Color type
-  `parse_color_string` function L67-78 — `(value: &str) -> Result<Color, ThemeParseError>` — Parse a color from a CSS string value
-  `parse_named_color` function L81-163 — `(name: &str) -> Option<Color>` — Parse CSS named colors
-  `RuleProperties` struct L440-443 — `{ standard: HashMap<String, String>, custom: HashMap<String, String> }` — Collected properties from a CSS rule
-  `extract_properties` function L446-686 — `(rule: &lightningcss::rules::style::StyleRule) -> RuleProperties` — Extract properties from a style rule's declarations
-  `get_selector_string` function L689-691 — `(rule: &lightningcss::rules::style::StyleRule) -> String` — Get selector string from a style rule
-  `apply_properties` function L713-789 — `( theme: &mut Theme, selector: &str, standard: &HashMap<String, String>, custom:...` — Apply parsed properties to theme based on selector
-  `apply_terminal_properties` function L791-876 — `( theme: &mut Theme, standard: &HashMap<String, String>, custom: &HashMap<String...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_selection_properties` function L878-889 — `( theme: &mut Theme, standard: &HashMap<String, String>, ) -> Result<(), ThemePa...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_highlight_properties` function L891-906 — `( theme: &mut Theme, standard: &HashMap<String, String>, custom: &HashMap<String...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_cursor_properties` function L908-920 — `( theme: &mut Theme, standard: &HashMap<String, String>, ) -> Result<(), ThemePa...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_backdrop_properties` function L922-1339 — `( theme: &mut Theme, custom: &HashMap<String, String>, ) -> Result<(), ThemePars...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_tab_bar_properties` function L1341-1362 — `( theme: &mut Theme, standard: &HashMap<String, String>, custom: &HashMap<String...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_tab_properties` function L1364-1393 — `( theme: &mut Theme, standard: &HashMap<String, String>, ) -> Result<(), ThemePa...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_tab_active_properties` function L1395-1413 — `( theme: &mut Theme, standard: &HashMap<String, String>, _custom: &HashMap<Strin...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_tab_close_properties` function L1415-1436 — `( theme: &mut Theme, standard: &HashMap<String, String>, custom: &HashMap<String...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_focus_properties` function L1438-1465 — `( theme: &mut Theme, standard: &HashMap<String, String>, custom: &HashMap<String...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_hover_properties` function L1467-1475 — `( theme: &mut Theme, standard: &HashMap<String, String>, ) -> Result<(), ThemePa...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_context_menu_properties` function L1477-1491 — `( theme: &mut Theme, standard: &HashMap<String, String>, ) -> Result<(), ThemePa...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_search_bar_properties` function L1493-1511 — `( theme: &mut Theme, standard: &HashMap<String, String>, custom: &HashMap<String...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_rename_bar_properties` function L1513-1528 — `( theme: &mut Theme, standard: &HashMap<String, String>, custom: &HashMap<String...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_event_properties` function L1532-1930 — `( event: &mut Option<EventOverride>, standard: &HashMap<String, String>, custom:...` — Apply event override properties (::on-bell, ::on-command-fail, etc.)
-  `parse_duration` function L1933-1944 — `(s: &str) -> u32` — Parse duration string (e.g., "500ms", "1.5s", "1000") to milliseconds
-  `strip_quotes` function L1947-1954 — `(s: &str) -> String` — Strip surrounding quotes from a string
-  `apply_ansi_palette` function L1962-2026 — `( theme: &mut Theme, custom: &HashMap<String, String>, ) -> Result<(), ThemePars...` — Apply ANSI palette colors from custom properties
-  `get_color` function L1967-1971 — `(custom: &'a HashMap<String, String>, name: &str) -> Option<&'a String>` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `apply_palette_properties` function L2028-2093 — `( theme: &mut Theme, custom: &HashMap<String, String>, ) -> Result<(), ThemePars...` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `tests` module L2096-3269 — `-` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_hex_color` function L2100-2107 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_rgb_color` function L2110-2117 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_simple_theme` function L2120-2130 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_theme_with_comments` function L2133-2149 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_gradient` function L2152-2155 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_ansi_palette` function L2158-2197 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_background_image` function L2200-2220 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_background_size` function L2223-2231 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_background_position` function L2234-2247 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_background_repeat` function L2250-2264 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_extended_palette` function L2267-2299 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_cursor_with_glow` function L2302-2325 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_hex_color_3_digit` function L2330-2342 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_hex_color_4_digit` function L2345-2360 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_hex_color_6_digit` function L2363-2378 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_hex_color_8_digit` function L2381-2393 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_hex_color_uppercase` function L2396-2403 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_hex_color_without_hash` function L2406-2410 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_hex_color_invalid` function L2413-2423 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_rgb_color_0_255_values` function L2426-2437 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_rgb_color_0_1_values` function L2440-2445 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_rgba_color` function L2448-2458 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_rgb_color_whitespace` function L2461-2468 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_rgb_color_invalid` function L2471-2483 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_named_color_basic` function L2486-2501 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_named_color_case_insensitive` function L2504-2513 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_named_color_gray_grey_variants` function L2516-2532 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_named_color_cyan_aqua_alias` function L2535-2541 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_named_color_magenta_fuchsia_alias` function L2544-2550 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_named_color_transparent` function L2553-2559 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_named_color_extended` function L2562-2573 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_named_color_unknown` function L2576-2580 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_color_dispatch` function L2583-2597 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_color_whitespace_handling` function L2600-2606 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_gradient_with_direction` function L2611-2621 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_gradient_without_direction` function L2624-2630 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_gradient_with_color_stops` function L2633-2639 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_gradient_with_named_colors` function L2642-2650 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_gradient_whitespace_handling` function L2657-2663 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_gradient_invalid_format` function L2666-2678 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_gradient_mixed_color_formats` function L2681-2691 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_text_shadow_basic` function L2696-2702 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_text_shadow_with_rgba` function L2705-2711 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_text_shadow_with_rgb` function L2714-2718 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_text_shadow_different_radii` function L2721-2730 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_text_shadow_invalid` function L2733-2737 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_size_keywords` function L2742-2748 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_size_fixed_dimensions` function L2751-2760 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_size_single_px` function L2763-2768 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_size_canvas_percent` function L2771-2780 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_size_image_scale` function L2783-2789 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_size_whitespace` function L2792-2798 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_position_keywords` function L2803-2822 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_position_corners` function L2825-2846 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_position_percentages` function L2849-2870 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_position_case_insensitive` function L2873-2882 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_repeat_keywords` function L2887-2901 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_repeat_two_value_syntax` function L2904-2917 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_repeat_case_insensitive` function L2920-2926 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_background_repeat_unknown_defaults_to_no_repeat` function L2929-2935 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_on_bell_event` function L2942-2959 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_on_command_success_event` function L2962-2975 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_on_command_fail_event` function L2978-2993 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_on_focus_event` function L2996-3008 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_on_blur_event` function L3011-3024 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_starfield_patch_in_event` function L3027-3047 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_particle_patch_in_event` function L3050-3070 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_grid_patch_in_event` function L3073-3091 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_rain_patch_in_event` function L3094-3112 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_matrix_patch_in_event` function L3115-3133 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_shape_patch_in_event` function L3136-3156 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_sprite_patch_in_event` function L3159-3179 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_parse_duration_formats` function L3182-3206 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_multiple_events_in_theme` function L3209-3233 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.
-  `test_combined_patches_in_single_event` function L3236-3268 — `()` — CSS parsing including support for calc(), color functions, and @keyframes.

### src/app

> *Semantic summary to be generated by AI agent.*

#### src/app/effects.rs

-  `configure_effects_from_theme` function L9-232 — `(effects_renderer: &mut EffectsRenderer, theme: &Theme)` — Configure backdrop effects from theme settings

#### src/app/handler.rs

-  `App` type L33-638 — `impl ApplicationHandler for App` — Handles window events, keyboard/mouse input, and frame timing.
-  `resumed` function L34-50 — `(&mut self, event_loop: &ActiveEventLoop)` — Handles window events, keyboard/mouse input, and frame timing.
-  `window_event` function L52-515 — `(&mut self, event_loop: &ActiveEventLoop, id: WindowId, event: WindowEvent)` — Handles window events, keyboard/mouse input, and frame timing.
-  `about_to_wait` function L517-637 — `(&mut self, event_loop: &ActiveEventLoop)` — Handles window events, keyboard/mouse input, and frame timing.
-  `TARGET_FRAME_TIME` variable L606-607 — `: std::time::Duration` — Handles window events, keyboard/mouse input, and frame timing.
-  `UNFOCUSED_FRAME_TIME` variable L623-624 — `: std::time::Duration` — Handles window events, keyboard/mouse input, and frame timing.

#### src/app/initialization.rs

-  `DetachPayload` struct L22-27 — `{ tab: Tab, shell: ShellTerminal, content_hash: u64, screen_position: Option<win...` — Payload for creating a window with an existing tab+shell (detach operation).
-  `MergePayload` struct L30-36 — `{ tab: Tab, shell: ShellTerminal, content_hash: u64, target_window_id: WindowId,...` — Payload for merging a tab into an existing window.
-  `App` type L44-447 — `= App` — Contains the heavy `create_window()` function and scale factor handling.
-  `create_window` function L45-398 — `(&mut self, event_loop: &ActiveEventLoop) -> WindowId` — Contains the heavy `create_window()` function and scale factor handling.
-  `create_window_for_detach` function L404-446 — `( &mut self, event_loop: &ActiveEventLoop, payload: DetachPayload, ) -> WindowId` — Create a new window with an existing tab+shell (for tab detachment).
-  `handle_scale_factor_change` function L453-547 — `( state: &mut WindowState, shared: &SharedGpuState, config: &Config, new_scale: ...` — Handle scale factor change (display DPI change)

#### src/app/menu_actions.rs

-  `App` type L15-280 — `= App` — Processes macOS menu bar actions (new tab, close, theme switching, etc.).
-  `handle_menu_action` function L16-182 — `(&mut self, action: MenuAction, event_loop: &ActiveEventLoop)` — Processes macOS menu bar actions (new tab, close, theme switching, etc.).
-  `adjust_font_scale` function L184-259 — `(&mut self, delta: f32)` — Processes macOS menu bar actions (new tab, close, theme switching, etc.).
-  `navigate_tab` function L261-271 — `(&mut self, next: bool)` — Processes macOS menu bar actions (new tab, close, theme switching, etc.).
-  `select_tab_index` function L273-279 — `(&mut self, index: usize)` — Processes macOS menu bar actions (new tab, close, theme switching, etc.).

#### src/app/mod.rs

-  `effects` module L6 — `-` — Contains the `App` struct and core methods for managing windows,
-  `handler` module L7 — `-` — GPU state, config, and theme resources.
-  `initialization` module L8 — `-` — GPU state, config, and theme resources.
-  `menu_actions` module L9 — `-` — GPU state, config, and theme resources.
-  `MIN_FONT_SCALE` variable L33 — `: f32` — GPU state, config, and theme resources.
-  `MAX_FONT_SCALE` variable L34 — `: f32` — GPU state, config, and theme resources.
-  `FONT_SCALE_STEP` variable L35 — `: f32` — GPU state, config, and theme resources.
-  `App` struct L37-69 — `{ windows: HashMap<WindowId, WindowState>, shared_gpu: Option<SharedGpuState>, f...` — GPU state, config, and theme resources.
-  `App` type L71-433 — `= App` — GPU state, config, and theme resources.
-  `new` function L72-107 — `() -> Self` — GPU state, config, and theme resources.
-  `next_tab_id` function L110-114 — `(&mut self) -> u64` — Allocate the next globally unique tab ID.
-  `create_drag_overlay` function L120-155 — `( &mut self, event_loop: &winit::event_loop::ActiveEventLoop, title: &str, scree...` — Create a small floating overlay window for drag feedback.
-  `init_shared_gpu` function L157-161 — `(&mut self)` — GPU state, config, and theme resources.
-  `focused_window_mut` function L163-165 — `(&mut self) -> Option<&mut WindowState>` — GPU state, config, and theme resources.
-  `update_crt_pipeline` function L168-201 — `( state: &mut WindowState, shared: &SharedGpuState, theme: &Theme, )` — Update CRT pipeline and textures for new theme
-  `update_background_image` function L204-231 — `( state: &mut WindowState, device: &wgpu::Device, queue: &wgpu::Queue, theme: &T...` — Update background image state for new theme
-  `create_sprite_state` function L234-292 — `( device: &wgpu::Device, queue: &wgpu::Queue, theme: &Theme, format: wgpu::Textu...` — Create sprite animation state from theme configuration
-  `close_window` function L294-333 — `(&mut self, window_id: WindowId)` — GPU state, config, and theme resources.
-  `reload_config` function L336-380 — `(&mut self)` — Reload config from disk and apply changes
-  `reload_theme` function L383-431 — `(&mut self)` — Reload themes from disk and apply to all windows
-  `apply_theme_to_window` function L440-463 — `( state: &mut WindowState, shared_gpu: Option<&SharedGpuState>, theme_name: &str...` — Apply a theme switch to a specific window state.

### src/bin

> *Semantic summary to be generated by AI agent.*

#### src/bin/benchmark.rs

-  `FrameStats` struct L14-17 — `{ frame_times: VecDeque<Duration>, max_samples: usize }` — Frame timing statistics
-  `FrameStats` type L19-72 — `= FrameStats` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `new` function L20-25 — `(max_samples: usize) -> Self` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `record` function L27-32 — `(&mut self, duration: Duration)` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `avg_ms` function L34-40 — `(&self) -> f64` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `min_ms` function L42-47 — `(&self) -> f64` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `max_ms` function L49-54 — `(&self) -> f64` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `p99_ms` function L56-66 — `(&self) -> f64` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `fps` function L68-71 — `(&self) -> f64` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `MemoryStats` struct L77-80 — `{ rss_bytes: u64, timestamp: Instant }` — Memory statistics
-  `MemoryStats` type L82-125 — `= MemoryStats` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `capture` function L84-100 — `() -> Option<Self>` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `capture` function L103-120 — `() -> Option<Self>` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `rss_mb` function L122-124 — `(&self) -> f64` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `Scenario` struct L129-133 — `{ name: &'static str, description: &'static str, duration_secs: u64 }` — Benchmark scenario
-  `ScenarioResult` struct L137-143 — `{ scenario: Scenario, frame_stats: FrameStats, memory_start: Option<MemoryStats>...` — Benchmark result for a single scenario
-  `ScenarioResult` type L145-185 — `= ScenarioResult` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `print_report` function L146-184 — `(&self)` — This opens a real window, runs rendering scenarios, and reports performance data.
-  `generate_scenario_content` function L188-254 — `(scenario_name: &str, frame: u64) -> Vec<u8>` — Generate content for different scenarios
-  `run_scenario` function L257-311 — `(scenario: Scenario) -> ScenarioResult` — Run a single benchmark scenario
-  `main` function L313-392 — `()` — This opens a real window, runs rendering scenarios, and reports performance data.

#### src/bin/benchmark_gpu.rs

-  `FrameStats` struct L13-16 — `{ times: VecDeque<Duration>, max_samples: usize }` — Frame timing statistics
-  `FrameStats` type L18-53 — `= FrameStats` — by running scripted content scenarios and measuring frame times.
-  `new` function L19-24 — `(max_samples: usize) -> Self` — by running scripted content scenarios and measuring frame times.
-  `record` function L26-31 — `(&mut self, d: Duration)` — by running scripted content scenarios and measuring frame times.
-  `avg_ms` function L33-38 — `(&self) -> f64` — by running scripted content scenarios and measuring frame times.
-  `fps` function L40-43 — `(&self) -> f64` — by running scripted content scenarios and measuring frame times.
-  `percentile` function L45-52 — `(&self, p: f64) -> f64` — by running scripted content scenarios and measuring frame times.
-  `get_rss_mb` function L56-69 — `() -> f64` — by running scripted content scenarios and measuring frame times.
-  `get_rss_mb` function L72-74 — `() -> f64` — by running scripted content scenarios and measuring frame times.
-  `main` function L76-170 — `()` — by running scripted content scenarios and measuring frame times.

#### src/bin/profile_gpu_memory.rs

-  `main` function L23-473 — `()` — ```
-  `load_system_font` function L476-517 — `() -> Vec<u8>` — Load a monospace font from system fonts.

#### src/bin/profile_memory.rs

-  `ALLOC` variable L16 — `: dhat::Alloc` — Exercises the terminal hot paths and produces a DHAT allocation profile.
-  `main` function L27-152 — `()` — Simulate realistic terminal workload:

### src

> *Semantic summary to be generated by AI agent.*

#### src/config.rs

- pub `ConfigPaths` struct L13-16 — `{ config_dir: PathBuf }` — Configuration paths that can be overridden for testing
- pub `new` function L20-22 — `(config_dir: PathBuf) -> Self` — Create from a specific directory
- pub `from_env_or_default` function L25-40 — `() -> Option<Self>` — Get paths from environment or default
- pub `config_path` function L43-45 — `(&self) -> PathBuf` — Get the config file path
- pub `themes_dir` function L48-50 — `(&self) -> PathBuf` — Get the themes directory path
- pub `shell_assets_dir` function L53-55 — `(&self) -> PathBuf` — Get the shell integration assets directory path
- pub `ShellConfig` struct L62-73 — `{ program: Option<String>, args: Vec<String>, working_directory: Option<PathBuf>...` — Shell configuration
- pub `FontConfig` struct L78-86 — `{ family: Vec<String>, size: f32, line_height: f32 }` — Font configuration
- pub `WindowConfig` struct L107-116 — `{ columns: usize, rows: usize, title: String, fullscreen: bool }` — Window configuration
- pub `ThemeConfig` struct L132-135 — `{ name: String }` — Theme configuration
- pub `CursorStyle` enum L152-157 — `Block | Bar | Underline` — Cursor shape style
- pub `CursorConfig` struct L162-169 — `{ style: CursorStyle, blink: bool, blink_interval_ms: u64 }` — Cursor configuration
- pub `BellConfig` struct L184-191 — `{ visual: bool, flash_duration_ms: u64, flash_intensity: f32 }` — Bell configuration
- pub `KeyAction` enum L206-227 — `NewTab | CloseTab | NextTab | PrevTab | SelectTab1 | SelectTab2 | SelectTab3 | S...` — Keybinding action
- pub `Keybinding` struct L231-239 — `{ key: String, mods: Vec<String>, action: KeyAction }` — Single keybinding
- pub `KeybindingsConfig` struct L244-247 — `{ bindings: Vec<Keybinding> }` — Keybindings configuration
- pub `Config` struct L360-368 — `{ shell: ShellConfig, font: FontConfig, window: WindowConfig, theme: ThemeConfig...` — Complete configuration
- pub `config_dir` function L372-374 — `() -> Option<PathBuf>` — Get the config directory path (~/.config/crt or from CRT_CONFIG_DIR)
- pub `load` function L377-379 — `() -> Self` — Load config from default path (respects CRT_CONFIG_DIR env var)
- pub `load_with_error` function L382-393 — `() -> (Self, Option<String>)` — Load config from default path, returning any error message
- pub `load_from` function L397-399 — `(path: &Path) -> Self` — Load config from a specific file path
- pub `load_from_with_error` function L402-426 — `(path: &Path) -> (Self, Option<String>)` — Load config from a specific file path, returning any error message
- pub `load_with_paths` function L430-433 — `(paths: &ConfigPaths) -> Self` — Load config using specified paths
- pub `shell_assets_dir` function L436-438 — `() -> Option<PathBuf>` — Get the shell integration assets directory
-  `ConfigPaths` type L18-56 — `= ConfigPaths` — - `ConfigPaths` for programmatic control (useful for testing)
-  `FontConfig` type L88-102 — `impl Default for FontConfig` — - `ConfigPaths` for programmatic control (useful for testing)
-  `default` function L89-101 — `() -> Self` — - `ConfigPaths` for programmatic control (useful for testing)
-  `WindowConfig` type L118-127 — `impl Default for WindowConfig` — - `ConfigPaths` for programmatic control (useful for testing)
-  `default` function L119-126 — `() -> Self` — - `ConfigPaths` for programmatic control (useful for testing)
-  `ThemeConfig` type L137-147 — `impl Default for ThemeConfig` — - `ConfigPaths` for programmatic control (useful for testing)
-  `default` function L138-146 — `() -> Self` — - `ConfigPaths` for programmatic control (useful for testing)
-  `CursorConfig` type L171-179 — `impl Default for CursorConfig` — - `ConfigPaths` for programmatic control (useful for testing)
-  `default` function L172-178 — `() -> Self` — - `ConfigPaths` for programmatic control (useful for testing)
-  `BellConfig` type L193-201 — `impl Default for BellConfig` — - `ConfigPaths` for programmatic control (useful for testing)
-  `default` function L194-200 — `() -> Self` — - `ConfigPaths` for programmatic control (useful for testing)
-  `KeybindingsConfig` type L249-355 — `impl Default for KeybindingsConfig` — - `ConfigPaths` for programmatic control (useful for testing)
-  `default` function L250-354 — `() -> Self` — - `ConfigPaths` for programmatic control (useful for testing)
-  `Config` type L370-439 — `= Config` — - `ConfigPaths` for programmatic control (useful for testing)
-  `tests` module L442-1083 — `-` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_config_paths_new` function L448-456 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_config_default` function L459-469 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_load_from_missing_file` function L472-476 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_load_from_valid_file` function L479-506 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_load_with_paths` function L509-524 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_load_invalid_toml` function L527-536 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_partial_config_uses_defaults` function L539-555 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_cursor_style_default` function L560-565 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_cursor_style_serde` function L568-580 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_cursor_blink_config` function L583-593 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_bell_config_default` function L598-603 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_bell_config_serde` function L606-618 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_font_config_default` function L623-629 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_font_config_serde` function L632-644 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_font_config_empty_family` function L647-650 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_shell_config_default` function L655-660 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_shell_config_serde` function L663-675 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_window_config_default` function L680-686 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_window_config_serde` function L689-703 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_theme_config_default` function L708-714 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_theme_config_serde` function L717-720 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_key_action_serde` function L725-756 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_keybinding_with_multiple_mods` function L759-771 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_keybinding_no_mods` function L774-785 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_keybindings_config_default` function L788-804 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_load_with_error_returns_error_message` function L809-824 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_load_with_error_no_error_on_valid` function L827-837 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_load_with_error_missing_file` function L840-847 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_full_config_serde` function L852-909 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_config_unknown_fields_ignored` function L912-929 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_config_type_mismatch_uses_default` function L932-944 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_keybinding_invalid_action_rejected` function L949-958 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_keybinding_empty_mods_list` function L961-972 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_keybindings_config_custom_replaces_defaults` function L975-994 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_cursor_invalid_style_rejected` function L997-1000 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_partial_nested_config_preserves_sibling_defaults` function L1003-1015 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_empty_config_file_uses_all_defaults` function L1018-1027 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_config_paths_shell_assets_dir` function L1030-1036 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_shell_config_semantic_prompts_default_false` function L1039-1042 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_shell_config_semantic_prompts_serde` function L1045-1048 — `()` — - `ConfigPaths` for programmatic control (useful for testing)
-  `test_all_key_actions_deserialize` function L1051-1082 — `()` — - `ConfigPaths` for programmatic control (useful for testing)

#### src/font.rs

- pub `load_font_variants` function L82-121 — `(config: &FontConfig) -> FontVariants` — Load font variants based on config
- pub `list_monospace_fonts` function L125-142 — `() -> Vec<String>` — List available monospace fonts (for debugging/config help)
-  `FONT_DB` variable L12 — `: OnceLock<Database>` — Global font database (loaded once)
-  `fonts_dir` function L15-17 — `() -> Option<PathBuf>` — Get the fonts directory path (~/.config/crt/fonts)
-  `font_db` function L20-40 — `() -> &'static Database` — Get or initialize the font database
-  `load_font` function L43-62 — `(family: &str, weight: Weight, style: Style) -> Option<Vec<u8>>` — Load font data by family name and style
-  `load_font_from_families` function L65-73 — `(families: &[String], weight: Weight, style: Style) -> Option<Vec<u8>>` — Try to load a font from a list of family names (first match wins)

#### src/main.rs

- pub `profiling` module L13 — `-` — 2.
-  `app` module L7 — `-` — Two-pass rendering:
-  `config` module L8 — `-` — 2.
-  `font` module L9 — `-` — 2.
-  `gpu` module L10 — `-` — 2.
-  `input` module L11 — `-` — 2.
-  `menu` module L12 — `-` — 2.
-  `render` module L14 — `-` — 2.
-  `theme_registry` module L15 — `-` — 2.
-  `watcher` module L16 — `-` — 2.
-  `window` module L17 — `-` — 2.
-  `main` function L21-48 — `()` — 2.

#### src/menu.rs

- pub `MenuAction` enum L13-47 — `NewTab | NewWindow | RenameWindow | CloseTab | CloseWindow | Quit | Copy | Paste...` — Menu action identifiers
- pub `tab_index` function L51-64 — `(&self) -> Option<usize>` — Returns the 0-based tab index for SelectTab variants, or None for other actions.
- pub `MenuIds` struct L69-90 — `{ new_tab: MenuId, new_window: MenuId, rename_window: MenuId, close_tab: MenuId,...` — Menu item IDs stored for event handling
- pub `build_menu_bar` function L93-426 — `(_theme_names: &[&str], _current_theme: &str) -> (Menu, MenuIds, Submenu)` — Creates and manages the native macOS menu bar with standard terminal actions.
- pub `set_windows_menu` function L431-452 — `(window_submenu: &Submenu)` — Set the Window submenu as the macOS Windows menu
- pub `menu_id_to_action` function L455-548 — `(id: &MenuId, ids: &MenuIds) -> Option<MenuAction>` — Creates and manages the native macOS menu bar with standard terminal actions.
-  `MenuAction` type L49-65 — `= MenuAction` — Creates and manages the native macOS menu bar with standard terminal actions.

#### src/profiling.rs

- pub `is_enabled` function L25-27 — `() -> bool` — Check if profiling is enabled (fast path)
- pub `init` function L30-36 — `()` — Initialize the profiler (call once at startup)
- pub `start_profiling` function L40-65 — `() -> Option<PathBuf>` — Start profiling (can be called at runtime)
- pub `stop_profiling` function L69-88 — `() -> Option<PathBuf>` — Stop profiling and finalize the log
- pub `toggle` function L92-100 — `() -> (bool, Option<PathBuf>)` — Toggle profiling on/off
- pub `init_from_config` function L103-121 — `(enabled: bool, output_path: Option<PathBuf>)` — Initialize from config (call after config is loaded)
- pub `record_frame` function L135-140 — `(timing: FrameTiming)` — Record a frame timing
- pub `record_subsystem` function L143-148 — `(name: &'static str, duration: Duration)` — Record a subsystem timing
- pub `event` function L151-156 — `(category: &str, message: &str)` — Record a custom event
- pub `log_message` function L160-165 — `(level: &str, target: &str, message: &str)` — Forward a log message to the profile file
- pub `record_memory` function L197-202 — `()` — Record memory stats
- pub `shutdown` function L205-207 — `()` — Flush profiling data and write summary (call at app shutdown)
- pub `ScopedTimer` struct L210-213 — `{ name: &'static str, start: Instant }` — Scoped timer that records duration on drop
- pub `new` function L216-221 — `(name: &'static str) -> Self` — which can be shared for debugging.
- pub `FrameTiming` struct L244-250 — `{ total_us: u64, update_us: u64, render_us: u64, present_us: u64, effects_us: u6...` — Frame timing breakdown
- pub `GridSnapshot` struct L254-265 — `{ columns: usize, lines: usize, cursor_col: usize, cursor_line: i32, cursor_visi...` — Grid snapshot for debugging terminal state
- pub `record_grid_snapshot` function L271-276 — `(snapshot: GridSnapshot)` — Record a grid snapshot
-  `PROFILER` variable L20 — `: LazyLock<Mutex<Option<Profiler>>>` — Global profiler instance (Option allows runtime start/stop)
-  `PROFILING_ENABLED` variable L21 — `: AtomicBool` — which can be shared for debugging.
-  `with_profiler` function L124-132 — `(f: F) -> Option<R>` — Execute a closure with the profiler
-  `profile_log` macro L169-194 — `-` — Convenience macro for logging that also forwards to profiler
-  `ScopedTimer` type L215-222 — `= ScopedTimer` — which can be shared for debugging.
-  `ScopedTimer` type L224-228 — `impl Drop for ScopedTimer` — which can be shared for debugging.
-  `drop` function L225-227 — `(&mut self)` — which can be shared for debugging.
-  `profile_scope` macro L232-240 — `-` — Create a scoped timer (no-op if profiling disabled)
-  `GRID_SNAPSHOT_INTERVAL_SECS` variable L268 — `: u64` — How often to capture grid snapshots (in seconds)
-  `TimingStats` struct L280-286 — `{ count: u64, total_us: u64, min_us: u64, max_us: u64, samples: VecDeque<u64> }` — Timing statistics
-  `TimingStats` type L288-325 — `= TimingStats` — which can be shared for debugging.
-  `new` function L289-295 — `() -> Self` — which can be shared for debugging.
-  `record` function L297-307 — `(&mut self, duration_us: u64)` — which can be shared for debugging.
-  `avg_ms` function L309-314 — `(&self) -> f64` — which can be shared for debugging.
-  `percentile` function L316-324 — `(&self, p: f64) -> f64` — which can be shared for debugging.
-  `MemorySample` struct L329-333 — `{ timestamp_ms: u64, rss_kb: u64 }` — Memory sample
-  `Profiler` struct L336-361 — `{ start_time: Instant, output_path: PathBuf, writer: Option<BufWriter<File>>, fr...` — Main profiler state
-  `Profiler` type L363-684 — `= Profiler` — which can be shared for debugging.
-  `new` function L364-402 — `() -> Self` — which can be shared for debugging.
-  `set_output_path` function L404-410 — `(&mut self, path: PathBuf)` — which can be shared for debugging.
-  `write` function L412-417 — `(&mut self, line: &str)` — which can be shared for debugging.
-  `flush` function L419-423 — `(&mut self)` — which can be shared for debugging.
-  `log_system_info` function L425-460 — `(&mut self)` — which can be shared for debugging.
-  `record_frame` function L462-491 — `(&mut self, timing: FrameTiming)` — which can be shared for debugging.
-  `record_subsystem` function L493-499 — `(&mut self, name: &'static str, duration: Duration)` — which can be shared for debugging.
-  `log_event` function L501-504 — `(&mut self, category: &str, message: &str)` — which can be shared for debugging.
-  `record_memory` function L506-514 — `(&mut self)` — which can be shared for debugging.
-  `record_grid_snapshot` function L516-558 — `(&mut self, snapshot: GridSnapshot)` — which can be shared for debugging.
-  `write_periodic_summary` function L560-582 — `(&mut self)` — which can be shared for debugging.
-  `shutdown` function L584-683 — `(&mut self)` — which can be shared for debugging.
-  `get_rss_kb` function L687-693 — `() -> Option<u64>` — which can be shared for debugging.
-  `get_rss_kb` function L696-706 — `() -> Option<u64>` — which can be shared for debugging.
-  `tests` module L709-788 — `-` — which can be shared for debugging.
-  `test_timing_stats` function L713-723 — `()` — which can be shared for debugging.
-  `test_scoped_timer_compiles` function L726-729 — `()` — which can be shared for debugging.
-  `test_is_enabled_default` function L732-736 — `()` — which can be shared for debugging.
-  `test_safe_line_truncation` function L742-787 — `()` — Test that line truncation for profiling snapshot handles multi-byte UTF-8 safely.
-  `truncate_line` function L744-750 — `(line: &str) -> String` — which can be shared for debugging.

#### src/theme_registry.rs

- pub `ThemeRegistry` struct L10-17 — `{ themes: HashMap<String, Theme>, themes_dir: PathBuf, default_theme: String }` — Registry of available themes, loaded from the themes directory
- pub `new` function L21-29 — `(themes_dir: PathBuf, default_theme: String) -> Self` — Create a new theme registry and scan for available themes
- pub `scan_themes` function L32-70 — `(&mut self)` — Scan the themes directory and load all available themes
- pub `list_themes` function L79-83 — `(&self) -> Vec<&str>` — Get list of available theme names, sorted alphabetically
- pub `get_theme` function L86-88 — `(&self, name: &str) -> Option<&Theme>` — Get a theme by name
- pub `get_default_theme` function L91-105 — `(&self) -> (&str, Theme)` — Get the default theme (from config or fallback)
- pub `default_theme_name` function L108-110 — `(&self) -> &str` — Get the default theme name
- pub `reload_all` function L113-115 — `(&mut self)` — Reload all themes from disk
-  `ThemeRegistry` type L19-116 — `= ThemeRegistry` — Scans the themes directory and caches parsed themes for runtime switching.
-  `load_theme_from_path` function L73-76 — `(&self, path: &std::path::Path) -> Result<Theme, String>` — Load a theme from a CSS file path
-  `tests` module L119-256 — `-` — Scans the themes directory and caches parsed themes for runtime switching.
-  `MINIMAL_CSS` variable L125-130 — `: &str` — Minimal valid CSS theme for testing
-  `setup_themes_dir` function L132-138 — `(themes: &[(&str, &str)]) -> TempDir` — Scans the themes directory and caches parsed themes for runtime switching.
-  `new_empty_dir` function L141-145 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `new_nonexistent_dir` function L148-152 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `loads_css_files` function L155-162 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `ignores_non_css_files` function L165-172 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `get_theme_by_name` function L175-180 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `get_default_theme_when_present` function L183-188 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `get_default_theme_falls_back_when_missing` function L191-197 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `get_default_theme_uses_builtin_when_no_themes` function L200-205 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `default_theme_name_returns_configured` function L208-212 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `list_themes_sorted_alphabetically` function L215-219 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `reload_picks_up_new_themes` function L222-232 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `reload_removes_deleted_themes` function L235-244 — `()` — Scans the themes directory and caches parsed themes for runtime switching.
-  `malformed_css_skipped` function L247-255 — `()` — Scans the themes directory and caches parsed themes for runtime switching.

#### src/watcher.rs

- pub `ConfigEvent` enum L16-21 — `ConfigChanged | ThemeChanged` — Events from the config watcher
- pub `ConfigWatcher` struct L24-29 — `{ _watcher: RecommendedWatcher, receiver: Receiver<ConfigEvent>, last_config_eve...` — Watches config directory for changes
- pub `new` function L33-92 — `() -> Option<Self>` — Create a new config watcher
- pub `poll` function L95-122 — `(&mut self) -> Option<ConfigEvent>` — Poll for config events (non-blocking) with debouncing
-  `DEBOUNCE_MS` variable L12 — `: u128` — Debounce window in milliseconds
-  `ConfigWatcher` type L31-123 — `= ConfigWatcher` — Watches ~/.config/crt/config.toml and theme files for changes.

### src/gpu

> *Semantic summary to be generated by AI agent.*

#### src/gpu/buffer_pool.rs

- pub `BufferClass` enum L25-32 — `GridInstance | RectInstance | SmallUniform` — Buffer size classes for pooling
- pub `size_bytes` function L36-42 — `(&self) -> u64` — Get the size in bytes for this buffer class
- pub `usages` function L45-52 — `(&self) -> wgpu::BufferUsages` — Get the buffer usage flags for this class
- pub `label` function L55-61 — `(&self) -> &'static str` — Get a descriptive label for this class
- pub `PoolStats` struct L74-78 — `{ allocations: u64, reuses: u64, returns: u64 }` — Pool statistics for monitoring
- pub `BufferPool` struct L153-155 — `{ inner: Arc<Mutex<BufferPoolInner>> }` — Buffer pool for reusing GPU buffers across window lifecycles
- pub `new` function L163-167 — `(device: Arc<wgpu::Device>, max_per_class: usize) -> Self` — Create a new buffer pool
- pub `checkout` function L174-187 — `(&self, class: BufferClass) -> Option<PooledBuffer>` — Check out a buffer from the pool
- pub `stats` function L190-198 — `(&self) -> PoolStats` — Get current pool statistics
- pub `shrink` function L203-210 — `(&self)` — Shrink the pool by releasing excess buffers
- pub `PooledBuffer` struct L216-220 — `{ buffer: Option<wgpu::Buffer>, class: BufferClass, pool: Weak<Mutex<BufferPoolI...` — A buffer checked out from the pool with RAII semantics
- pub `buffer` function L224-226 — `(&self) -> &wgpu::Buffer` — Get a reference to the underlying buffer
- pub `class` function L229-231 — `(&self) -> BufferClass` — Get the buffer class
-  `BufferClass` type L34-62 — `= BufferClass` — The RAII pattern means buffers auto-return to pool when windows close.
-  `BufferPoolInner` struct L65-70 — `{ device: Arc<wgpu::Device>, pools: HashMap<BufferClass, Vec<wgpu::Buffer>>, max...` — Internal pool state
-  `BufferPoolInner` type L80-142 — `= BufferPoolInner` — The RAII pattern means buffers auto-return to pool when windows close.
-  `new` function L81-88 — `(device: Arc<wgpu::Device>, max_per_class: usize) -> Self` — The RAII pattern means buffers auto-return to pool when windows close.
-  `checkout` function L90-113 — `(&mut self, class: BufferClass) -> wgpu::Buffer` — The RAII pattern means buffers auto-return to pool when windows close.
-  `return_buffer` function L115-129 — `(&mut self, class: BufferClass, buffer: wgpu::Buffer)` — The RAII pattern means buffers auto-return to pool when windows close.
-  `shrink` function L131-141 — `(&mut self)` — The RAII pattern means buffers auto-return to pool when windows close.
-  `BufferPool` type L157-211 — `= BufferPool` — The RAII pattern means buffers auto-return to pool when windows close.
-  `PooledBuffer` type L222-232 — `= PooledBuffer` — The RAII pattern means buffers auto-return to pool when windows close.
-  `PooledBuffer` type L234-247 — `impl Drop for PooledBuffer` — The RAII pattern means buffers auto-return to pool when windows close.
-  `drop` function L235-246 — `(&mut self)` — The RAII pattern means buffers auto-return to pool when windows close.
-  `PooledBuffer` type L249-255 — `= PooledBuffer` — The RAII pattern means buffers auto-return to pool when windows close.
-  `Target` type L250 — `= wgpu::Buffer` — The RAII pattern means buffers auto-return to pool when windows close.
-  `deref` function L252-254 — `(&self) -> &Self::Target` — The RAII pattern means buffers auto-return to pool when windows close.
-  `tests` module L258-267 — `-` — The RAII pattern means buffers auto-return to pool when windows close.
-  `test_buffer_class_sizes` function L262-266 — `()` — The RAII pattern means buffers auto-return to pool when windows close.

#### src/gpu/mod.rs

- pub `SharedGpuState` struct L30-47 — `{ instance: wgpu::Instance, adapter: wgpu::Adapter, device: Arc<wgpu::Device>, q...` — Shared GPU resources across all windows
- pub `new` function L51-107 — `() -> Self` — Initialize shared GPU resources
- pub `ensure_shared_pipelines` function L113-118 — `(&mut self, target_format: wgpu::TextureFormat)` — Ensure shared render pipelines are initialized for the given surface format.
- pub `ensure_vello_renderer` function L125-149 — `(&self)` — Ensure the Vello renderer is initialized (lazy initialization)
- pub `vello_renderer_arc` function L152-154 — `(&self) -> Arc<Mutex<Option<vello::Renderer>>>` — Get a clone of the shared Vello renderer Arc for passing to EffectsRenderer
- pub `reset_vello_renderer` function L161-185 — `(&self)` — Reset the Vello renderer to clean up accumulated texture atlas resources.
- pub `WindowGpuState` struct L189-255 — `{ surface: wgpu::Surface<'static>, config: wgpu::SurfaceConfiguration, glyph_cac...` — Per-window GPU state (surface tied to specific window)
- pub `cleanup` function L264-289 — `(&mut self, device: &wgpu::Device)` — Explicitly release GPU resources before dropping.
-  `buffer_pool` module L9 — `-` — Shared and per-window GPU resources for wgpu rendering.
-  `texture_pool` module L10 — `-` — - `texture_pool` - Render target texture pooling by size bucket
-  `SharedGpuState` type L49-186 — `= SharedGpuState` — - `texture_pool` - Render target texture pooling by size bucket
-  `WindowGpuState` type L257-290 — `= WindowGpuState` — - `texture_pool` - Render target texture pooling by size bucket
-  `WindowGpuState` type L292-300 — `impl Drop for WindowGpuState` — - `texture_pool` - Render target texture pooling by size bucket
-  `drop` function L293-299 — `(&mut self)` — - `texture_pool` - Render target texture pooling by size bucket

#### src/gpu/texture_pool.rs

- pub `TextureBucket` struct L14-21 — `{ width: u32, height: u32, format: wgpu::TextureFormat }` — Texture size bucket for pooling
- pub `from_size` function L28-34 — `(width: u32, height: u32, format: wgpu::TextureFormat) -> Self` — Create a bucket for the given dimensions
- pub `TexturePoolStats` struct L58-62 — `{ allocations: u64, reuses: u64, returns: u64 }` — Pool statistics for monitoring
- pub `TexturePool` struct L179-181 — `{ inner: Arc<Mutex<TexturePoolInner>> }` — Texture pool for reusing GPU render targets across window lifecycles
- pub `new` function L189-193 — `(device: Arc<wgpu::Device>, max_per_bucket: usize) -> Self` — Create a new texture pool
- pub `checkout` function L203-224 — `( &self, width: u32, height: u32, format: wgpu::TextureFormat, ) -> Option<Poole...` — Check out a texture from the pool
- pub `stats` function L228-236 — `(&self) -> TexturePoolStats` — Get current pool statistics
- pub `shrink` function L241-248 — `(&self)` — Shrink the pool by releasing excess textures
- pub `PooledTexture` struct L257-266 — `{ texture: Option<wgpu::Texture>, view: Option<wgpu::TextureView>, bucket: Textu...` — A texture checked out from the pool with RAII semantics
- pub `texture` function L270-274 — `(&self) -> &wgpu::Texture` — Get a reference to the underlying texture
- pub `view` function L277-279 — `(&self) -> &wgpu::TextureView` — Get a reference to the texture view
- pub `actual_width` function L283-285 — `(&self) -> u32` — Get the actual requested width (may be smaller than texture width)
- pub `actual_height` function L289-291 — `(&self) -> u32` — Get the actual requested height (may be smaller than texture height)
- pub `bucket_size` function L295-297 — `(&self) -> (u32, u32)` — Get the bucket (power-of-two) dimensions
-  `TextureBucket` type L23-35 — `= TextureBucket` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `align_to_64` function L38-40 — `(n: u32) -> u32` — Round up to next multiple of 64 (GPU-friendly alignment)
-  `TexturePoolInner` struct L43-48 — `{ device: Arc<wgpu::Device>, pools: HashMap<TextureBucket, Vec<TextureEntry>>, m...` — Internal pool state
-  `TextureEntry` struct L51-54 — `{ texture: wgpu::Texture, view: wgpu::TextureView }` — A pooled texture with its view
-  `TexturePoolInner` type L64-168 — `= TexturePoolInner` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `new` function L65-72 — `(device: Arc<wgpu::Device>, max_per_bucket: usize) -> Self` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `checkout` function L74-122 — `( &mut self, width: u32, height: u32, format: wgpu::TextureFormat, ) -> (wgpu::T...` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `return_texture` function L124-150 — `( &mut self, bucket: TextureBucket, texture: wgpu::Texture, view: wgpu::TextureV...` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `shrink` function L152-167 — `(&mut self)` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `TexturePool` type L183-249 — `= TexturePool` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `PooledTexture` type L268-298 — `= PooledTexture` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `PooledTexture` type L300-313 — `impl Drop for PooledTexture` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `drop` function L301-312 — `(&mut self)` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `tests` module L316-341 — `-` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `test_align_to_64` function L320-327 — `()` — Textures are organized by size buckets (power-of-two rounding) and format.
-  `test_texture_bucket` function L330-340 — `()` — Textures are organized by size buckets (power-of-two rounding) and format.

### src/input

> *Semantic summary to be generated by AI agent.*

#### src/input/drag.rs

- pub `DRAG_THRESHOLD` variable L13 — `: f64` — Minimum pixel distance the cursor must move before a drag activates.
- pub `DragDropTarget` enum L17-29 — `Reorder | Merge | Detach | Pending` — What will happen when the user releases the mouse during a tab drag.
- pub `TabDragState` struct L35-48 — `{ tab_id: TabId, source_window_id: WindowId, start_pos: PhysicalPosition<f64>, c...` — State tracking an in-progress tab drag operation.
- pub `new` function L52-65 — `( tab_id: TabId, source_window_id: WindowId, start_pos: PhysicalPosition<f64>, )...` — Create a new drag state in the initial (pending) state.
- pub `exceeds_threshold` function L68-72 — `(&self) -> bool` — Check if the cursor has moved far enough to activate the drag.
- pub `compute_reorder_index` function L84-113 — `( cursor_x: f32, tab_rects: &[crt_renderer::TabRect], dragged_index: usize, ) ->...` — Compute the insertion index for tab reordering based on cursor position.
- pub `WindowScreenRect` struct L117-127 — `{ window_id: WindowId, origin: PhysicalPosition<i32>, size: PhysicalSize<u32>, t...` — A window's screen-space rectangle and tab bar region, used for drop target resolution.
- pub `resolve_drop_target` function L167-223 — `( cursor_screen: PhysicalPosition<f64>, source_window_id: WindowId, dragged_inde...` — Resolve what drop target the cursor is over.
- pub `should_start_drag` function L234-252 — `( tab_bar: &crt_renderer::TabBar, context_menu_visible: bool, x: f32, y: f32, ) ...` — Check whether a tab drag should be initiated for a mouse press at (x, y).
-  `TabDragState` type L50-73 — `= TabDragState` — reordering, detaching, and merging tabs across windows.
-  `WindowScreenRect` type L129-152 — `= WindowScreenRect` — reordering, detaching, and merging tabs across windows.
-  `contains` function L131-137 — `(&self, x: f64, y: f64) -> bool` — Check if a screen-space point is inside this window
-  `tab_bar_contains` function L140-146 — `(&self, x: f64, y: f64) -> bool` — Check if a screen-space point is inside this window's tab bar region
-  `to_local_x` function L149-151 — `(&self, screen_x: f64) -> f32` — Convert screen-space x to window-local x for tab hit testing
-  `tests` module L255-643 — `-` — reordering, detaching, and merging tabs across windows.
-  `make_drag` function L262-267 — `(start_x: f64, start_y: f64) -> TabDragState` — reordering, detaching, and merging tabs across windows.
-  `drag_drop_target_equality` function L270-282 — `()` — reordering, detaching, and merging tabs across windows.
-  `new_drag_state_is_pending_and_inactive` function L285-294 — `()` — reordering, detaching, and merging tabs across windows.
-  `exceeds_threshold_false_when_stationary` function L297-300 — `()` — reordering, detaching, and merging tabs across windows.
-  `exceeds_threshold_false_for_small_movement` function L303-307 — `()` — reordering, detaching, and merging tabs across windows.
-  `exceeds_threshold_true_for_large_movement` function L310-314 — `()` — reordering, detaching, and merging tabs across windows.
-  `exceeds_threshold_diagonal` function L317-322 — `()` — reordering, detaching, and merging tabs across windows.
-  `exceeds_threshold_exactly_at_boundary` function L325-330 — `()` — reordering, detaching, and merging tabs across windows.
-  `make_tab_rects` function L336-348 — `(count: usize) -> Vec<TabRect>` — reordering, detaching, and merging tabs across windows.
-  `reorder_index_empty_rects` function L351-353 — `()` — reordering, detaching, and merging tabs across windows.
-  `reorder_index_drag_first_to_second` function L356-362 — `()` — reordering, detaching, and merging tabs across windows.
-  `reorder_index_drag_last_to_first` function L365-370 — `()` — reordering, detaching, and merging tabs across windows.
-  `make_window_rect` function L374-403 — `( id_num: u64, x: i32, y: i32, w: u32, h: u32, tab_bar_h: f32, num_tabs: usize, ...` — reordering, detaching, and merging tabs across windows.
-  `resolve_outside_all_windows_is_detach` function L406-419 — `()` — reordering, detaching, and merging tabs across windows.
-  `resolve_over_source_tab_bar_is_reorder` function L422-435 — `()` — reordering, detaching, and merging tabs across windows.
-  `resolve_over_window_body_is_detach` function L438-451 — `()` — reordering, detaching, and merging tabs across windows.
-  `resolve_empty_windows_is_detach` function L459-469 — `()` — reordering, detaching, and merging tabs across windows.
-  `reorder_index_stays_in_place` function L472-479 — `()` — reordering, detaching, and merging tabs across windows.
-  `drag_state_transitions_pending_to_active` function L484-498 — `()` — reordering, detaching, and merging tabs across windows.
-  `drag_state_target_updates_continuously` function L501-517 — `()` — reordering, detaching, and merging tabs across windows.
-  `window_screen_rect_contains` function L522-532 — `()` — reordering, detaching, and merging tabs across windows.
-  `window_screen_rect_tab_bar_contains` function L535-543 — `()` — reordering, detaching, and merging tabs across windows.
-  `window_screen_rect_to_local_x` function L546-550 — `()` — reordering, detaching, and merging tabs across windows.
-  `reorder_index_single_tab` function L555-560 — `()` — reordering, detaching, and merging tabs across windows.
-  `reorder_index_cursor_far_right` function L563-568 — `()` — reordering, detaching, and merging tabs across windows.
-  `reorder_index_cursor_far_left` function L571-576 — `()` — reordering, detaching, and merging tabs across windows.
-  `resolve_cursor_at_window_edge_is_in_window` function L581-594 — `()` — reordering, detaching, and merging tabs across windows.
-  `resolve_single_tab_over_source_is_pending` function L597-610 — `()` — reordering, detaching, and merging tabs across windows.
-  `resolve_single_tab_outside_is_pending` function L613-626 — `()` — reordering, detaching, and merging tabs across windows.
-  `resolve_cursor_just_below_tab_bar_on_source_is_detach` function L629-642 — `()` — reordering, detaching, and merging tabs across windows.

#### src/input/key_encoder.rs

- pub `encode_key` function L88-119 — `( key: &Key, ctrl_pressed: bool, shift_pressed: bool, alt_pressed: bool, ) -> Op...` — Encode a winit key event to terminal escape sequence bytes
-  `winit_to_termwiz_keycode` function L13-68 — `(key: &Key) -> Option<KeyCode>` — Convert a winit Key to a termwiz KeyCode
-  `build_modifiers` function L71-83 — `(ctrl: bool, shift: bool, alt: bool) -> TermwizModifiers` — Build termwiz modifiers from individual modifier flags
-  `tests` module L122-310 — `-` — maintaining manual escape sequence mappings.
-  `test_encode_tab` function L126-129 — `()` — maintaining manual escape sequence mappings.
-  `test_encode_shift_tab` function L132-138 — `()` — maintaining manual escape sequence mappings.
-  `test_encode_arrow_keys` function L141-147 — `()` — maintaining manual escape sequence mappings.
-  `test_encode_function_keys` function L150-156 — `()` — maintaining manual escape sequence mappings.
-  `test_encode_character` function L159-162 — `()` — maintaining manual escape sequence mappings.
-  `test_encode_ctrl_c` function L165-169 — `()` — maintaining manual escape sequence mappings.
-  `test_encode_home_end` function L172-185 — `()` — maintaining manual escape sequence mappings.
-  `encode_enter_produces_cr` function L190-193 — `()` — maintaining manual escape sequence mappings.
-  `encode_escape_produces_esc` function L196-199 — `()` — maintaining manual escape sequence mappings.
-  `encode_backspace_produces_del` function L202-205 — `()` — maintaining manual escape sequence mappings.
-  `encode_delete_key` function L208-214 — `()` — maintaining manual escape sequence mappings.
-  `encode_insert_key` function L217-222 — `()` — maintaining manual escape sequence mappings.
-  `encode_page_up_down` function L225-231 — `()` — maintaining manual escape sequence mappings.
-  `encode_left_right_arrows` function L234-239 — `()` — maintaining manual escape sequence mappings.
-  `encode_ctrl_a_through_z` function L242-255 — `()` — maintaining manual escape sequence mappings.
-  `encode_f1_through_f12_all_different` function L258-278 — `()` — maintaining manual escape sequence mappings.
-  `encode_space` function L281-284 — `()` — maintaining manual escape sequence mappings.
-  `encode_alt_character` function L287-293 — `()` — maintaining manual escape sequence mappings.
-  `encode_unknown_named_key_returns_none` function L296-300 — `()` — maintaining manual escape sequence mappings.
-  `encode_shift_arrows_produce_modified_sequences` function L303-309 — `()` — maintaining manual escape sequence mappings.

#### src/input/keyboard.rs

- pub `KeyboardAction` enum L20-51 — `Handled | NotHandled | CloseWindow | CloseTab | NewWindow | NewTab | Quit | Scro...` — Result of keyboard event handling
- pub `InputContext` struct L57-72 — `{ context_menu_visible: bool, tab_editing_active: bool, window_rename_active: bo...` — Read-only context for keyboard action determination.
- pub `from_state` function L76-86 — `(state: &WindowState) -> Self` — Extract input context from window state
- pub `determine_command_shortcut` function L93-134 — `( key: &Key, shift_pressed: bool, ctx: &InputContext, ) -> Option<KeyboardAction...` — Determine the keyboard action for a command shortcut (Cmd/Ctrl + key).
- pub `determine_scroll_action` function L139-159 — `( key: &Key, mod_pressed: bool, shift_pressed: bool, ) -> Option<Scroll>` — Determine the scroll action for a key combination.
- pub `handle_keyboard_input` function L164-295 — `( state: &mut WindowState, key: &Key, text: Option<&str>, modifiers: &Modifiers,...` — Handle keyboard input event
-  `InputContext` type L74-87 — `= InputContext` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `handle_scroll_shortcuts` function L298-316 — `( state: &mut WindowState, key: &Key, mod_pressed: bool, shift_pressed: bool, ) ...` — Handle scroll shortcuts (Shift+PageUp/PageDown/Home/End)
-  `handle_search_input` function L319-368 — `( state: &mut WindowState, key: &Key, mod_pressed: bool, ) -> Option<KeyboardAct...` — Handle search mode input
-  `handle_command_shortcuts` function L371-460 — `( state: &mut WindowState, key: &Key, shift_pressed: bool, ) -> Option<KeyboardA...` — Handle command shortcuts (Cmd/Ctrl + key)
-  `tests` module L463-571 — `-` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `default_ctx` function L466-476 — `() -> InputContext` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_cmd_q_returns_quit` function L479-484 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_cmd_t_returns_new_tab` function L487-492 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_cmd_n_returns_new_window` function L495-500 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_cmd_w_single_tab_returns_close_window` function L503-512 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_cmd_w_multiple_tabs_returns_close_tab` function L515-524 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_cmd_c_returns_copy` function L527-532 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_cmd_f_returns_toggle_search` function L535-540 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_cmd_1_returns_select_tab_0` function L543-548 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_cmd_shift_bracket_prev_tab` function L551-556 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_scroll_shift_pageup` function L559-563 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.
-  `test_scroll_no_shift_returns_none` function L566-570 — `()` — Returns actions that main.rs applies, keeping ownership/lifetime concerns there.

#### src/input/mod.rs

- pub `drag` module L5 — `-` — Keyboard and mouse input processing for terminal and tab bar.
- pub `DetectedUrl` struct L30-41 — `{ url: String, start_col: usize, end_col: usize, line: usize, end_line: usize }` — Detected URL with its position in the terminal (supports multi-line spans)
- pub `detect_urls_in_line` function L63-80 — `(line_text: &str, line_num: usize) -> Vec<DetectedUrl>` — Scan a line of text for URLs and return their positions
- pub `find_url_at_position` function L83-85 — `(urls: &[DetectedUrl], col: usize, line: usize) -> Option<&DetectedUrl>` — Check if a position (col, line) is within a detected URL (supports multi-line URLs)
- pub `find_url_index_at_position` function L88-90 — `(urls: &[DetectedUrl], col: usize, line: usize) -> Option<usize>` — Find the index of a URL at a given position (supports multi-line URLs)
- pub `merge_wrapped_urls` function L117-151 — `(urls: &mut Vec<DetectedUrl>, line_texts: &BTreeMap<i32, String>, cols: usize)` — Merge URLs that wrap across multiple lines
- pub `open_url` function L168-179 — `(url: &str)` — Open a URL in the default browser
- pub `MOUSE_BUTTON_LEFT` variable L187 — `: u8` — Keyboard and mouse input processing for terminal and tab bar.
- pub `MOUSE_BUTTON_MIDDLE` variable L188 — `: u8` — Keyboard and mouse input processing for terminal and tab bar.
- pub `MOUSE_BUTTON_RIGHT` variable L189 — `: u8` — Keyboard and mouse input processing for terminal and tab bar.
- pub `MOUSE_BUTTON_RELEASE` variable L190 — `: u8` — Keyboard and mouse input processing for terminal and tab bar.
- pub `MOUSE_BUTTON_MOTION` variable L191 — `: u8` — Keyboard and mouse input processing for terminal and tab bar.
- pub `MOUSE_BUTTON_SCROLL_UP` variable L192 — `: u8` — Keyboard and mouse input processing for terminal and tab bar.
- pub `MOUSE_BUTTON_SCROLL_DOWN` variable L193 — `: u8` — Keyboard and mouse input processing for terminal and tab bar.
- pub `should_report_mouse` function L196-204 — `(shell: &ShellTerminal) -> bool` — Check if the terminal has mouse reporting enabled
- pub `should_report_motion` function L207-212 — `(shell: &ShellTerminal, button_pressed: bool) -> bool` — Check if the terminal is tracking mouse motion
- pub `is_sgr_mouse_mode` function L215-221 — `(shell: &ShellTerminal) -> bool` — Check if SGR extended mouse mode is enabled
- pub `mouse_report` function L231-246 — `(button: u8, col: usize, line: usize, pressed: bool, sgr_mode: bool) -> Vec<u8>` — Generate mouse escape sequence for terminal
- pub `TabEditResult` enum L249-254 — `Handled | NotHandled` — Result of handling tab editing input
- pub `handle_tab_editing` function L257-316 — `(state: &mut WindowState, key: &Key, mod_pressed: bool) -> TabEditResult` — Handle keyboard input for tab title editing
- pub `handle_shell_input` function L323-434 — `( state: &mut WindowState, key: &Key, text: Option<&str>, mod_pressed: bool, ctr...` — Handle shell input (send to PTY)
- pub `handle_tab_click` function L437-503 — `(state: &mut WindowState, x: f32, y: f32, now: std::time::Instant) -> bool` — Handle mouse click on tab bar
- pub `handle_resize` function L506-596 — `( state: &mut WindowState, shared: &crate::gpu::SharedGpuState, new_width: u32, ...` — Handle window resize
- pub `screen_to_cell` function L600-612 — `(state: &WindowState, x: f32, y: f32) -> Option<(usize, usize)>` — Convert screen coordinates to terminal cell (column, line)
- pub `handle_terminal_mouse_press` function L617-619 — `(state: &mut WindowState, x: f32, y: f32, now: Instant) -> bool` — Handle mouse press for terminal selection or mouse reporting
- pub `handle_terminal_mouse_button` function L623-729 — `( state: &mut WindowState, x: f32, y: f32, now: Instant, button: u8, pressed: bo...` — Handle mouse button press/release for any button
- pub `handle_terminal_mouse_move` function L732-773 — `(state: &mut WindowState, x: f32, y: f32)` — Handle mouse move for terminal selection (dragging) or mouse motion reporting
- pub `handle_terminal_mouse_release` function L776-806 — `(state: &mut WindowState, x: f32, y: f32)` — Handle mouse release for terminal selection or mouse reporting
- pub `handle_terminal_scroll` function L810-839 — `(state: &mut WindowState, x: f32, y: f32, delta_y: f32) -> bool` — Handle mouse scroll wheel for terminal scrollback or mouse reporting
- pub `clear_terminal_selection` function L842-854 — `(state: &mut WindowState)` — Clear terminal selection (e.g., when user types or presses Escape)
- pub `get_terminal_selection_text` function L857-861 — `(state: &WindowState) -> Option<String>` — Get selected text from terminal (for copy)
- pub `get_clipboard_content` function L871-908 — `() -> Option<String>` — Get clipboard content from system clipboard
- pub `set_clipboard_content` function L951-955 — `(text: &str)` — Set clipboard content
- pub `paste_to_terminal` function L961-1012 — `(state: &mut WindowState, content: &str)` — Paste content to terminal with bracketed paste mode support
- pub `scroll_to_current_match` function L1015-1057 — `(state: &mut WindowState)` — Scroll terminal to make current search match visible
- pub `update_search_matches` function L1060-1101 — `(state: &mut WindowState)` — Update search matches based on current query
-  `key_encoder` module L6 — `-` — Keyboard and mouse input processing for terminal and tab bar.
-  `keyboard` module L7 — `-` — Keyboard and mouse input processing for terminal and tab bar.
-  `mouse` module L8 — `-` — Keyboard and mouse input processing for terminal and tab bar.
-  `url_regex` function L44-60 — `() -> &'static Regex` — Get the URL regex (compiled once)
-  `URL_REGEX` variable L45 — `: OnceLock<Regex>` — Keyboard and mouse input processing for terminal and tab bar.
-  `is_position_in_url` function L93-110 — `(url: &DetectedUrl, col: usize, line: usize) -> bool` — Check if a (col, line) position falls within a URL's span
-  `find_url_continuation_end` function L154-165 — `(text: &str) -> usize` — Find where URL-like characters end in a continuation line
-  `MULTI_CLICK_THRESHOLD` variable L182 — `: Duration` — Threshold for multi-click detection
-  `MULTI_CLICK_DISTANCE` variable L184 — `: usize` — Maximum distance (in cells) for multi-click to register
-  `save_clipboard_image_to_temp` function L911-948 — `(image_data: &arboard::ImageData) -> Option<String>` — Save clipboard image data to a temporary file and return the path
-  `tests` module L1104-1323 — `-` — Keyboard and mouse input processing for terminal and tab bar.
-  `test_safe_utf8_truncation` function L1112-1156 — `()` — Test that UTF-8 string truncation handles multi-byte characters safely.
-  `detect_https_url` function L1161-1167 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `detect_http_url` function L1170-1175 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `detect_file_url` function L1178-1182 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `detect_www_prefix` function L1185-1189 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `detect_url_with_query_and_fragment` function L1192-1196 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `detect_multiple_urls_in_line` function L1199-1204 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `detect_no_urls_in_plain_text` function L1207-1210 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `find_url_at_position_hit` function L1213-1216 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `find_url_at_position_miss` function L1219-1225 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `find_url_index_at_position_returns_index` function L1228-1232 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `mouse_report_sgr_press` function L1237-1241 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `mouse_report_sgr_release` function L1244-1248 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `mouse_report_legacy_press` function L1251-1255 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `mouse_report_legacy_release` function L1258-1261 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `mouse_report_scroll_buttons` function L1264-1272 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `mouse_report_legacy_clamps_coordinates` function L1275-1281 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `merge_wrapped_urls_single_line` function L1286-1294 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `merge_wrapped_urls_across_lines` function L1297-1309 — `()` — Keyboard and mouse input processing for terminal and tab bar.
-  `merge_wrapped_urls_stops_at_new_protocol` function L1312-1322 — `()` — Keyboard and mouse input processing for terminal and tab bar.

#### src/input/mouse.rs

- pub `GridLayout` struct L30-45 — `{ content_offset_x: f32, content_offset_y: f32, padding: f32, cell_width: f32, l...` — Layout parameters needed for coordinate conversion
- pub `screen_to_grid_position` function L52-67 — `(x: f32, y: f32, layout: &GridLayout) -> Option<(usize, usize)>` — Convert screen pixel coordinates to terminal grid position (col, line).
- pub `MouseClickTarget` enum L71-92 — `OpenUrl | ContextSubmenuItem | ContextMenuItem | ContextSubmenuParent | DismissC...` — What a mouse click targets
- pub `determine_click_target` function L98-146 — `( button: MouseButton, button_state: ElementState, cmd_pressed: bool, context_me...` — Determine what a mouse click targets, without performing any side effects.
- pub `compute_click_count` function L153-175 — `( now: Instant, last_click_time: Option<Instant>, last_click_pos: Option<(usize,...` — Compute click count for multi-click detection (single, double, triple).
- pub `normalize_scroll_delta` function L181-186 — `(delta: &MouseScrollDelta, line_height: f32) -> f32` — Normalize a pixel scroll delta to a line count.
- pub `handle_cursor_moved` function L193-222 — `(state: &mut WindowState, x: f32, y: f32)` — Handle cursor moved event
- pub `handle_mouse_input` function L227-326 — `( state: &mut WindowState, button: MouseButton, button_state: ElementState, modi...` — Handle mouse button event
- pub `handle_mouse_wheel` function L329-353 — `(state: &mut WindowState, delta: MouseScrollDelta)` — Handle mouse wheel event
-  `grid_layout_from_state` function L358-369 — `(state: &WindowState) -> GridLayout` — Build a `GridLayout` from the current window state.
-  `handle_context_menu_action` function L372-419 — `(state: &mut WindowState, item: ContextMenuItem)` — Handle context menu action (copy, paste, select all)
-  `tests` module L422-646 — `-` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `test_layout` function L425-435 — `() -> GridLayout` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `grid_position_at_origin` function L440-445 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `grid_position_mid_screen` function L448-453 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `grid_position_outside_returns_none` function L456-462 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `grid_position_clamps_to_bounds` function L465-473 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `click_count_single_on_first_click` function L478-490 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `click_count_double_on_rapid_same_position` function L493-506 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `click_count_triple_then_wraps` function L509-536 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `click_count_resets_on_timeout` function L539-552 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `click_count_resets_on_distance` function L555-568 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `click_target_cmd_click_url` function L573-582 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `click_target_right_click_shows_menu` function L585-594 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `click_target_right_click_moves_menu_when_visible` function L597-606 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `click_target_left_press_terminal` function L609-618 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `click_target_release` function L621-630 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `scroll_delta_line_passthrough` function L635-638 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count
-  `scroll_delta_pixel_converts_to_lines` function L641-645 — `()` — - `normalize_scroll_delta` — converts pixel scroll delta to line count

### src/render

> *Semantic summary to be generated by AI agent.*

#### src/render/context_menu.rs

- pub `render` function L9-412 — `( state: &mut WindowState, shared: &SharedGpuState, encoder: &mut wgpu::CommandE...` — Render context menu overlay
-  `push_menu_border` function L415-432 — `( renderer: &mut crt_renderer::RectRenderer, x: f32, y: f32, width: f32, height:...` — Helper to push menu border rectangles

#### src/render/dialogs.rs

- pub `render_search_bar` function L9-168 — `( state: &mut WindowState, shared: &mut SharedGpuState, encoder: &mut wgpu::Comm...` — Render search bar overlay
- pub `render_window_rename` function L171-333 — `( state: &mut WindowState, shared: &mut SharedGpuState, encoder: &mut wgpu::Comm...` — Render window rename input bar overlay

#### src/render/mod.rs

- pub `PtyUpdateResult` struct L20-27 — `{ content_changed: bool, shell_events: Vec<ShellEvent>, title_change: Option<Str...` — Result of processing PTY updates from the active shell
- pub `process_pty_updates` function L33-41 — `(shell: &mut ShellTerminal) -> PtyUpdateResult` — Process PTY output and collect shell events from a shell terminal.
- pub `EffectPatchAction` enum L44-55 — `Apply | Restore` — An effect patch action computed by `compute_effect_patches`.
- pub `compute_effect_patches` function L61-96 — `( overrides: &crate::window::OverrideState, theme: &Theme, ) -> Vec<EffectPatchA...` — Compute which effect patches need to be applied or restored.
- pub `ShellEventOverrides` struct L99-106 — `{ activations: Vec<(OverrideEventType, EventOverride)>, bell_triggered: bool, cl...` — Result of computing shell event overrides from theme configuration
- pub `compute_shell_event_overrides` function L111-161 — `( shell_events: &[ShellEvent], theme: &Theme, ) -> ShellEventOverrides` — Compute theme override activations from shell events.
- pub `render_frame` function L177-1167 — `(state: &mut WindowState, shared: &mut SharedGpuState)` — Render a single frame for a window
-  `context_menu` module L5 — `-` — Multi-pass rendering pipeline for terminal content and effects.
-  `dialogs` module L6 — `-` — Multi-pass rendering pipeline for terminal content and effects.
-  `overlays` module L7 — `-` — Multi-pass rendering pipeline for terminal content and effects.
-  `selection` module L8 — `-` — Multi-pass rendering pipeline for terminal content and effects.
-  `check_effect` macro L68-86 — `-` — Multi-pass rendering pipeline for terminal content and effects.
-  `to_effect_config` function L164-170 — `(source: &T) -> EffectConfig` — Convert a ToEffectConfig implementor to an EffectConfig
-  `VELLO_RESET_INTERVAL` variable L174 — `: u32` — How often to reset vello renderer to clean up atlas resources (in frames)
-  `ASSUMED_DT` variable L211 — `: f32` — Multi-pass rendering pipeline for terminal content and effects.
-  `LOGGED_SPRITE` variable L428-429 — `: std::sync::atomic::AtomicBool` — Multi-pass rendering pipeline for terminal content and effects.
-  `render_tab_titles` function L1170-1297 — `( state: &mut WindowState, shared: &SharedGpuState, encoder: &mut wgpu::CommandE...` — Render tab title text with glow effect
-  `tests` module L1300-1807 — `-` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_no_events_produces_empty` function L1309-1315 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_bell_sets_bell_triggered` function L1318-1324 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_command_success_sets_clear_fail` function L1327-1333 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_command_fail_no_flags` function L1336-1342 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_no_activations_when_theme_has_no_overrides` function L1345-1355 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_bell_activation_when_theme_has_on_bell` function L1358-1369 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_command_fail_activation_when_theme_configured` function L1372-1384 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_command_success_activation` function L1387-1398 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_multiple_events_produce_multiple_activations` function L1401-1416 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_mixed_configured_and_unconfigured` function L1419-1431 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `shell_overrides_event_type_from_shell_event` function L1434-1447 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `override_state_default_empty` function L1452-1456 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `override_state_add_and_has_active` function L1459-1470 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `override_state_add_replaces_same_type` function L1473-1492 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `override_state_different_types_coexist` function L1495-1512 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `override_state_patched_tracking` function L1515-1523 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `override_state_clear_event` function L1526-1545 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `override_state_clear_all` function L1548-1566 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `override_state_update_removes_expired` function L1569-1584 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `override_state_update_keeps_active` function L1587-1599 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `effect_patches_empty_when_no_overrides` function L1604-1609 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `effect_patches_apply_when_starfield_patch_present` function L1612-1631 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `effect_patches_no_double_apply_when_already_patched` function L1634-1654 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `effect_patches_restore_when_patch_removed_and_theme_has_base` function L1657-1667 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `effect_patches_no_restore_when_no_base_theme_config` function L1670-1678 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `effect_patches_multiple_effects` function L1681-1709 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `active_override_long_duration_is_active` function L1714-1725 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `active_override_zero_duration_expired` function L1728-1740 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `pty_update_result_fields` function L1745-1754 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `pty_update_result_no_changes` function L1757-1766 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `effect_patch_action_apply_variant` function L1771-1788 — `()` — Multi-pass rendering pipeline for terminal content and effects.
-  `effect_patch_action_restore_variant` function L1791-1806 — `()` — Multi-pass rendering pipeline for terminal content and effects.

#### src/render/overlays.rs

- pub `render_bell_flash` function L9-58 — `( state: &mut WindowState, shared: &SharedGpuState, encoder: &mut wgpu::CommandE...` — Render bell flash overlay (theme-driven color and intensity)
- pub `render_zoom_indicator` function L61-180 — `( state: &mut WindowState, shared: &SharedGpuState, encoder: &mut wgpu::CommandE...` — Render zoom indicator overlay (centered pill showing zoom percentage)
- pub `render_copy_indicator` function L183-300 — `( state: &mut WindowState, shared: &SharedGpuState, encoder: &mut wgpu::CommandE...` — Render copy indicator ("Copied!" feedback pill)
- pub `render_toast` function L303-435 — `( state: &mut WindowState, shared: &SharedGpuState, encoder: &mut wgpu::CommandE...` — Render toast notification (bottom-centered)

#### src/render/selection.rs

- pub `render_selection_rects` function L11-91 — `( state: &mut WindowState, selection: &SelectionRange, display_offset: i32, )` — Render selection rectangles via RectRenderer (direct, no intermediate texture)

### src/window

> *Semantic summary to be generated by AI agent.*

#### src/window/interaction.rs

- pub `SearchMatch` struct L11-18 — `{ line: i32, start_col: usize, end_col: usize }` — Search match position in terminal
- pub `SearchState` struct L22-31 — `{ active: bool, query: String, matches: Vec<SearchMatch>, current_match: usize }` — Search state for find-in-terminal functionality
- pub `ContextMenuItem` enum L35-44 — `Copy | Paste | SelectAll | Separator | Themes | Theme` — Context menu item
- pub `label` function L48-57 — `(&self) -> String` — Get the display label for this menu item
- pub `shortcut` function L60-77 — `(&self) -> &'static str` — Get the keyboard shortcut hint (or arrow indicator for submenu)
- pub `has_submenu` function L80-82 — `(&self) -> bool` — Returns true if this is a submenu parent
- pub `is_separator` function L85-87 — `(&self) -> bool` — Returns true if this is a separator item
- pub `is_selectable` function L90-92 — `(&self) -> bool` — Returns true if this is a selectable (non-separator) item
- pub `edit_items` function L95-101 — `() -> Vec<ContextMenuItem>` — Get the base edit menu items
- pub `InteractionState` struct L108-127 — `{ cursor_position: (f32, f32), last_click_time: Option<Instant>, last_click_tab:...` — Interaction state (cursor, mouse, selection, URLs)
- pub `ContextMenu` struct L131-159 — `{ visible: bool, x: f32, y: f32, hovered_item: Option<usize>, focused_item: Opti...` — Context menu state
- pub `items` function L163-170 — `(&self) -> Vec<ContextMenuItem>` — Build the main menu items (Themes shown as a parent item, not expanded)
- pub `theme_items` function L173-178 — `(&self) -> Vec<ContextMenuItem>` — Build the theme submenu items
- pub `show` function L181-189 — `(&mut self, x: f32, y: f32)` — Show the context menu at the given position
- pub `hide` function L192-198 — `(&mut self)` — Hide the context menu
- pub `focus_next` function L201-221 — `(&mut self)` — Move focus to the next selectable item (wraps around, skips separators)
- pub `focus_prev` function L224-244 — `(&mut self)` — Move focus to the previous selectable item (wraps around, skips separators)
- pub `get_focused_item` function L247-250 — `(&self) -> Option<ContextMenuItem>` — Get the currently focused item
- pub `contains` function L253-258 — `(&self, x: f32, y: f32) -> bool` — Check if a point is inside the main menu
- pub `contains_submenu` function L261-269 — `(&self, x: f32, y: f32) -> bool` — Check if a point is inside the submenu
- pub `item_at` function L272-280 — `(&self, x: f32, y: f32) -> Option<ContextMenuItem>` — Get the menu item at the given position (main menu only)
- pub `submenu_item_at` function L283-291 — `(&self, x: f32, y: f32) -> Option<ContextMenuItem>` — Get the submenu item at the given position
- pub `is_current_theme` function L294-296 — `(&self, name: &str) -> bool` — Check if a theme name is the currently active theme
- pub `update_hover` function L299-327 — `(&mut self, x: f32, y: f32)` — Update hover state based on mouse position
- pub `themes_item_index` function L330-334 — `(&self) -> Option<usize>` — Get the index of the Themes item in the main menu
-  `ContextMenuItem` type L46-102 — `= ContextMenuItem` — Groups state related to user interaction, mouse handling, search, and context menus.
-  `ContextMenu` type L161-335 — `= ContextMenu` — Groups state related to user interaction, mouse handling, search, and context menus.

#### src/window/mod.rs

- pub `WindowState` struct L37-61 — `{ window: Arc<Window>, gpu: WindowGpuState, shells: HashMap<TabId, ShellTerminal...` — Per-window state containing window handle, GPU state, shells, and interaction state
- pub `set_theme` function L65-94 — `(&mut self, name: &str, theme: Theme)` — Set the theme for this window, updating all GPU resources
- pub `update_text_buffer` function L99-504 — `( &mut self, shared_gpu: &SharedGpuState, ) -> Option<TextBufferUpdateResult>` — Update text buffer for this window's active shell
- pub `create_shell_for_tab` function L507-526 — `(&mut self, tab_id: u64, options: SpawnOptions)` — Create a shell for a new tab with spawn options
- pub `active_shell_cwd` function L529-533 — `(&self) -> Option<std::path::PathBuf>` — Get the current working directory of the active tab's shell
- pub `remove_shell_for_tab` function L536-540 — `(&mut self, tab_id: u64)` — Remove shell for a closed tab
- pub `force_active_tab_redraw` function L543-548 — `(&mut self)` — Force redraw of active tab by clearing its content hash
-  `interaction` module L5 — `-` — Per-window state including shells, GPU resources, and interaction state.
-  `overrides` module L6 — `-` — Per-window state including shells, GPU resources, and interaction state.
-  `render` module L7 — `-` — Per-window state including shells, GPU resources, and interaction state.
-  `types` module L8 — `-` — Per-window state including shells, GPU resources, and interaction state.
-  `ui` module L9 — `-` — Per-window state including shells, GPU resources, and interaction state.
-  `WindowState` type L63-549 — `= WindowState` — Per-window state including shells, GPU resources, and interaction state.

#### src/window/overrides.rs

- pub `OverrideEventType` enum L15-21 — `Bell | CommandSuccess | CommandFail | FocusGained | FocusLost` — Event type that triggered an override
- pub `ActiveOverride` struct L37-44 — `{ event_type: OverrideEventType, properties: EventOverride, triggered_at: Instan...` — Active theme override state (triggered by events like bell, command success/fail)
- pub `new` function L49-55 — `(event_type: OverrideEventType, properties: EventOverride) -> Self` — Create a new active override from an event
- pub `duration_ms` function L58-60 — `(&self) -> u32` — Get the duration of this override in milliseconds
- pub `is_active` function L63-66 — `(&self) -> bool` — Check if the override is still active
- pub `intensity` function L71-82 — `(&self) -> f32` — Get the remaining intensity (1.0 at start, fades to 0.0 at end)
- pub `elapsed` function L85-87 — `(&self) -> Duration` — Get the elapsed time since the override was triggered
- pub `OverrideState` struct L121-126 — `{ active: Vec<ActiveOverride>, patched_effects: HashSet<EffectId> }` — Manager for active theme overrides
- pub `add` function L131-138 — `(&mut self, event_type: OverrideEventType, properties: EventOverride)` — Add a new override, potentially replacing an existing one of the same type
- pub `has_active` function L141-143 — `(&self) -> bool` — Check if any override is active
- pub `update` function L148-158 — `(&mut self) -> bool` — Update state, removing expired overrides
- pub `is_patched` function L161-163 — `(&self, effect: EffectId) -> bool` — Check if an effect is currently patched
- pub `set_patched` function L166-168 — `(&mut self, effect: EffectId)` — Mark an effect as patched
- pub `clear_patched` function L171-173 — `(&mut self, effect: EffectId)` — Clear the patched state for an effect
- pub `get_effective_flash` function L198-208 — `(&self) -> Option<(crt_theme::Color, f32)>` — Get the effective flash for rendering (color and faded intensity)
- pub `clear` function L211-213 — `(&mut self)` — Clear all overrides
- pub `clear_event` function L216-222 — `(&mut self, event_type: OverrideEventType)` — Clear overrides of a specific event type
-  `OverrideEventType` type L23-31 — `= OverrideEventType` — Handles event-triggered temporary theme overrides (bell, command success/fail, focus).
-  `from` function L24-30 — `(event: ShellEvent) -> Self` — Handles event-triggered temporary theme overrides (bell, command success/fail, focus).
-  `ActiveOverride` type L47-88 — `= ActiveOverride` — Handles event-triggered temporary theme overrides (bell, command success/fail, focus).
-  `override_copy_getter` macro L92-102 — `-` — Macro for override getters that return `Option<T>` where T: Copy.
-  `override_ref_getter` macro L105-115 — `-` — Macro for override getters that return `Option<&T>` (reference to non-Copy field).
-  `OverrideState` type L129-223 — `= OverrideState` — Handles event-triggered temporary theme overrides (bell, command success/fail, focus).

#### src/window/render.rs

- pub `CursorInfo` struct L14-27 — `{ x: f32, y: f32, cell_width: f32, cell_height: f32, visible: bool, shape: crt_c...` — Cursor position info returned from text buffer update
- pub `TextDecoration` struct L31-44 — `{ x: f32, y: f32, cell_width: f32, cell_height: f32, color: [f32; 4], kind: Deco...` — Text decoration (underline, strikethrough, or background)
- pub `DecorationKind` enum L48-55 — `Background | Underline | Strikethrough` — Types of text decoration
- pub `TextBufferUpdateResult` struct L58-61 — `{ cursor: CursorInfo, decorations: Vec<TextDecoration> }` — Result from text buffer update
- pub `PreparedCell` struct L78-93 — `{ character: char, x: f32, y: f32, fg_color: [f32; 4], bold: bool, italic: bool,...` — A cell prepared for rendering, with computed colors and routing.
- pub `TerminalRenderData` struct L98-105 — `{ cells: Vec<PreparedCell>, cursor: CursorInfo, decorations: Vec<TextDecoration>...` — Renderer-agnostic terminal render data, prepared from terminal state.
- pub `RenderLayout` struct L108-114 — `{ offset_x: f32, offset_y: f32, padding: f32, cell_width: f32, line_height: f32 ...` — Layout parameters for terminal rendering
- pub `RenderContext` struct L117-133 — `{ layout: RenderLayout, display_offset: i32, cursor_viewport_line: i32, palette:...` — Context for preparing terminal render data (read-only references)
- pub `prepare_render_cells` function L139-303 — `( collected_cells: &[CollectedCell], ctx: &RenderContext<'_>, ) -> (Vec<Prepared...` — Prepare terminal render data from collected cells.
- pub `CachedRenderState` struct L307-320 — `{ decorations: Vec<TextDecoration>, cursor: Option<CursorInfo>, line_texts: std:...` — Cached rendering state that persists across frames
- pub `RenderState` struct L326-339 — `{ dirty: bool, frame_count: u32, occluded: bool, focused: bool, cached: CachedRe...` — Render state (dirty tracking, frame count, visibility)
-  `CollectedCell` struct L66-73 — `{ col: usize, grid_line: i32, c: char, flags: CellFlags, fg: AnsiColor, bg: Ansi...` — Collected cell data for single-pass processing
-  `tests` module L342-435 — `-` — `prepare_render_cells()` function for renderer-agnostic terminal data.
-  `test_prepare_render_cells_smoke` function L347-434 — `()` — `prepare_render_cells()` function for renderer-agnostic terminal data.

#### src/window/types.rs

- pub `TabId` type L7 — `= u64` — Unique identifier for a terminal tab
- pub `EffectId` enum L14-22 — `Starfield | Particles | Grid | Rain | Matrix | Shape | Sprite` — Compile-time-safe identifier for backdrop effects.
- pub `as_str` function L26-36 — `(self) -> &'static str` — Convert to the string key expected by the renderer crate.
-  `EffectId` type L24-37 — `= EffectId` — Basic types and utility functions for window state.
-  `ansi_color_to_rgba` function L40-112 — `( color: AnsiColor, palette: &AnsiPalette, default_fg: [f32; 4], default_bg: [f3...` — Map alacritty_terminal AnsiColor to RGBA array using theme palette

#### src/window/ui.rs

- pub `WindowRenameState` struct L13-18 — `{ active: bool, input: String }` — Window rename input state
- pub `start` function L22-25 — `(&mut self, current_title: &str)` — Start renaming with current window title
- pub `cancel` function L28-31 — `(&mut self)` — Cancel renaming
- pub `confirm` function L34-46 — `(&mut self) -> Option<String>` — Confirm and return the new title
- pub `BellState` struct L51-60 — `{ triggered_at: Option<Instant>, flash_duration: Duration, intensity: f32, enabl...` — Bell visual flash state
- pub `from_config` function L75-82 — `(config: &crate::config::BellConfig) -> Self` — Create from config
- pub `trigger` function L85-89 — `(&mut self)` — Trigger the bell (start visual flash)
- pub `flash_intensity` function L92-110 — `(&self) -> f32` — Get the current flash intensity (0.0 = no flash, up to configured intensity)
- pub `is_active` function L113-115 — `(&self) -> bool` — Check if flash is still active
- pub `UiState` struct L122-141 — `{ search: SearchState, bell: BellState, context_menu: ContextMenu, zoom_indicato...` — UI overlay state (search, bell, context menu, zoom indicator, toast)
- pub `Toast` struct L145-154 — `{ message: String, triggered_at: Option<Instant>, display_duration: Duration, to...` — Toast notification for errors and status messages
- pub `ToastType` enum L158-164 — `Info | Warning | Error` — Type of toast notification
- pub `show` function L168-173 — `(&mut self, message: impl Into<String>, toast_type: ToastType)` — Show a toast message
- pub `is_visible` function L176-180 — `(&self) -> bool` — Check if the toast should be visible
- pub `opacity` function L183-199 — `(&self) -> f32` — Get the opacity (fades out over last 1s)
- pub `ZoomIndicator` struct L204-211 — `{ triggered_at: Option<Instant>, scale: f32, display_duration: Duration }` — Zoom indicator state for font size feedback
- pub `trigger` function L215-219 — `(&mut self, scale: f32)` — Trigger the zoom indicator with the current scale
- pub `is_visible` function L222-226 — `(&self) -> bool` — Check if the indicator should be visible
- pub `opacity` function L229-247 — `(&self) -> f32` — Get the opacity (fades out over last 500ms)
- pub `CopyIndicator` struct L252-257 — `{ triggered_at: Option<Instant>, display_duration: Duration }` — Copy indicator state (shows brief "Copied!" feedback)
- pub `trigger` function L261-264 — `(&mut self)` — Trigger the copy indicator
- pub `is_visible` function L267-271 — `(&self) -> bool` — Check if the indicator should be visible
- pub `opacity` function L274-292 — `(&self) -> f32` — Get the opacity (fades out over last 300ms)
-  `WindowRenameState` type L20-47 — `= WindowRenameState` — search, bell, context menu, zoom indicator, toast, window rename, and theme overrides.
-  `BellState` type L62-71 — `impl Default for BellState` — search, bell, context menu, zoom indicator, toast, window rename, and theme overrides.
-  `default` function L63-70 — `() -> Self` — search, bell, context menu, zoom indicator, toast, window rename, and theme overrides.
-  `BellState` type L73-116 — `= BellState` — search, bell, context menu, zoom indicator, toast, window rename, and theme overrides.
-  `Toast` type L166-200 — `= Toast` — search, bell, context menu, zoom indicator, toast, window rename, and theme overrides.
-  `ZoomIndicator` type L213-248 — `= ZoomIndicator` — search, bell, context menu, zoom indicator, toast, window rename, and theme overrides.
-  `CopyIndicator` type L259-293 — `= CopyIndicator` — search, bell, context menu, zoom indicator, toast, window rename, and theme overrides.

### tests/common

> *Semantic summary to be generated by AI agent.*

#### tests/common/mod.rs

- pub `TestEnvironment` struct L17-22 — `{ temp_dir: TempDir, config_dir: PathBuf }` — Test environment with isolated config directory
- pub `new` function L26-38 — `() -> Self` — Create a new isolated test environment
- pub `write_config` function L41-44 — `(&self, content: &str)` — Write a test config file
- pub `write_theme` function L47-50 — `(&self, name: &str, content: &str)` — Write a test theme file
- pub `config_dir_str` function L53-57 — `(&self) -> &str` — Get config directory path for setting CRT_CONFIG_DIR
- pub `TerminalTestHarness` struct L67-69 — `{ terminal: Terminal }` — Test harness for terminal testing
- pub `new` function L73-76 — `(cols: usize, lines: usize) -> Self` — Create a new terminal test harness
- pub `default_size` function L79-81 — `() -> Self` — Create with default size (80x24)
- pub `terminal` function L84-86 — `(&self) -> &Terminal` — Get the underlying terminal
- pub `terminal_mut` function L89-91 — `(&mut self) -> &mut Terminal` — Get mutable terminal access
- pub `input` function L94-96 — `(&mut self, bytes: &[u8])` — Process input bytes through terminal
- pub `input_str` function L99-101 — `(&mut self, s: &str)` — Process a string as input
- pub `visible_lines` function L104-125 — `(&self) -> Vec<String>` — Get all visible lines as strings
- pub `visible_text` function L128-130 — `(&self) -> String` — Get visible content as a single string (lines joined by newlines)
- pub `cursor_position` function L133-136 — `(&self) -> (i32, usize)` — Get cursor position (row, col)
- pub `assert_cursor_at` function L139-150 — `(&self, row: i32, col: usize)` — Assert cursor is at specific position
- pub `assert_line_contains` function L153-168 — `(&self, line_num: usize, expected: &str)` — Assert line contains specific text
- pub `assert_line_eq` function L171-184 — `(&self, line_num: usize, expected: &str)` — Assert line equals specific text
- pub `ShellTestHarness` struct L188-192 — `{ shell: ShellTerminal, timeout: Duration }` — Test harness for shell integration testing
- pub `new` function L196-202 — `(cols: usize, lines: usize) -> anyhow::Result<Self>` — Create a new shell test harness
- pub `with_shell` function L205-211 — `(cols: usize, lines: usize, shell_path: &str) -> anyhow::Result<Self>` — Create with a specific shell
- pub `default_size` function L214-216 — `() -> anyhow::Result<Self>` — Create with default size (80x24)
- pub `shell` function L219-221 — `(&self) -> &ShellTerminal` — Get the underlying shell terminal
- pub `shell_mut` function L224-226 — `(&mut self) -> &mut ShellTerminal` — Get mutable shell terminal access
- pub `wait_for_ready` function L231-252 — `(&mut self)` — Wait for the shell to be ready (prompt displayed).
- pub `send` function L255-257 — `(&self, input: &str)` — Send input to the shell
- pub `send_bytes` function L260-262 — `(&self, input: &[u8])` — Send input bytes to the shell
- pub `execute` function L265-271 — `(&mut self, command: &str) -> anyhow::Result<String>` — Send a command and wait for it to complete (waits for shell prompt)
- pub `process_output` function L274-276 — `(&mut self) -> bool` — Process pending PTY output and return true if any was processed
- pub `wait_for_stable_output` function L279-313 — `( &mut self, stability_duration: Duration, ) -> anyhow::Result<String>` — Wait for output to stabilize (no new output for given duration)
- pub `wait_for_text` function L316-343 — `(&mut self, expected: &str) -> anyhow::Result<()>` — Wait for specific text to appear in terminal output
- pub `visible_lines` function L346-366 — `(&self) -> Vec<String>` — Get visible lines as strings
- pub `all_content` function L369-377 — `(&self) -> String` — Get all terminal content (including history)
- pub `MemoryStats` struct L382-387 — `{ rss: Option<u64>, vsize: Option<u64> }` — Memory statistics for performance testing
- pub `current` function L392-415 — `() -> Self` — Get current memory stats for this process (macOS)
- pub `current` function L419-422 — `() -> Self` — Get current memory stats (fallback for non-macOS)
- pub `rss_human` function L425-427 — `(&self) -> String` — Format RSS in human-readable form
- pub `vsize_human` function L430-432 — `(&self) -> String` — Format virtual size in human-readable form
- pub `Timer` struct L453-456 — `{ start: Instant, name: String }` — Simple performance timer
- pub `new` function L460-465 — `(name: &str) -> Self` — Start a new timer with a name
- pub `elapsed` function L468-470 — `(&self) -> Duration` — Get elapsed time
- pub `stop` function L473-477 — `(self) -> Duration` — Stop and report elapsed time
-  `TestEnvironment` type L24-58 — `= TestEnvironment` — - Assertion utilities
-  `TestEnvironment` type L60-64 — `impl Default for TestEnvironment` — - Assertion utilities
-  `default` function L61-63 — `() -> Self` — - Assertion utilities
-  `TerminalTestHarness` type L71-185 — `= TerminalTestHarness` — - Assertion utilities
-  `ShellTestHarness` type L194-378 — `= ShellTestHarness` — - Assertion utilities
-  `MemoryStats` type L389-433 — `= MemoryStats` — - Assertion utilities
-  `format_bytes` function L436-450 — `(bytes: u64) -> String` — Format bytes in human-readable form
-  `KB` variable L437 — `: u64` — - Assertion utilities
-  `MB` variable L438 — `: u64` — - Assertion utilities
-  `GB` variable L439 — `: u64` — - Assertion utilities
-  `Timer` type L458-478 — `= Timer` — - Assertion utilities
-  `tests` module L481-541 — `-` — - Assertion utilities
-  `test_environment_creation` function L485-489 — `()` — - Assertion utilities
-  `test_environment_write_config` function L492-501 — `()` — - Assertion utilities
-  `test_terminal_harness_basic` function L504-511 — `()` — - Assertion utilities
-  `test_terminal_harness_visible_lines` function L514-522 — `()` — - Assertion utilities
-  `test_memory_stats` function L525-532 — `()` — - Assertion utilities
-  `test_timer` function L535-540 — `()` — - Assertion utilities

### tests

> *Semantic summary to be generated by AI agent.*

#### tests/shell_tests.rs

-  `common` module L6 — `-` — Tests real shell interaction via PTY.
-  `test_shell_spawn` function L14-20 — `()` — These tests spawn actual shell processes.
-  `test_shell_echo` function L23-36 — `()` — These tests spawn actual shell processes.
-  `test_shell_multiple_commands` function L39-54 — `()` — These tests spawn actual shell processes.
-  `test_shell_pwd` function L57-67 — `()` — These tests spawn actual shell processes.
-  `test_shell_term_env` function L72-84 — `()` — These tests spawn actual shell processes.
-  `test_shell_colorterm_env` function L87-99 — `()` — These tests spawn actual shell processes.
-  `test_shell_resize` function L104-124 — `()` — These tests spawn actual shell processes.
-  `test_shell_exit_status` function L129-142 — `()` — These tests spawn actual shell processes.
-  `test_shell_ctrl_c` function L147-166 — `()` — These tests spawn actual shell processes.
-  `test_shell_ctrl_d_partial` function L169-186 — `()` — These tests spawn actual shell processes.
-  `test_terminal_history` function L191-217 — `()` — These tests spawn actual shell processes.
-  `test_terminal_scroll` function L220-247 — `()` — These tests spawn actual shell processes.
-  `test_selection_shell_output` function L252-278 — `()` — These tests spawn actual shell processes.
-  `test_shell_cat` function L283-295 — `()` — These tests spawn actual shell processes.
-  `test_shell_pipe` function L300-311 — `()` — These tests spawn actual shell processes.
-  `test_shell_long_line` function L316-331 — `()` — These tests spawn actual shell processes.

#### tests/terminal_tests.rs

-  `common` module L6 — `-` — Tests terminal behavior without needing a real shell/PTY.
-  `test_simple_text_output` function L14-22 — `()` — These tests run headlessly and are fast.
-  `test_multiline_output` function L25-33 — `()` — These tests run headlessly and are fast.
-  `test_carriage_return` function L36-43 — `()` — These tests run headlessly and are fast.
-  `test_line_wrap` function L46-55 — `()` — These tests run headlessly and are fast.
-  `test_cursor_movement_down` function L60-67 — `()` — These tests run headlessly and are fast.
-  `test_cursor_movement_right` function L70-77 — `()` — These tests run headlessly and are fast.
-  `test_cursor_absolute_position` function L80-87 — `()` — These tests run headlessly and are fast.
-  `test_clear_screen` function L90-99 — `()` — These tests run headlessly and are fast.
-  `test_clear_line` function L102-111 — `()` — These tests run headlessly and are fast.
-  `test_insert_lines` function L114-127 — `()` — These tests run headlessly and are fast.
-  `test_sgr_bold` function L132-142 — `()` — These tests run headlessly and are fast.
-  `test_sgr_colors` function L145-154 — `()` — These tests run headlessly and are fast.
-  `test_sgr_256_color` function L157-166 — `()` — These tests run headlessly and are fast.
-  `test_sgr_truecolor` function L169-178 — `()` — These tests run headlessly and are fast.
-  `test_osc_set_title` function L183-195 — `()` — These tests run headlessly and are fast.
-  `test_bell` function L198-208 — `()` — These tests run headlessly and are fast.
-  `test_osc133_prompt_detection` function L213-221 — `()` — These tests run headlessly and are fast.
-  `test_osc133_input_detection` function L224-232 — `()` — These tests run headlessly and are fast.
-  `test_osc133_full_cycle` function L235-248 — `()` — These tests run headlessly and are fast.
-  `test_scroll_region` function L253-266 — `()` — These tests run headlessly and are fast.
-  `test_selection_basic` function L271-292 — `()` — These tests run headlessly and are fast.
-  `test_selection_clear` function L295-308 — `()` — These tests run headlessly and are fast.
-  `test_selection_in_scrollback_history` function L313-363 — `()` — These tests run headlessly and are fast.
-  `test_selection_multiline_in_scrollback` function L366-405 — `()` — These tests run headlessly and are fast.
-  `test_selection_spanning_history_and_visible` function L408-446 — `()` — These tests run headlessly and are fast.
-  `test_selection_coordinates_at_scroll_bottom` function L449-476 — `()` — These tests run headlessly and are fast.
-  `test_viewport_to_grid_coordinate_conversion` function L479-523 — `()` — These tests run headlessly and are fast.
-  `test_selection_after_scroll_then_scroll_back` function L526-568 — `()` — These tests run headlessly and are fast.
-  `test_line_selection_in_scrollback` function L571-604 — `()` — These tests run headlessly and are fast.
-  `test_semantic_selection_in_scrollback` function L607-642 — `()` — These tests run headlessly and are fast.
-  `test_resize_terminal` function L647-658 — `()` — These tests run headlessly and are fast.
-  `test_resize_preserves_content` function L661-672 — `()` — These tests run headlessly and are fast.
-  `test_damage_tracking` function L677-688 — `()` — These tests run headlessly and are fast.
-  `test_tab_character` function L693-704 — `()` — These tests run headlessly and are fast.
-  `test_backspace` function L709-720 — `()` — These tests run headlessly and are fast.
-  `test_delete_character` function L725-735 — `()` — These tests run headlessly and are fast.
-  `test_large_output` function L740-753 — `()` — These tests run headlessly and are fast.
-  `test_unicode_basic` function L758-767 — `()` — These tests run headlessly and are fast.
-  `test_unicode_emoji` function L770-777 — `()` — These tests run headlessly and are fast.
-  `test_unicode_combining` function L780-788 — `()` — These tests run headlessly and are fast.

#### tests/visual_tests.rs

-  `project_root` function L25-27 — `() -> std::path::PathBuf` — Project root for resolving golden/failure paths.
-  `load_test_font` function L30-71 — `() -> Vec<u8>` — Load a monospace font for testing.
-  `VisualTestContext` struct L74-81 — `{ headless: HeadlessRenderer, glyph_cache: GlyphCache, grid_renderer: GridRender...` — Test rendering context: headless renderer + glyph cache + renderers.
-  `VisualTestContext` type L83-258 — `= VisualTestContext` — ```
-  `new` function L84-116 — `(width: u32, height: u32) -> Option<Self>` — ```
-  `draw_text` function L119-121 — `(&mut self, text: &str, col: usize, row: usize, color: [f32; 4])` — Render text at a grid position (col, row) with the given color.
-  `draw_text_styled` function L124-145 — `( &mut self, text: &str, col: usize, row: usize, color: [f32; 4], style: GlyphSt...` — Render styled text at a grid position.
-  `draw_rect` function L148-150 — `(&mut self, x: f32, y: f32, w: f32, h: f32, color: [f32; 4])` — Draw a filled rectangle at pixel coordinates.
-  `draw_cell_bg` function L153-160 — `(&mut self, col: usize, row: usize, width: usize, color: [f32; 4])` — Draw a cell background at grid position.
-  `render_and_capture` function L163-249 — `(&mut self) -> Vec<u8>` — Execute render passes and capture as PNG.
-  `cell_width` function L251-253 — `(&self) -> f32` — ```
-  `line_height` function L255-257 — `(&self) -> f32` — ```
-  `assert_golden` function L261-263 — `(actual_png: &[u8], test_name: &str)` — Assert that a rendered PNG matches its golden file.
-  `assert_golden_with_tolerance` function L265-267 — `(actual_png: &[u8], test_name: &str, tolerance: f64)` — ```
-  `visual_text_basic_ascii` function L275-292 — `()` — Basic ASCII text rendering - white text on dark background.
-  `visual_text_styles` function L296-331 — `()` — Bold and italic text styles.
-  `visual_text_ansi_colors` function L335-362 — `()` — ANSI 16-color text rendering.
-  `visual_text_unicode` function L366-392 — `()` — Unicode text rendering: box-drawing, CJK, emoji.
-  `visual_cursor_block` function L400-421 — `()` — Block cursor rendering.
-  `visual_cursor_beam` function L425-446 — `()` — Beam (bar) cursor rendering.
-  `visual_cursor_underline` function L450-471 — `()` — Underline cursor rendering.
-  `visual_selection_single_line` function L479-498 — `()` — Single-line selection highlight.
-  `visual_selection_multi_line` function L502-525 — `()` — Multi-line selection highlight.
-  `visual_tab_single` function L533-562 — `()` — Single tab display.
-  `visual_tab_multiple` function L566-601 — `()` — Multiple tabs with active indicator.
-  `visual_tab_truncation` function L605-631 — `()` — Long tab title truncation.
-  `render_with_crt_effect` function L639-788 — `(width: u32, height: u32, crt_uniforms: CrtUniforms) -> Option<Vec<u8>>` — Helper: Render text content to a source texture, then apply CRT post-processing
-  `visual_crt_scanlines` function L792-814 — `()` — CRT scanline effect at different intensities.
-  `visual_crt_curvature` function L818-840 — `()` — CRT screen curvature effect.
-  `visual_crt_vignette` function L844-866 — `()` — CRT vignette effect.
-  `visual_crt_glow` function L870-892 — `()` — CRT glow/bloom on text.
-  `visual_theme_default` function L900-927 — `()` — Default theme: dark background with standard terminal colors.
-  `visual_theme_custom_colors` function L931-956 — `()` — Custom theme with overridden colors.
-  `visual_theme_background_gradient` function L960-1008 — `()` — Background gradient rendering test.

